{
    "name": "Branching and control flow",
    "description": "Instructions run one after the other until something jumps somewhere else. An `if` in C becomes a `goto`, and a `goto` becomes a compare and a conditional branch.",
    "order": 0,
    "topic": "branching"
}
