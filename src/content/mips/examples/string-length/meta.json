{
    "name": "Byte length of a string",
    "description": "Measure a zero-terminated string in bytes by subtracting its start address from its terminator address.",
    "order": 6,
    "topic": "string-length"
}
