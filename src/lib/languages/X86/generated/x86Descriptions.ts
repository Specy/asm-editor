/**
 * What each instruction does, for the instructions an appendix of NASM once documented.
 *
 * Generated by `node scripts/x86-docs-generate.mjs`; edit that script, never this file.
 *
 * Prose: NASM's "x86 Instruction Reference" appendix (`doc/insref.src`), last shipped in
 * nasm-2.05.01 and removed in 2.06. Copyright 1996-2008 the NASM Authors, under the LGPL that
 * NASM carried at that release; NASM relicensed to BSD-2-Clause in 2.07. Converted from NASM
 * documentation markup to markdown, otherwise unchanged.
 * 
 * It predates long mode, so it describes behaviour and never operand forms: the forms come
 * from the current table in `x86Instructions.ts`.
 */

export type X86Description = {
    /** The appendix's own heading, e.g. `\`ADD\`: Add Integers`. */
    title: string
    /** Markdown. */
    description: string
}

export const X86_DESCRIPTIONS: Record<string, X86Description> = {
    "aaa": {"title":"`AAA`, `AAS`, `AAM`, `AAD`: ASCII Adjustments","description":"These instructions are used in conjunction with the add, subtract,\nmultiply and divide instructions to perform binary-coded decimal\narithmetic in *unpacked* (one BCD digit per byte - easy to\ntranslate to and from `ASCII`, hence the instruction names) form.\nThere are also packed BCD instructions `DAA` and `DAS`: see\n`DAA`.\n\n- `AAA` (ASCII Adjust After Addition) should be used after a\none-byte `ADD` instruction whose destination was the `AL`\nregister: by means of examining the value in the low nibble of\n`AL` and also the auxiliary carry flag `AF`, it determines\nwhether the addition has overflowed, and adjusts it (and sets\nthe carry flag) if so. You can add long BCD strings together\nby doing `ADD`/`AAA` on the low digits, then doing\n`ADC`/`AAA` on each subsequent digit.\n\n- `AAS` (ASCII Adjust AL After Subtraction) works similarly to\n`AAA`, but is for use after `SUB` instructions rather than\n`ADD`.\n\n- `AAM` (ASCII Adjust AX After Multiply) is for use after you\nhave multiplied two decimal digits together and left the result\nin `AL`: it divides `AL` by ten and stores the quotient in\n`AH`, leaving the remainder in `AL`. The divisor 10 can be\nchanged by specifying an operand to the instruction: a particularly\nhandy use of this is `AAM 16`, causing the two nibbles in `AL`\nto be separated into `AH` and `AL`.\n\n- `AAD` (ASCII Adjust AX Before Division) performs the inverse\noperation to `AAM`: it multiplies `AH` by ten, adds it to\n`AL`, and sets `AH` to zero. Again, the multiplier 10 can\nbe changed."},
    "aad": {"title":"`AAA`, `AAS`, `AAM`, `AAD`: ASCII Adjustments","description":"These instructions are used in conjunction with the add, subtract,\nmultiply and divide instructions to perform binary-coded decimal\narithmetic in *unpacked* (one BCD digit per byte - easy to\ntranslate to and from `ASCII`, hence the instruction names) form.\nThere are also packed BCD instructions `DAA` and `DAS`: see\n`DAA`.\n\n- `AAA` (ASCII Adjust After Addition) should be used after a\none-byte `ADD` instruction whose destination was the `AL`\nregister: by means of examining the value in the low nibble of\n`AL` and also the auxiliary carry flag `AF`, it determines\nwhether the addition has overflowed, and adjusts it (and sets\nthe carry flag) if so. You can add long BCD strings together\nby doing `ADD`/`AAA` on the low digits, then doing\n`ADC`/`AAA` on each subsequent digit.\n\n- `AAS` (ASCII Adjust AL After Subtraction) works similarly to\n`AAA`, but is for use after `SUB` instructions rather than\n`ADD`.\n\n- `AAM` (ASCII Adjust AX After Multiply) is for use after you\nhave multiplied two decimal digits together and left the result\nin `AL`: it divides `AL` by ten and stores the quotient in\n`AH`, leaving the remainder in `AL`. The divisor 10 can be\nchanged by specifying an operand to the instruction: a particularly\nhandy use of this is `AAM 16`, causing the two nibbles in `AL`\nto be separated into `AH` and `AL`.\n\n- `AAD` (ASCII Adjust AX Before Division) performs the inverse\noperation to `AAM`: it multiplies `AH` by ten, adds it to\n`AL`, and sets `AH` to zero. Again, the multiplier 10 can\nbe changed."},
    "aam": {"title":"`AAA`, `AAS`, `AAM`, `AAD`: ASCII Adjustments","description":"These instructions are used in conjunction with the add, subtract,\nmultiply and divide instructions to perform binary-coded decimal\narithmetic in *unpacked* (one BCD digit per byte - easy to\ntranslate to and from `ASCII`, hence the instruction names) form.\nThere are also packed BCD instructions `DAA` and `DAS`: see\n`DAA`.\n\n- `AAA` (ASCII Adjust After Addition) should be used after a\none-byte `ADD` instruction whose destination was the `AL`\nregister: by means of examining the value in the low nibble of\n`AL` and also the auxiliary carry flag `AF`, it determines\nwhether the addition has overflowed, and adjusts it (and sets\nthe carry flag) if so. You can add long BCD strings together\nby doing `ADD`/`AAA` on the low digits, then doing\n`ADC`/`AAA` on each subsequent digit.\n\n- `AAS` (ASCII Adjust AL After Subtraction) works similarly to\n`AAA`, but is for use after `SUB` instructions rather than\n`ADD`.\n\n- `AAM` (ASCII Adjust AX After Multiply) is for use after you\nhave multiplied two decimal digits together and left the result\nin `AL`: it divides `AL` by ten and stores the quotient in\n`AH`, leaving the remainder in `AL`. The divisor 10 can be\nchanged by specifying an operand to the instruction: a particularly\nhandy use of this is `AAM 16`, causing the two nibbles in `AL`\nto be separated into `AH` and `AL`.\n\n- `AAD` (ASCII Adjust AX Before Division) performs the inverse\noperation to `AAM`: it multiplies `AH` by ten, adds it to\n`AL`, and sets `AH` to zero. Again, the multiplier 10 can\nbe changed."},
    "aas": {"title":"`AAA`, `AAS`, `AAM`, `AAD`: ASCII Adjustments","description":"These instructions are used in conjunction with the add, subtract,\nmultiply and divide instructions to perform binary-coded decimal\narithmetic in *unpacked* (one BCD digit per byte - easy to\ntranslate to and from `ASCII`, hence the instruction names) form.\nThere are also packed BCD instructions `DAA` and `DAS`: see\n`DAA`.\n\n- `AAA` (ASCII Adjust After Addition) should be used after a\none-byte `ADD` instruction whose destination was the `AL`\nregister: by means of examining the value in the low nibble of\n`AL` and also the auxiliary carry flag `AF`, it determines\nwhether the addition has overflowed, and adjusts it (and sets\nthe carry flag) if so. You can add long BCD strings together\nby doing `ADD`/`AAA` on the low digits, then doing\n`ADC`/`AAA` on each subsequent digit.\n\n- `AAS` (ASCII Adjust AL After Subtraction) works similarly to\n`AAA`, but is for use after `SUB` instructions rather than\n`ADD`.\n\n- `AAM` (ASCII Adjust AX After Multiply) is for use after you\nhave multiplied two decimal digits together and left the result\nin `AL`: it divides `AL` by ten and stores the quotient in\n`AH`, leaving the remainder in `AL`. The divisor 10 can be\nchanged by specifying an operand to the instruction: a particularly\nhandy use of this is `AAM 16`, causing the two nibbles in `AL`\nto be separated into `AH` and `AL`.\n\n- `AAD` (ASCII Adjust AX Before Division) performs the inverse\noperation to `AAM`: it multiplies `AH` by ten, adds it to\n`AL`, and sets `AH` to zero. Again, the multiplier 10 can\nbe changed."},
    "adc": {"title":"`ADC`: Add with Carry","description":"`ADC` performs integer addition: it adds its two operands\ntogether, plus the value of the carry flag, and leaves the result in\nits destination (first) operand. The destination operand can be a\nregister or a memory location. The source operand can be a register,\na memory location or an immediate value.\n\nThe flags are set according to the result of the operation: in\nparticular, the carry flag is affected and can be used by a\nsubsequent `ADC` instruction.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction.\n\nTo add two numbers without also adding the contents of the carry\nflag, use `ADD` (`ADD`)."},
    "add": {"title":"`ADD`: Add Integers","description":"`ADD` performs integer addition: it adds its two operands\ntogether, and leaves the result in its destination (first) operand.\nThe destination operand can be a register or a memory location.\nThe source operand can be a register, a memory location or an\nimmediate value.\n\nThe flags are set according to the result of the operation: in\nparticular, the carry flag is affected and can be used by a\nsubsequent `ADC` instruction.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction."},
    "addpd": {"title":"`ADDPD`: ADD Packed Double-Precision FP Values","description":"`ADDPD` performs addition on each of two packed double-precision\nFP value pairs.\n\n```\n   dst[0-63]   := dst[0-63]   + src[0-63],\n   dst[64-127] := dst[64-127] + src[64-127].\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location."},
    "addps": {"title":"`ADDPS`: ADD Packed Single-Precision FP Values","description":"`ADDPS` performs addition on each of four packed single-precision\nFP value pairs\n\n```\n   dst[0-31]   := dst[0-31]   + src[0-31],\n   dst[32-63]  := dst[32-63]  + src[32-63],\n   dst[64-95]  := dst[64-95]  + src[64-95],\n   dst[96-127] := dst[96-127] + src[96-127].\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location."},
    "addsd": {"title":"`ADDSD`: ADD Scalar Double-Precision FP Values","description":"`ADDSD` adds the low double-precision FP values from the source\nand destination operands and stores the double-precision FP result\nin the destination operand.\n\n```\n   dst[0-63]   := dst[0-63] + src[0-63],\n   dst[64-127) remains unchanged.\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 64-bit memory location."},
    "addss": {"title":"`ADDSS`: ADD Scalar Single-Precision FP Values","description":"`ADDSS` adds the low single-precision FP values from the source\nand destination operands and stores the single-precision FP result\nin the destination operand.\n\n```\n   dst[0-31]   := dst[0-31] + src[0-31],\n   dst[32-127] remains unchanged.\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 32-bit memory location."},
    "and": {"title":"`AND`: Bitwise AND","description":"`AND` performs a bitwise AND operation between its two operands\n(i.e. each bit of the result is 1 if and only if the corresponding\nbits of the two inputs were both 1), and stores the result in the\ndestination (first) operand. The destination operand can be a\nregister or a memory location. The source operand can be a register,\na memory location or an immediate value.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction.\n\nThe `MMX` instruction `PAND` (see `PAND`) performs the same\noperation on the 64-bit `MMX` registers."},
    "andnpd": {"title":"`ANDNPD`: Bitwise Logical AND NOT of Packed Double-Precision FP Values","description":"`ANDNPD` inverts the bits of the two double-precision\nfloating-point values in the destination register, and then\nperforms a logical AND between the two double-precision\nfloating-point values in the source operand and the temporary\ninverted result, storing the result in the destination register.\n\n```\n   dst[0-63]   := src[0-63]   AND NOT dst[0-63],\n   dst[64-127] := src[64-127] AND NOT dst[64-127].\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location."},
    "andnps": {"title":"`ANDNPS`: Bitwise Logical AND NOT of Packed Single-Precision FP Values","description":"`ANDNPS` inverts the bits of the four single-precision\nfloating-point values in the destination register, and then\nperforms a logical AND between the four single-precision\nfloating-point values in the source operand and the temporary\ninverted result, storing the result in the destination register.\n\n```\n   dst[0-31]   := src[0-31]   AND NOT dst[0-31],\n   dst[32-63]  := src[32-63]  AND NOT dst[32-63],\n   dst[64-95]  := src[64-95]  AND NOT dst[64-95],\n   dst[96-127] := src[96-127] AND NOT dst[96-127].\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location."},
    "andpd": {"title":"`ANDPD`: Bitwise Logical AND For Single FP","description":"`ANDPD` performs a bitwise logical AND of the two double-precision\nfloating point values in the source and destination operand, and\nstores the result in the destination register.\n\n```\n   dst[0-63]   := src[0-63]   AND dst[0-63],\n   dst[64-127] := src[64-127] AND dst[64-127].\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location."},
    "andps": {"title":"`ANDPS`: Bitwise Logical AND For Single FP","description":"`ANDPS` performs a bitwise logical AND of the four single-precision\nfloating point values in the source and destination operand, and\nstores the result in the destination register.\n\n```\n   dst[0-31]   := src[0-31]   AND dst[0-31],\n   dst[32-63]  := src[32-63]  AND dst[32-63],\n   dst[64-95]  := src[64-95]  AND dst[64-95],\n   dst[96-127] := src[96-127] AND dst[96-127].\n```\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location."},
    "arpl": {"title":"`ARPL`: Adjust RPL Field of Selector","description":"`ARPL` expects its two word operands to be segment selectors. It\nadjusts the `RPL` (requested privilege level - stored in the bottom\ntwo bits of the selector) field of the destination (first) operand\nto ensure that it is no less (i.e. no more privileged than) the `RPL`\nfield of the source operand. The zero flag is set if and only if a\nchange had to be made."},
    "bound": {"title":"`BOUND`: Check Array Index against Bounds","description":"`BOUND` expects its second operand to point to an area of memory\ncontaining two signed values of the same size as its first operand\n(i.e. two words for the 16-bit form; two doublewords for the 32-bit\nform). It performs two signed comparisons: if the value in the\nregister passed as its first operand is less than the first of the\nin-memory values, or is greater than or equal to the second, it\nthrows a `BR` exception. Otherwise, it does nothing."},
    "bsf": {"title":"`BSF`, `BSR`: Bit Scan","description":"- `BSF` searches for the least significant set bit in its source\n(second) operand, and if it finds one, stores the index in\nits destination (first) operand. If no set bit is found, the\ncontents of the destination operand are undefined. If the source\noperand is zero, the zero flag is set.\n\n- `BSR` performs the same function, but searches from the top\ninstead, so it finds the most significant set bit.\n\nBit indices are from 0 (least significant) to 15 or 31 (most\nsignificant). The destination operand can only be a register.\nThe source operand can be a register or a memory location."},
    "bsr": {"title":"`BSF`, `BSR`: Bit Scan","description":"- `BSF` searches for the least significant set bit in its source\n(second) operand, and if it finds one, stores the index in\nits destination (first) operand. If no set bit is found, the\ncontents of the destination operand are undefined. If the source\noperand is zero, the zero flag is set.\n\n- `BSR` performs the same function, but searches from the top\ninstead, so it finds the most significant set bit.\n\nBit indices are from 0 (least significant) to 15 or 31 (most\nsignificant). The destination operand can only be a register.\nThe source operand can be a register or a memory location."},
    "bswap": {"title":"`BSWAP`: Byte Swap","description":"`BSWAP` swaps the order of the four bytes of a 32-bit register:\nbits 0-7 exchange places with bits 24-31, and bits 8-15 swap with\nbits 16-23. There is no explicit 16-bit equivalent: to byte-swap\n`AX`, `BX`, `CX` or `DX`, `XCHG` can be used. When `BSWAP`\nis used with a 16-bit register, the result is undefined."},
    "bt": {"title":"`BT`, `BTC`, `BTR`, `BTS`: Bit Test","description":"These instructions all test one bit of their first operand, whose\nindex is given by the second operand, and store the value of that\nbit into the carry flag. Bit indices are from 0 (least significant)\nto 15 or 31 (most significant).\n\nIn addition to storing the original value of the bit into the carry\nflag, `BTR` also resets (clears) the bit in the operand itself.\n`BTS` sets the bit, and `BTC` complements the bit. `BT` does\nnot modify its operands.\n\nThe destination can be a register or a memory location. The source can\nbe a register or an immediate value.\n\nIf the destination operand is a register, the bit offset should be\nin the range 0-15 (for 16-bit operands) or 0-31 (for 32-bit operands).\nAn immediate value outside these ranges will be taken modulo 16/32\nby the processor.\n\nIf the destination operand is a memory location, then an immediate\nbit offset follows the same rules as for a register. If the bit offset\nis in a register, then it can be anything within the signed range of\nthe register used (ie, for a 32-bit operand, it can be (-2^31) to (2^31 - 1)"},
    "btc": {"title":"`BT`, `BTC`, `BTR`, `BTS`: Bit Test","description":"These instructions all test one bit of their first operand, whose\nindex is given by the second operand, and store the value of that\nbit into the carry flag. Bit indices are from 0 (least significant)\nto 15 or 31 (most significant).\n\nIn addition to storing the original value of the bit into the carry\nflag, `BTR` also resets (clears) the bit in the operand itself.\n`BTS` sets the bit, and `BTC` complements the bit. `BT` does\nnot modify its operands.\n\nThe destination can be a register or a memory location. The source can\nbe a register or an immediate value.\n\nIf the destination operand is a register, the bit offset should be\nin the range 0-15 (for 16-bit operands) or 0-31 (for 32-bit operands).\nAn immediate value outside these ranges will be taken modulo 16/32\nby the processor.\n\nIf the destination operand is a memory location, then an immediate\nbit offset follows the same rules as for a register. If the bit offset\nis in a register, then it can be anything within the signed range of\nthe register used (ie, for a 32-bit operand, it can be (-2^31) to (2^31 - 1)"},
    "btr": {"title":"`BT`, `BTC`, `BTR`, `BTS`: Bit Test","description":"These instructions all test one bit of their first operand, whose\nindex is given by the second operand, and store the value of that\nbit into the carry flag. Bit indices are from 0 (least significant)\nto 15 or 31 (most significant).\n\nIn addition to storing the original value of the bit into the carry\nflag, `BTR` also resets (clears) the bit in the operand itself.\n`BTS` sets the bit, and `BTC` complements the bit. `BT` does\nnot modify its operands.\n\nThe destination can be a register or a memory location. The source can\nbe a register or an immediate value.\n\nIf the destination operand is a register, the bit offset should be\nin the range 0-15 (for 16-bit operands) or 0-31 (for 32-bit operands).\nAn immediate value outside these ranges will be taken modulo 16/32\nby the processor.\n\nIf the destination operand is a memory location, then an immediate\nbit offset follows the same rules as for a register. If the bit offset\nis in a register, then it can be anything within the signed range of\nthe register used (ie, for a 32-bit operand, it can be (-2^31) to (2^31 - 1)"},
    "bts": {"title":"`BT`, `BTC`, `BTR`, `BTS`: Bit Test","description":"These instructions all test one bit of their first operand, whose\nindex is given by the second operand, and store the value of that\nbit into the carry flag. Bit indices are from 0 (least significant)\nto 15 or 31 (most significant).\n\nIn addition to storing the original value of the bit into the carry\nflag, `BTR` also resets (clears) the bit in the operand itself.\n`BTS` sets the bit, and `BTC` complements the bit. `BT` does\nnot modify its operands.\n\nThe destination can be a register or a memory location. The source can\nbe a register or an immediate value.\n\nIf the destination operand is a register, the bit offset should be\nin the range 0-15 (for 16-bit operands) or 0-31 (for 32-bit operands).\nAn immediate value outside these ranges will be taken modulo 16/32\nby the processor.\n\nIf the destination operand is a memory location, then an immediate\nbit offset follows the same rules as for a register. If the bit offset\nis in a register, then it can be anything within the signed range of\nthe register used (ie, for a 32-bit operand, it can be (-2^31) to (2^31 - 1)"},
    "call": {"title":"`CALL`: Call Subroutine","description":"`CALL` calls a subroutine, by means of pushing the current\ninstruction pointer (`IP`) and optionally `CS` as well on the\nstack, and then jumping to a given address.\n\n`CS` is pushed as well as `IP` if and only if the call is a far\ncall, i.e. a destination segment address is specified in the\ninstruction. The forms involving two colon-separated arguments are\nfar calls; so are the `CALL FAR mem` forms.\n\nThe immediate near call takes one of two forms (`call imm16/imm32`,\ndetermined by the current segment size limit. For 16-bit operands,\nyou would use `CALL 0x1234`, and for 32-bit operands you would use\n`CALL 0x12345678`. The value passed as an operand is a relative offset.\n\nYou can choose between the two immediate far call forms\n(`CALL imm:imm`) by the use of the `WORD` and `DWORD` keywords:\n`CALL WORD 0x1234:0x5678`) or `CALL DWORD 0x1234:0x56789abc`.\n\nThe `CALL FAR mem` forms execute a far call by loading the\ndestination address out of memory. The address loaded consists of 16\nor 32 bits of offset (depending on the operand size), and 16 bits of\nsegment. The operand size may be overridden using \\c{CALL WORD FAR\nmem} or `CALL DWORD FAR mem`.\n\nThe `CALL r/m` forms execute a near call (within the same\nsegment), loading the destination address out of memory or out of a\nregister. The keyword `NEAR` may be specified, for clarity, in\nthese forms, but is not necessary. Again, operand size can be\noverridden using `CALL WORD mem` or `CALL DWORD mem`.\n\nAs a convenience, NASM does not require you to call a far procedure\nsymbol by coding the cumbersome `CALL SEG routine:routine`, but\ninstead allows the easier synonym `CALL FAR routine`.\n\nThe `CALL r/m` forms given above are near calls; NASM will accept\nthe `NEAR` keyword (e.g. `CALL NEAR [address]`), even though it\nis not strictly necessary."},
    "cbw": {"title":"`CBW`, `CWD`, `CDQ`, `CWDE`: Sign Extensions","description":"All these instructions sign-extend a short value into a longer one,\nby replicating the top bit of the original value to fill the\nextended one.\n\n`CBW` extends `AL` into `AX` by repeating the top bit of\n`AL` in every bit of `AH`. `CWDE` extends `AX` into\n`EAX`. `CWD` extends `AX` into `DX:AX` by repeating\nthe top bit of `AX` throughout `DX`, and `CDQ` extends\n`EAX` into `EDX:EAX`."},
    "cdq": {"title":"`CBW`, `CWD`, `CDQ`, `CWDE`: Sign Extensions","description":"All these instructions sign-extend a short value into a longer one,\nby replicating the top bit of the original value to fill the\nextended one.\n\n`CBW` extends `AL` into `AX` by repeating the top bit of\n`AL` in every bit of `AH`. `CWDE` extends `AX` into\n`EAX`. `CWD` extends `AX` into `DX:AX` by repeating\nthe top bit of `AX` throughout `DX`, and `CDQ` extends\n`EAX` into `EDX:EAX`."},
    "clc": {"title":"`CLC`, `CLD`, `CLI`, `CLTS`: Clear Flags","description":"These instructions clear various flags. `CLC` clears the carry\nflag; `CLD` clears the direction flag; `CLI` clears the\ninterrupt flag (thus disabling interrupts); and `CLTS` clears the\ntask-switched (`TS`) flag in `CR0`.\n\nTo set the carry, direction, or interrupt flags, use the `STC`,\n`STD` and `STI` instructions (`STC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "cld": {"title":"`CLC`, `CLD`, `CLI`, `CLTS`: Clear Flags","description":"These instructions clear various flags. `CLC` clears the carry\nflag; `CLD` clears the direction flag; `CLI` clears the\ninterrupt flag (thus disabling interrupts); and `CLTS` clears the\ntask-switched (`TS`) flag in `CR0`.\n\nTo set the carry, direction, or interrupt flags, use the `STC`,\n`STD` and `STI` instructions (`STC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "clflush": {"title":"`CLFLUSH`: Flush Cache Line","description":"`CLFLUSH` invalidates the cache line that contains the linear address\nspecified by the source operand from all levels of the processor cache\nhierarchy (data and instruction). If, at any level of the cache\nhierarchy, the line is inconsistent with memory (dirty) it is written\nto memory before invalidation. The source operand points to a\nbyte-sized memory location.\n\nAlthough `CLFLUSH` is flagged `SSE2` and above, it may not be\npresent on all processors which have `SSE2` support, and it may be\nsupported on other processors; the `CPUID` instruction (`CPUID`)\nwill return a bit which indicates support for the `CLFLUSH` instruction."},
    "cli": {"title":"`CLC`, `CLD`, `CLI`, `CLTS`: Clear Flags","description":"These instructions clear various flags. `CLC` clears the carry\nflag; `CLD` clears the direction flag; `CLI` clears the\ninterrupt flag (thus disabling interrupts); and `CLTS` clears the\ntask-switched (`TS`) flag in `CR0`.\n\nTo set the carry, direction, or interrupt flags, use the `STC`,\n`STD` and `STI` instructions (`STC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "clts": {"title":"`CLC`, `CLD`, `CLI`, `CLTS`: Clear Flags","description":"These instructions clear various flags. `CLC` clears the carry\nflag; `CLD` clears the direction flag; `CLI` clears the\ninterrupt flag (thus disabling interrupts); and `CLTS` clears the\ntask-switched (`TS`) flag in `CR0`.\n\nTo set the carry, direction, or interrupt flags, use the `STC`,\n`STD` and `STI` instructions (`STC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "cmc": {"title":"`CMC`: Complement Carry Flag","description":"`CMC` changes the value of the carry flag: if it was 0, it sets it\nto 1, and vice versa."},
    "cmova": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovae": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovb": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovbe": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovc": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmove": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovg": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovge": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovl": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovle": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovna": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnae": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnb": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnbe": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnc": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovne": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovng": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnge": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnl": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnle": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovno": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnp": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovns": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovnz": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovo": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovp": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovpe": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovpo": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovs": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmovz": {"title":"`CMOVcc`: Conditional Move","description":"`CMOV` moves its source (second) operand into its destination\n(first) operand if the given condition code is satisfied; otherwise\nit does nothing.\n\nFor a list of condition codes, see the condition codes.\n\nAlthough the `CMOV` instructions are flagged `P6` and above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "cmp": {"title":"`CMP`: Compare Integers","description":"`CMP` performs a 'mental' subtraction of its second operand from\nits first operand, and affects the flags as if the subtraction had\ntaken place, but does not store the result of the subtraction\nanywhere.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction.\n\nThe destination operand can be a register or a memory location. The\nsource can be a register, memory location or an immediate value of\nthe same size as the destination."},
    "cmplepd": {"title":"`CMPccPD`: Packed Double-Precision FP Compare","description":"The `CMPccPD` instructions compare the two packed double-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a quadword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpleps": {"title":"`CMPccPS`: Packed Single-Precision FP Compare","description":"The `CMPccPS` instructions compare the two packed single-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a doubleword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmplesd": {"title":"`CMPccSD`: Scalar Double-Precision FP Compare","description":"The `CMPccSD` instructions compare the low-order double-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a quadword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpless": {"title":"`CMPccSS`: Scalar Single-Precision FP Compare","description":"The `CMPccSS` instructions compare the low-order single-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a doubleword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpnlepd": {"title":"`CMPccPD`: Packed Double-Precision FP Compare","description":"The `CMPccPD` instructions compare the two packed double-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a quadword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpnleps": {"title":"`CMPccPS`: Packed Single-Precision FP Compare","description":"The `CMPccPS` instructions compare the two packed single-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a doubleword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpnlesd": {"title":"`CMPccSD`: Scalar Double-Precision FP Compare","description":"The `CMPccSD` instructions compare the low-order double-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a quadword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpnless": {"title":"`CMPccSS`: Scalar Single-Precision FP Compare","description":"The `CMPccSS` instructions compare the low-order single-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a doubleword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmppd": {"title":"`CMPccPD`: Packed Double-Precision FP Compare","description":"The `CMPccPD` instructions compare the two packed double-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a quadword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpps": {"title":"`CMPccPS`: Packed Single-Precision FP Compare","description":"The `CMPccPS` instructions compare the two packed single-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a doubleword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpsb": {"title":"`CMPSB`, `CMPSW`, `CMPSD`: Compare Strings","description":"`CMPSB` compares the byte at `[DS:SI]` or `[DS:ESI]` with the\nbyte at `[ES:DI]` or `[ES:EDI]`, and sets the flags accordingly.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` and\n`DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES CMPSB`). The use of `ES` for the load from `[DI]`\nor `[EDI]` cannot be overridden.\n\n`CMPSW` and `CMPSD` work in the same way, but they compare a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "cmpsd": {"title":"`CMPSB`, `CMPSW`, `CMPSD`: Compare Strings","description":"`CMPSB` compares the byte at `[DS:SI]` or `[DS:ESI]` with the\nbyte at `[ES:DI]` or `[ES:EDI]`, and sets the flags accordingly.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` and\n`DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES CMPSB`). The use of `ES` for the load from `[DI]`\nor `[EDI]` cannot be overridden.\n\n`CMPSW` and `CMPSD` work in the same way, but they compare a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "cmpsq": {"title":"`CMPSB`, `CMPSW`, `CMPSD`: Compare Strings","description":"`CMPSB` compares the byte at `[DS:SI]` or `[DS:ESI]` with the\nbyte at `[ES:DI]` or `[ES:EDI]`, and sets the flags accordingly.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` and\n`DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES CMPSB`). The use of `ES` for the load from `[DI]`\nor `[EDI]` cannot be overridden.\n\n`CMPSW` and `CMPSD` work in the same way, but they compare a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "cmpss": {"title":"`CMPccSS`: Scalar Single-Precision FP Compare","description":"The `CMPccSS` instructions compare the low-order single-precision\nFP values in the source and destination operands, and returns the\nresult of the comparison in the destination register. The result of\neach comparison is a doubleword mask of all 1s (comparison true) or\nall 0s (comparison false).\n\nThe destination is an `XMM` register. The source can be either an\n`XMM` register or a 128-bit memory location.\n\nThe third operand is an 8-bit immediate value, of which the low 3\nbits define the type of comparison. For ease of programming, the\n8 two-operand pseudo-instructions are provided, with the third\noperand already filled in. The\n`Condition Predicates` are:\n\n```\nEQ     0   Equal\nLT     1   Less-than\nLE     2   Less-than-or-equal\nUNORD  3   Unordered\nNE     4   Not-equal\nNLT    5   Not-less-than\nNLE    6   Not-less-than-or-equal\nORD    7   Ordered\n```\n\nFor more details of the comparison predicates, and details of how\nto emulate the \"greater-than\" equivalents, see the SSE condition predicates"},
    "cmpsw": {"title":"`CMPSB`, `CMPSW`, `CMPSD`: Compare Strings","description":"`CMPSB` compares the byte at `[DS:SI]` or `[DS:ESI]` with the\nbyte at `[ES:DI]` or `[ES:EDI]`, and sets the flags accordingly.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` and\n`DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES CMPSB`). The use of `ES` for the load from `[DI]`\nor `[EDI]` cannot be overridden.\n\n`CMPSW` and `CMPSD` work in the same way, but they compare a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "cmpxchg": {"title":"`CMPXCHG`, `CMPXCHG486`: Compare and Exchange","description":"These two instructions perform exactly the same operation; however,\napparently some (not all) 486 processors support it under a\nnon-standard opcode, so NASM provides the undocumented\n`CMPXCHG486` form to generate the non-standard opcode.\n\n`CMPXCHG` compares its destination (first) operand to the value in\n`AL`, `AX` or `EAX` (depending on the operand size of the\ninstruction). If they are equal, it copies its source (second)\noperand into the destination and sets the zero flag. Otherwise, it\nclears the zero flag and copies the destination register to AL, AX or EAX.\n\nThe destination can be either a register or a memory location. The\nsource is a register.\n\n`CMPXCHG` is intended to be used for atomic operations in\nmultitasking or multiprocessor environments. To safely update a\nvalue in shared memory, for example, you might load the value into\n`EAX`, load the updated value into `EBX`, and then execute the\ninstruction `LOCK CMPXCHG [value],EBX`. If `value` has not\nchanged since being loaded, it is updated with your desired new\nvalue, and the zero flag is set to let you know it has worked. (The\n`LOCK` prefix prevents another processor doing anything in the\nmiddle of this operation: it guarantees atomicity.) However, if\nanother processor has modified the value in between your load and\nyour attempted store, the store does not happen, and you are\nnotified of the failure by a cleared zero flag, so you can go round\nand try again."},
    "cmpxchg486": {"title":"`CMPXCHG`, `CMPXCHG486`: Compare and Exchange","description":"These two instructions perform exactly the same operation; however,\napparently some (not all) 486 processors support it under a\nnon-standard opcode, so NASM provides the undocumented\n`CMPXCHG486` form to generate the non-standard opcode.\n\n`CMPXCHG` compares its destination (first) operand to the value in\n`AL`, `AX` or `EAX` (depending on the operand size of the\ninstruction). If they are equal, it copies its source (second)\noperand into the destination and sets the zero flag. Otherwise, it\nclears the zero flag and copies the destination register to AL, AX or EAX.\n\nThe destination can be either a register or a memory location. The\nsource is a register.\n\n`CMPXCHG` is intended to be used for atomic operations in\nmultitasking or multiprocessor environments. To safely update a\nvalue in shared memory, for example, you might load the value into\n`EAX`, load the updated value into `EBX`, and then execute the\ninstruction `LOCK CMPXCHG [value],EBX`. If `value` has not\nchanged since being loaded, it is updated with your desired new\nvalue, and the zero flag is set to let you know it has worked. (The\n`LOCK` prefix prevents another processor doing anything in the\nmiddle of this operation: it guarantees atomicity.) However, if\nanother processor has modified the value in between your load and\nyour attempted store, the store does not happen, and you are\nnotified of the failure by a cleared zero flag, so you can go round\nand try again."},
    "cmpxchg8b": {"title":"`CMPXCHG8B`: Compare and Exchange Eight Bytes","description":"This is a larger and more unwieldy version of `CMPXCHG`: it\ncompares the 64-bit (eight-byte) value stored at `[mem]` with the\nvalue in `EDX:EAX`. If they are equal, it sets the zero flag and\nstores `ECX:EBX` into the memory area. If they are unequal, it\nclears the zero flag and stores the memory contents into `EDX:EAX`.\n\n`CMPXCHG8B` can be used with the `LOCK` prefix, to allow atomic\nexecution. This is useful in multi-processor and multi-tasking\nenvironments."},
    "comisd": {"title":"`COMISD`: Scalar Ordered Double-Precision FP Compare and Set EFLAGS","description":"`COMISD` compares the low-order double-precision FP value in the\ntwo source operands. ZF, PF and CF are set according to the result.\nOF, AF and AF are cleared. The unordered result is returned if either\nsource is a NaN (QNaN or SNaN).\n\nThe destination operand is an `XMM` register. The source can be either\nan `XMM` register or a memory location.\n\nThe flags are set according to the following rules:\n\n```\n   Result          Flags        Values\n```\n\n```\n   UNORDERED:      ZF,PF,CF <-- 111;\n   GREATER_THAN:   ZF,PF,CF <-- 000;\n   LESS_THAN:      ZF,PF,CF <-- 001;\n   EQUAL:          ZF,PF,CF <-- 100;\n```"},
    "comiss": {"title":"`COMISS`: Scalar Ordered Single-Precision FP Compare and Set EFLAGS","description":"`COMISS` compares the low-order single-precision FP value in the\ntwo source operands. ZF, PF and CF are set according to the result.\nOF, AF and AF are cleared. The unordered result is returned if either\nsource is a NaN (QNaN or SNaN).\n\nThe destination operand is an `XMM` register. The source can be either\nan `XMM` register or a memory location.\n\nThe flags are set according to the following rules:\n\n```\n   Result          Flags        Values\n```\n\n```\n   UNORDERED:      ZF,PF,CF <-- 111;\n   GREATER_THAN:   ZF,PF,CF <-- 000;\n   LESS_THAN:      ZF,PF,CF <-- 001;\n   EQUAL:          ZF,PF,CF <-- 100;\n```"},
    "cpuid": {"title":"`CPUID`: Get CPU Identification Code","description":"`CPUID` returns various information about the processor it is\nbeing executed on. It fills the four registers `EAX`, `EBX`,\n`ECX` and `EDX` with information, which varies depending on the\ninput contents of `EAX`.\n\n`CPUID` also acts as a barrier to serialize instruction execution:\nexecuting the `CPUID` instruction guarantees that all the effects\n(memory modification, flag modification, register modification) of\nprevious instructions have been completed before the next\ninstruction gets fetched.\n\nThe information returned is as follows:\n\n- If `EAX` is zero on input, `EAX` on output holds the maximum\nacceptable input value of `EAX`, and `EBX:EDX:ECX` contain the\nstring `\"GenuineIntel\"` (or not, if you have a clone processor).\nThat is to say, `EBX` contains `\"Genu\"` (in NASM's own sense of\ncharacter constants, described in ), `EDX` contains\n`\"ineI\"` and `ECX` contains `\"ntel\"`.\n\n- If `EAX` is one on input, `EAX` on output contains version\ninformation about the processor, and `EDX` contains a set of\nfeature flags, showing the presence and absence of various features.\nFor example, bit 8 is set if the `CMPXCHG8B` instruction\n(`CMPXCHG8B`) is supported, bit 15 is set if the conditional\nmove instructions (`CMOVcc` and `FCMOVB`) are supported,\nand bit 23 is set if `MMX` instructions are supported.\n\n- If `EAX` is two on input, `EAX`, `EBX`, `ECX` and `EDX`\nall contain information about caches and TLBs (Translation Lookahead\nBuffers).\n\nFor more information on the data returned from `CPUID`, see the\ndocumentation from Intel and other processor manufacturers."},
    "cvtdq2pd": {"title":"`CVTDQ2PD`: Packed Signed INT32 to Packed Double-Precision FP Conversion","description":"`CVTDQ2PD` converts two packed signed doublewords from the source\noperand to two packed double-precision FP values in the destination\noperand.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 64-bit memory location. If the\nsource is a register, the packed integers are in the low quadword."},
    "cvtdq2ps": {"title":"`CVTDQ2PS`: Packed Signed INT32 to Packed Single-Precision FP Conversion","description":"`CVTDQ2PS` converts four packed signed doublewords from the source\noperand to four packed single-precision FP values in the destination\noperand.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtpd2dq": {"title":"`CVTPD2DQ`: Packed Double-Precision FP to Packed Signed INT32 Conversion","description":"`CVTPD2DQ` converts two packed double-precision FP values from the\nsource operand to two packed signed doublewords in the low quadword\nof the destination operand. The high quadword of the destination is\nset to all 0s.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtpd2pi": {"title":"`CVTPD2PI`: Packed Double-Precision FP to Packed Signed INT32 Conversion","description":"`CVTPD2PI` converts two packed double-precision FP values from the\nsource operand to two packed signed doublewords in the destination\noperand.\n\nThe destination operand is an `MMX` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtpd2ps": {"title":"`CVTPD2PS`: Packed Double-Precision FP to Packed Single-Precision FP Conversion","description":"`CVTPD2PS` converts two packed double-precision FP values from the\nsource operand to two packed single-precision FP values in the low\nquadword of the destination operand. The high quadword of the\ndestination is set to all 0s.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtpi2pd": {"title":"`CVTPI2PD`: Packed Signed INT32 to Packed Double-Precision FP Conversion","description":"`CVTPI2PD` converts two packed signed doublewords from the source\noperand to two packed double-precision FP values in the destination\noperand.\n\nThe destination operand is an `XMM` register. The source can be\neither an `MMX` register or a 64-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtpi2ps": {"title":"`CVTPI2PS`: Packed Signed INT32 to Packed Single-FP Conversion","description":"`CVTPI2PS` converts two packed signed doublewords from the source\noperand to two packed single-precision FP values in the low quadword\nof the destination operand. The high quadword of the destination\nremains unchanged.\n\nThe destination operand is an `XMM` register. The source can be\neither an `MMX` register or a 64-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtps2dq": {"title":"`CVTPS2DQ`: Packed Single-Precision FP to Packed Signed INT32 Conversion","description":"`CVTPS2DQ` converts four packed single-precision FP values from the\nsource operand to four packed signed doublewords in the destination operand.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtps2pd": {"title":"`CVTPS2PD`: Packed Single-Precision FP to Packed Double-Precision FP Conversion","description":"`CVTPS2PD` converts two packed single-precision FP values from the\nsource operand to two packed double-precision FP values in the destination\noperand.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 64-bit memory location. If the source\nis a register, the input values are in the low quadword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtps2pi": {"title":"`CVTPS2PI`: Packed Single-Precision FP to Packed Signed INT32 Conversion","description":"`CVTPS2PI` converts two packed single-precision FP values from\nthe source operand to two packed signed doublewords in the destination\noperand.\n\nThe destination operand is an `MMX` register. The source can be\neither an `XMM` register or a 64-bit memory location. If the\nsource is a register, the input values are in the low quadword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtsd2si": {"title":"`CVTSD2SI`: Scalar Double-Precision FP to Signed INT32 Conversion","description":"`CVTSD2SI` converts a double-precision FP value from the source\noperand to a signed doubleword in the destination operand.\n\nThe destination operand is a general purpose register. The source can be\neither an `XMM` register or a 64-bit memory location. If the\nsource is a register, the input value is in the low quadword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtsd2ss": {"title":"`CVTSD2SS`: Scalar Double-Precision FP to Scalar Single-Precision FP Conversion","description":"`CVTSD2SS` converts a double-precision FP value from the source\noperand to a single-precision FP value in the low doubleword of the\ndestination operand. The upper 3 doublewords are left unchanged.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 64-bit memory location. If the\nsource is a register, the input value is in the low quadword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtsi2sd": {"title":"`CVTSI2SD`: Signed INT32 to Scalar Double-Precision FP Conversion","description":"`CVTSI2SD` converts a signed doubleword from the source operand to\na double-precision FP value in the low quadword of the destination\noperand. The high quadword is left unchanged.\n\nThe destination operand is an `XMM` register. The source can be either\na general purpose register or a 32-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtsi2ss": {"title":"`CVTSI2SS`: Signed INT32 to Scalar Single-Precision FP Conversion","description":"`CVTSI2SS` converts a signed doubleword from the source operand to a\nsingle-precision FP value in the low doubleword of the destination operand.\nThe upper 3 doublewords are left unchanged.\n\nThe destination operand is an `XMM` register. The source can be either\na general purpose register or a 32-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtss2sd": {"title":"`CVTSS2SD`: Scalar Single-Precision FP to Scalar Double-Precision FP Conversion","description":"`CVTSS2SD` converts a single-precision FP value from the source operand\nto a double-precision FP value in the low quadword of the destination\noperand. The upper quadword is left unchanged.\n\nThe destination operand is an `XMM` register. The source can be either\nan `XMM` register or a 32-bit memory location. If the source is a\nregister, the input value is contained in the low doubleword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvtss2si": {"title":"`CVTSS2SI`: Scalar Single-Precision FP to Signed INT32 Conversion","description":"`CVTSS2SI` converts a single-precision FP value from the source\noperand to a signed doubleword in the destination operand.\n\nThe destination operand is a general purpose register. The source can be\neither an `XMM` register or a 32-bit memory location. If the\nsource is a register, the input value is in the low doubleword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvttpd2dq": {"title":"`CVTTPD2DQ`: Packed Double-Precision FP to Packed Signed INT32 Conversion with Truncation","description":"`CVTTPD2DQ` converts two packed double-precision FP values in the source\noperand to two packed single-precision FP values in the destination operand.\nIf the result is inexact, it is truncated (rounded toward zero). The high\nquadword is set to all 0s.\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvttpd2pi": {"title":"`CVTTPD2PI`: Packed Double-Precision FP to Packed Signed INT32 Conversion with Truncation","description":"`CVTTPD2PI` converts two packed double-precision FP values in the source\noperand to two packed single-precision FP values in the destination operand.\nIf the result is inexact, it is truncated (rounded toward zero).\n\nThe destination operand is an `MMX` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvttps2dq": {"title":"`CVTTPS2DQ`: Packed Single-Precision FP to Packed Signed INT32 Conversion with Truncation","description":"`CVTTPS2DQ` converts four packed single-precision FP values in the source\noperand to four packed signed doublewords in the destination operand.\nIf the result is inexact, it is truncated (rounded toward zero).\n\nThe destination operand is an `XMM` register. The source can be\neither an `XMM` register or a 128-bit memory location.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvttps2pi": {"title":"`CVTTPS2PI`: Packed Single-Precision FP to Packed Signed INT32 Conversion with Truncation","description":"`CVTTPS2PI` converts two packed single-precision FP values in the source\noperand to two packed signed doublewords in the destination operand.\nIf the result is inexact, it is truncated (rounded toward zero). If\nthe source is a register, the input values are in the low quadword.\n\nThe destination operand is an `MMX` register. The source can be\neither an `XMM` register or a 64-bit memory location. If the source\nis a register, the input value is in the low quadword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvttsd2si": {"title":"`CVTTSD2SI`: Scalar Double-Precision FP to Signed INT32 Conversion with Truncation","description":"`CVTTSD2SI` converts a double-precision FP value in the source operand\nto a signed doubleword in the destination operand. If the result is\ninexact, it is truncated (rounded toward zero).\n\nThe destination operand is a general purpose register. The source can be\neither an `XMM` register or a 64-bit memory location. If the source is a\nregister, the input value is in the low quadword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cvttss2si": {"title":"`CVTTSS2SI`: Scalar Single-Precision FP to Signed INT32 Conversion with Truncation","description":"`CVTTSS2SI` converts a single-precision FP value in the source operand\nto a signed doubleword in the destination operand. If the result is\ninexact, it is truncated (rounded toward zero).\n\nThe destination operand is a general purpose register. The source can be\neither an `XMM` register or a 32-bit memory location. If the source is a\nregister, the input value is in the low doubleword.\n\nFor more details of this instruction, see the Intel Processor manuals."},
    "cwd": {"title":"`CBW`, `CWD`, `CDQ`, `CWDE`: Sign Extensions","description":"All these instructions sign-extend a short value into a longer one,\nby replicating the top bit of the original value to fill the\nextended one.\n\n`CBW` extends `AL` into `AX` by repeating the top bit of\n`AL` in every bit of `AH`. `CWDE` extends `AX` into\n`EAX`. `CWD` extends `AX` into `DX:AX` by repeating\nthe top bit of `AX` throughout `DX`, and `CDQ` extends\n`EAX` into `EDX:EAX`."},
    "cwde": {"title":"`CBW`, `CWD`, `CDQ`, `CWDE`: Sign Extensions","description":"All these instructions sign-extend a short value into a longer one,\nby replicating the top bit of the original value to fill the\nextended one.\n\n`CBW` extends `AL` into `AX` by repeating the top bit of\n`AL` in every bit of `AH`. `CWDE` extends `AX` into\n`EAX`. `CWD` extends `AX` into `DX:AX` by repeating\nthe top bit of `AX` throughout `DX`, and `CDQ` extends\n`EAX` into `EDX:EAX`."},
    "daa": {"title":"`DAA`, `DAS`: Decimal Adjustments","description":"These instructions are used in conjunction with the add and subtract\ninstructions to perform binary-coded decimal arithmetic in\n*packed* (one BCD digit per nibble) form. For the unpacked\nequivalents, see `AAA`.\n\n`DAA` should be used after a one-byte `ADD` instruction whose\ndestination was the `AL` register: by means of examining the value\nin the `AL` and also the auxiliary carry flag `AF`, it\ndetermines whether either digit of the addition has overflowed, and\nadjusts it (and sets the carry and auxiliary-carry flags) if so. You\ncan add long BCD strings together by doing `ADD`/`DAA` on the\nlow two digits, then doing `ADC`/`DAA` on each subsequent pair\nof digits.\n\n`DAS` works similarly to `DAA`, but is for use after `SUB`\ninstructions rather than `ADD`."},
    "das": {"title":"`DAA`, `DAS`: Decimal Adjustments","description":"These instructions are used in conjunction with the add and subtract\ninstructions to perform binary-coded decimal arithmetic in\n*packed* (one BCD digit per nibble) form. For the unpacked\nequivalents, see `AAA`.\n\n`DAA` should be used after a one-byte `ADD` instruction whose\ndestination was the `AL` register: by means of examining the value\nin the `AL` and also the auxiliary carry flag `AF`, it\ndetermines whether either digit of the addition has overflowed, and\nadjusts it (and sets the carry and auxiliary-carry flags) if so. You\ncan add long BCD strings together by doing `ADD`/`DAA` on the\nlow two digits, then doing `ADC`/`DAA` on each subsequent pair\nof digits.\n\n`DAS` works similarly to `DAA`, but is for use after `SUB`\ninstructions rather than `ADD`."},
    "dec": {"title":"`DEC`: Decrement Integer","description":"`DEC` subtracts 1 from its operand. It does *not* affect the\ncarry flag: to affect the carry flag, use `SUB something,1` (see\n`SUB`). `DEC` affects all the other flags according to the result.\n\nThis instruction can be used with a `LOCK` prefix to allow atomic\nexecution.\n\nSee also `INC` (`INC`)."},
    "div": {"title":"`DIV`: Unsigned Integer Divide","description":"`DIV` performs unsigned integer division. The explicit operand\nprovided is the divisor; the dividend and destination operands are\nimplicit, in the following way:\n\n- For `DIV r/m8`, `AX` is divided by the given operand; the\nquotient is stored in `AL` and the remainder in `AH`.\n\n- For `DIV r/m16`, `DX:AX` is divided by the given operand; the\nquotient is stored in `AX` and the remainder in `DX`.\n\n- For `DIV r/m32`, `EDX:EAX` is divided by the given operand;\nthe quotient is stored in `EAX` and the remainder in `EDX`.\n\nSigned integer division is performed by the `IDIV` instruction:\nsee `IDIV`."},
    "divpd": {"title":"`DIVPD`: Packed Double-Precision FP Divide","description":"`DIVPD` divides the two packed double-precision FP values in\nthe destination operand by the two packed double-precision FP\nvalues in the source operand, and stores the packed double-precision\nresults in the destination register.\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location.\n\n```\n   dst[0-63]   := dst[0-63]   / src[0-63],\n   dst[64-127] := dst[64-127] / src[64-127].\n```"},
    "divps": {"title":"`DIVPS`: Packed Single-Precision FP Divide","description":"`DIVPS` divides the four packed single-precision FP values in\nthe destination operand by the four packed single-precision FP\nvalues in the source operand, and stores the packed single-precision\nresults in the destination register.\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 128-bit memory location.\n\n```\n   dst[0-31]   := dst[0-31]   / src[0-31],\n   dst[32-63]  := dst[32-63]  / src[32-63],\n   dst[64-95]  := dst[64-95]  / src[64-95],\n   dst[96-127] := dst[96-127] / src[96-127].\n```"},
    "divsd": {"title":"`DIVSD`: Scalar Double-Precision FP Divide","description":"`DIVSD` divides the low-order double-precision FP value in the\ndestination operand by the low-order double-precision FP value in\nthe source operand, and stores the double-precision result in the\ndestination register.\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 64-bit memory location.\n\n```\n   dst[0-63]   := dst[0-63] / src[0-63],\n   dst[64-127] remains unchanged.\n```"},
    "divss": {"title":"`DIVSS`: Scalar Single-Precision FP Divide","description":"`DIVSS` divides the low-order single-precision FP value in the\ndestination operand by the low-order single-precision FP value in\nthe source operand, and stores the single-precision result in the\ndestination register.\n\nThe destination is an `XMM` register. The source operand can be\neither an `XMM` register or a 32-bit memory location.\n\n```\n   dst[0-31]   := dst[0-31] / src[0-31],\n   dst[32-127] remains unchanged.\n```"},
    "emms": {"title":"`EMMS`: Empty MMX State","description":"`EMMS` sets the FPU tag word (marking which floating-point registers\nare available) to all ones, meaning all registers are available for\nthe FPU to use. It should be used after executing `MMX` instructions\nand before executing any subsequent floating-point operations."},
    "enter": {"title":"`ENTER`: Create Stack Frame","description":"`ENTER` constructs a `stack frame` for a high-level language\nprocedure call. The first operand (the `iw` in the opcode\ndefinition above refers to the first operand) gives the amount of\nstack space to allocate for local variables; the second (the `ib`\nabove) gives the nesting level of the procedure (for languages like\nPascal, with nested procedures).\n\nThe function of `ENTER`, with a nesting level of zero, is\nequivalent to\n\n```\n          PUSH EBP            ; or PUSH BP         in 16 bits\n          MOV EBP,ESP         ; or MOV BP,SP       in 16 bits\n          SUB ESP,operand1    ; or SUB SP,operand1 in 16 bits\n```\n\nThis creates a stack frame with the procedure parameters accessible\nupwards from `EBP`, and local variables accessible downwards from\n`EBP`.\n\nWith a nesting level of one, the stack frame created is 4 (or 2)\nbytes bigger, and the value of the final frame pointer `EBP` is\naccessible in memory at `[EBP-4]`.\n\nThis allows `ENTER`, when called with a nesting level of two, to\nlook at the stack frame described by the *previous* value of\n`EBP`, find the frame pointer at offset -4 from that, and push it\nalong with its new frame pointer, so that when a level-two procedure\nis called from within a level-one procedure, `[EBP-4]` holds the\nframe pointer of the most recent level-one procedure call and\n`[EBP-8]` holds that of the most recent level-two call. And so on,\nfor nesting levels up to 31.\n\nStack frames created by `ENTER` can be destroyed by the `LEAVE`\ninstruction: see `LEAVE`."},
    "enterd": {"title":"`ENTER`: Create Stack Frame","description":"`ENTER` constructs a `stack frame` for a high-level language\nprocedure call. The first operand (the `iw` in the opcode\ndefinition above refers to the first operand) gives the amount of\nstack space to allocate for local variables; the second (the `ib`\nabove) gives the nesting level of the procedure (for languages like\nPascal, with nested procedures).\n\nThe function of `ENTER`, with a nesting level of zero, is\nequivalent to\n\n```\n          PUSH EBP            ; or PUSH BP         in 16 bits\n          MOV EBP,ESP         ; or MOV BP,SP       in 16 bits\n          SUB ESP,operand1    ; or SUB SP,operand1 in 16 bits\n```\n\nThis creates a stack frame with the procedure parameters accessible\nupwards from `EBP`, and local variables accessible downwards from\n`EBP`.\n\nWith a nesting level of one, the stack frame created is 4 (or 2)\nbytes bigger, and the value of the final frame pointer `EBP` is\naccessible in memory at `[EBP-4]`.\n\nThis allows `ENTER`, when called with a nesting level of two, to\nlook at the stack frame described by the *previous* value of\n`EBP`, find the frame pointer at offset -4 from that, and push it\nalong with its new frame pointer, so that when a level-two procedure\nis called from within a level-one procedure, `[EBP-4]` holds the\nframe pointer of the most recent level-one procedure call and\n`[EBP-8]` holds that of the most recent level-two call. And so on,\nfor nesting levels up to 31.\n\nStack frames created by `ENTER` can be destroyed by the `LEAVE`\ninstruction: see `LEAVE`."},
    "enterq": {"title":"`ENTER`: Create Stack Frame","description":"`ENTER` constructs a `stack frame` for a high-level language\nprocedure call. The first operand (the `iw` in the opcode\ndefinition above refers to the first operand) gives the amount of\nstack space to allocate for local variables; the second (the `ib`\nabove) gives the nesting level of the procedure (for languages like\nPascal, with nested procedures).\n\nThe function of `ENTER`, with a nesting level of zero, is\nequivalent to\n\n```\n          PUSH EBP            ; or PUSH BP         in 16 bits\n          MOV EBP,ESP         ; or MOV BP,SP       in 16 bits\n          SUB ESP,operand1    ; or SUB SP,operand1 in 16 bits\n```\n\nThis creates a stack frame with the procedure parameters accessible\nupwards from `EBP`, and local variables accessible downwards from\n`EBP`.\n\nWith a nesting level of one, the stack frame created is 4 (or 2)\nbytes bigger, and the value of the final frame pointer `EBP` is\naccessible in memory at `[EBP-4]`.\n\nThis allows `ENTER`, when called with a nesting level of two, to\nlook at the stack frame described by the *previous* value of\n`EBP`, find the frame pointer at offset -4 from that, and push it\nalong with its new frame pointer, so that when a level-two procedure\nis called from within a level-one procedure, `[EBP-4]` holds the\nframe pointer of the most recent level-one procedure call and\n`[EBP-8]` holds that of the most recent level-two call. And so on,\nfor nesting levels up to 31.\n\nStack frames created by `ENTER` can be destroyed by the `LEAVE`\ninstruction: see `LEAVE`."},
    "enterw": {"title":"`ENTER`: Create Stack Frame","description":"`ENTER` constructs a `stack frame` for a high-level language\nprocedure call. The first operand (the `iw` in the opcode\ndefinition above refers to the first operand) gives the amount of\nstack space to allocate for local variables; the second (the `ib`\nabove) gives the nesting level of the procedure (for languages like\nPascal, with nested procedures).\n\nThe function of `ENTER`, with a nesting level of zero, is\nequivalent to\n\n```\n          PUSH EBP            ; or PUSH BP         in 16 bits\n          MOV EBP,ESP         ; or MOV BP,SP       in 16 bits\n          SUB ESP,operand1    ; or SUB SP,operand1 in 16 bits\n```\n\nThis creates a stack frame with the procedure parameters accessible\nupwards from `EBP`, and local variables accessible downwards from\n`EBP`.\n\nWith a nesting level of one, the stack frame created is 4 (or 2)\nbytes bigger, and the value of the final frame pointer `EBP` is\naccessible in memory at `[EBP-4]`.\n\nThis allows `ENTER`, when called with a nesting level of two, to\nlook at the stack frame described by the *previous* value of\n`EBP`, find the frame pointer at offset -4 from that, and push it\nalong with its new frame pointer, so that when a level-two procedure\nis called from within a level-one procedure, `[EBP-4]` holds the\nframe pointer of the most recent level-one procedure call and\n`[EBP-8]` holds that of the most recent level-two call. And so on,\nfor nesting levels up to 31.\n\nStack frames created by `ENTER` can be destroyed by the `LEAVE`\ninstruction: see `LEAVE`."},
    "f2xm1": {"title":"`F2XM1`: Calculate 2**X-1","description":"`F2XM1` raises 2 to the power of `ST0`, subtracts one, and\nstores the result back into `ST0`. The initial contents of `ST0`\nmust be a number in the range -1.0 to +1.0."},
    "fabs": {"title":"`FABS`: Floating-Point Absolute Value","description":"`FABS` computes the absolute value of `ST0`,by clearing the sign\nbit, and stores the result back in `ST0`."},
    "fadd": {"title":"`FADD`, `FADDP`: Floating-Point Addition","description":"- `FADD`, given one operand, adds the operand to `ST0` and stores\nthe result back in `ST0`. If the operand has the `TO` modifier,\nthe result is stored in the register given rather than in `ST0`.\n\n- `FADDP` performs the same function as `FADD TO`, but pops the\nregister stack after storing the result.\n\nThe given two-operand forms are synonyms for the one-operand forms.\n\nTo add an integer value to `ST0`, use the c{FIADD} instruction\n(`FIADD`)"},
    "faddp": {"title":"`FADD`, `FADDP`: Floating-Point Addition","description":"- `FADD`, given one operand, adds the operand to `ST0` and stores\nthe result back in `ST0`. If the operand has the `TO` modifier,\nthe result is stored in the register given rather than in `ST0`.\n\n- `FADDP` performs the same function as `FADD TO`, but pops the\nregister stack after storing the result.\n\nThe given two-operand forms are synonyms for the one-operand forms.\n\nTo add an integer value to `ST0`, use the c{FIADD} instruction\n(`FIADD`)"},
    "fbld": {"title":"`FBLD`, `FBSTP`: BCD Floating-Point Load and Store","description":"`FBLD` loads an 80-bit (ten-byte) packed binary-coded decimal\nnumber from the given memory address, converts it to a real, and\npushes it on the register stack. `FBSTP` stores the value of\n`ST0`, in packed BCD, at the given address and then pops the\nregister stack."},
    "fbstp": {"title":"`FBLD`, `FBSTP`: BCD Floating-Point Load and Store","description":"`FBLD` loads an 80-bit (ten-byte) packed binary-coded decimal\nnumber from the given memory address, converts it to a real, and\npushes it on the register stack. `FBSTP` stores the value of\n`ST0`, in packed BCD, at the given address and then pops the\nregister stack."},
    "fchs": {"title":"`FCHS`: Floating-Point Change Sign","description":"`FCHS` negates the number in `ST0`, by inverting the sign bit:\nnegative numbers become positive, and vice versa."},
    "fclex": {"title":"`FCLEX`, `FNCLEX`: Clear Floating-Point Exceptions","description":"`FCLEX` clears any floating-point exceptions which may be pending.\n`FNCLEX` does the same thing but doesn't wait for previous\nfloating-point operations (including the *handling* of pending\nexceptions) to finish first."},
    "fcmovb": {"title":"`FCMOVcc`: Floating-Point Conditional Move","description":"The `FCMOV` instructions perform conditional move operations: each\nof them moves the contents of the given register into `ST0` if its\ncondition is satisfied, and does nothing if not.\n\nThe conditions are not the same as the standard condition codes used\nwith conditional jump instructions. The conditions `B`, `BE`,\n`NB`, `NBE`, `E` and `NE` are exactly as normal, but none of\nthe other standard ones are supported. Instead, the condition `U`\nand its counterpart `NU` are provided; the `U` condition is\nsatisfied if the last two floating-point numbers compared were\n*unordered*, i.e. they were not equal but neither one could be\nsaid to be greater than the other, for example if they were NaNs.\n(The flag state which signals this is the setting of the parity\nflag: so the `U` condition is notionally equivalent to `PE`, and\n`NU` is equivalent to `PO`.)\n\nThe `FCMOV` conditions test the main processor's status flags, not\nthe FPU status flags, so using `FCMOV` directly after `FCOM`\nwill not work. Instead, you should either use `FCOMI` which writes\ndirectly to the main CPU flags word, or use `FSTSW` to extract the\nFPU flags.\n\nAlthough the `FCMOV` instructions are flagged `P6` above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "fcmovbe": {"title":"`FCMOVcc`: Floating-Point Conditional Move","description":"The `FCMOV` instructions perform conditional move operations: each\nof them moves the contents of the given register into `ST0` if its\ncondition is satisfied, and does nothing if not.\n\nThe conditions are not the same as the standard condition codes used\nwith conditional jump instructions. The conditions `B`, `BE`,\n`NB`, `NBE`, `E` and `NE` are exactly as normal, but none of\nthe other standard ones are supported. Instead, the condition `U`\nand its counterpart `NU` are provided; the `U` condition is\nsatisfied if the last two floating-point numbers compared were\n*unordered*, i.e. they were not equal but neither one could be\nsaid to be greater than the other, for example if they were NaNs.\n(The flag state which signals this is the setting of the parity\nflag: so the `U` condition is notionally equivalent to `PE`, and\n`NU` is equivalent to `PO`.)\n\nThe `FCMOV` conditions test the main processor's status flags, not\nthe FPU status flags, so using `FCMOV` directly after `FCOM`\nwill not work. Instead, you should either use `FCOMI` which writes\ndirectly to the main CPU flags word, or use `FSTSW` to extract the\nFPU flags.\n\nAlthough the `FCMOV` instructions are flagged `P6` above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "fcmove": {"title":"`FCMOVcc`: Floating-Point Conditional Move","description":"The `FCMOV` instructions perform conditional move operations: each\nof them moves the contents of the given register into `ST0` if its\ncondition is satisfied, and does nothing if not.\n\nThe conditions are not the same as the standard condition codes used\nwith conditional jump instructions. The conditions `B`, `BE`,\n`NB`, `NBE`, `E` and `NE` are exactly as normal, but none of\nthe other standard ones are supported. Instead, the condition `U`\nand its counterpart `NU` are provided; the `U` condition is\nsatisfied if the last two floating-point numbers compared were\n*unordered*, i.e. they were not equal but neither one could be\nsaid to be greater than the other, for example if they were NaNs.\n(The flag state which signals this is the setting of the parity\nflag: so the `U` condition is notionally equivalent to `PE`, and\n`NU` is equivalent to `PO`.)\n\nThe `FCMOV` conditions test the main processor's status flags, not\nthe FPU status flags, so using `FCMOV` directly after `FCOM`\nwill not work. Instead, you should either use `FCOMI` which writes\ndirectly to the main CPU flags word, or use `FSTSW` to extract the\nFPU flags.\n\nAlthough the `FCMOV` instructions are flagged `P6` above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "fcmovnb": {"title":"`FCMOVcc`: Floating-Point Conditional Move","description":"The `FCMOV` instructions perform conditional move operations: each\nof them moves the contents of the given register into `ST0` if its\ncondition is satisfied, and does nothing if not.\n\nThe conditions are not the same as the standard condition codes used\nwith conditional jump instructions. The conditions `B`, `BE`,\n`NB`, `NBE`, `E` and `NE` are exactly as normal, but none of\nthe other standard ones are supported. Instead, the condition `U`\nand its counterpart `NU` are provided; the `U` condition is\nsatisfied if the last two floating-point numbers compared were\n*unordered*, i.e. they were not equal but neither one could be\nsaid to be greater than the other, for example if they were NaNs.\n(The flag state which signals this is the setting of the parity\nflag: so the `U` condition is notionally equivalent to `PE`, and\n`NU` is equivalent to `PO`.)\n\nThe `FCMOV` conditions test the main processor's status flags, not\nthe FPU status flags, so using `FCMOV` directly after `FCOM`\nwill not work. Instead, you should either use `FCOMI` which writes\ndirectly to the main CPU flags word, or use `FSTSW` to extract the\nFPU flags.\n\nAlthough the `FCMOV` instructions are flagged `P6` above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "fcmovnbe": {"title":"`FCMOVcc`: Floating-Point Conditional Move","description":"The `FCMOV` instructions perform conditional move operations: each\nof them moves the contents of the given register into `ST0` if its\ncondition is satisfied, and does nothing if not.\n\nThe conditions are not the same as the standard condition codes used\nwith conditional jump instructions. The conditions `B`, `BE`,\n`NB`, `NBE`, `E` and `NE` are exactly as normal, but none of\nthe other standard ones are supported. Instead, the condition `U`\nand its counterpart `NU` are provided; the `U` condition is\nsatisfied if the last two floating-point numbers compared were\n*unordered*, i.e. they were not equal but neither one could be\nsaid to be greater than the other, for example if they were NaNs.\n(The flag state which signals this is the setting of the parity\nflag: so the `U` condition is notionally equivalent to `PE`, and\n`NU` is equivalent to `PO`.)\n\nThe `FCMOV` conditions test the main processor's status flags, not\nthe FPU status flags, so using `FCMOV` directly after `FCOM`\nwill not work. Instead, you should either use `FCOMI` which writes\ndirectly to the main CPU flags word, or use `FSTSW` to extract the\nFPU flags.\n\nAlthough the `FCMOV` instructions are flagged `P6` above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "fcmovne": {"title":"`FCMOVcc`: Floating-Point Conditional Move","description":"The `FCMOV` instructions perform conditional move operations: each\nof them moves the contents of the given register into `ST0` if its\ncondition is satisfied, and does nothing if not.\n\nThe conditions are not the same as the standard condition codes used\nwith conditional jump instructions. The conditions `B`, `BE`,\n`NB`, `NBE`, `E` and `NE` are exactly as normal, but none of\nthe other standard ones are supported. Instead, the condition `U`\nand its counterpart `NU` are provided; the `U` condition is\nsatisfied if the last two floating-point numbers compared were\n*unordered*, i.e. they were not equal but neither one could be\nsaid to be greater than the other, for example if they were NaNs.\n(The flag state which signals this is the setting of the parity\nflag: so the `U` condition is notionally equivalent to `PE`, and\n`NU` is equivalent to `PO`.)\n\nThe `FCMOV` conditions test the main processor's status flags, not\nthe FPU status flags, so using `FCMOV` directly after `FCOM`\nwill not work. Instead, you should either use `FCOMI` which writes\ndirectly to the main CPU flags word, or use `FSTSW` to extract the\nFPU flags.\n\nAlthough the `FCMOV` instructions are flagged `P6` above, they\nmay not be supported by all Pentium Pro processors; the `CPUID`\ninstruction (`CPUID`) will return a bit which indicates whether\nconditional moves are supported."},
    "fcom": {"title":"`FCOM`, `FCOMP`, `FCOMPP`, `FCOMI`, `FCOMIP`: Floating-Point Compare","description":"`FCOM` compares `ST0` with the given operand, and sets the FPU\nflags accordingly. `ST0` is treated as the left-hand side of the\ncomparison, so that the carry flag is set (for a 'less-than' result)\nif `ST0` is less than the given operand.\n\n`FCOMP` does the same as `FCOM`, but pops the register stack\nafterwards. `FCOMPP` compares `ST0` with `ST1` and then pops\nthe register stack twice.\n\n`FCOMI` and `FCOMIP` work like the corresponding forms of\n`FCOM` and `FCOMP`, but write their results directly to the CPU\nflags register rather than the FPU status word, so they can be\nimmediately followed by conditional jump or conditional move\ninstructions.\n\nThe `FCOM` instructions differ from the `FUCOM` instructions\n(`FUCOM`) only in the way they handle quiet NaNs: `FUCOM`\nwill handle them silently and set the condition code flags to an\n'unordered' result, whereas `FCOM` will generate an exception."},
    "fcomi": {"title":"`FCOM`, `FCOMP`, `FCOMPP`, `FCOMI`, `FCOMIP`: Floating-Point Compare","description":"`FCOM` compares `ST0` with the given operand, and sets the FPU\nflags accordingly. `ST0` is treated as the left-hand side of the\ncomparison, so that the carry flag is set (for a 'less-than' result)\nif `ST0` is less than the given operand.\n\n`FCOMP` does the same as `FCOM`, but pops the register stack\nafterwards. `FCOMPP` compares `ST0` with `ST1` and then pops\nthe register stack twice.\n\n`FCOMI` and `FCOMIP` work like the corresponding forms of\n`FCOM` and `FCOMP`, but write their results directly to the CPU\nflags register rather than the FPU status word, so they can be\nimmediately followed by conditional jump or conditional move\ninstructions.\n\nThe `FCOM` instructions differ from the `FUCOM` instructions\n(`FUCOM`) only in the way they handle quiet NaNs: `FUCOM`\nwill handle them silently and set the condition code flags to an\n'unordered' result, whereas `FCOM` will generate an exception."},
    "fcomp": {"title":"`FCOM`, `FCOMP`, `FCOMPP`, `FCOMI`, `FCOMIP`: Floating-Point Compare","description":"`FCOM` compares `ST0` with the given operand, and sets the FPU\nflags accordingly. `ST0` is treated as the left-hand side of the\ncomparison, so that the carry flag is set (for a 'less-than' result)\nif `ST0` is less than the given operand.\n\n`FCOMP` does the same as `FCOM`, but pops the register stack\nafterwards. `FCOMPP` compares `ST0` with `ST1` and then pops\nthe register stack twice.\n\n`FCOMI` and `FCOMIP` work like the corresponding forms of\n`FCOM` and `FCOMP`, but write their results directly to the CPU\nflags register rather than the FPU status word, so they can be\nimmediately followed by conditional jump or conditional move\ninstructions.\n\nThe `FCOM` instructions differ from the `FUCOM` instructions\n(`FUCOM`) only in the way they handle quiet NaNs: `FUCOM`\nwill handle them silently and set the condition code flags to an\n'unordered' result, whereas `FCOM` will generate an exception."},
    "fcompp": {"title":"`FCOM`, `FCOMP`, `FCOMPP`, `FCOMI`, `FCOMIP`: Floating-Point Compare","description":"`FCOM` compares `ST0` with the given operand, and sets the FPU\nflags accordingly. `ST0` is treated as the left-hand side of the\ncomparison, so that the carry flag is set (for a 'less-than' result)\nif `ST0` is less than the given operand.\n\n`FCOMP` does the same as `FCOM`, but pops the register stack\nafterwards. `FCOMPP` compares `ST0` with `ST1` and then pops\nthe register stack twice.\n\n`FCOMI` and `FCOMIP` work like the corresponding forms of\n`FCOM` and `FCOMP`, but write their results directly to the CPU\nflags register rather than the FPU status word, so they can be\nimmediately followed by conditional jump or conditional move\ninstructions.\n\nThe `FCOM` instructions differ from the `FUCOM` instructions\n(`FUCOM`) only in the way they handle quiet NaNs: `FUCOM`\nwill handle them silently and set the condition code flags to an\n'unordered' result, whereas `FCOM` will generate an exception."},
    "fcos": {"title":"`FCOS`: Cosine","description":"`FCOS` computes the cosine of `ST0` (in radians), and stores the\nresult in `ST0`. The absolute value of `ST0` must be less than 2**63.\n\nSee also `FSINCOS` (`FSIN`)."},
    "fdecstp": {"title":"`FDECSTP`: Decrement Floating-Point Stack Pointer","description":"`FDECSTP` decrements the 'top' field in the floating-point status\nword. This has the effect of rotating the FPU register stack by one,\nas if the contents of `ST7` had been pushed on the stack. See also\n`FINCSTP` (`FINCSTP`)."},
    "fdiv": {"title":"`FDIV`, `FDIVP`, `FDIVR`, `FDIVRP`: Floating-Point Division","description":"- `FDIV` divides `ST0` by the given operand and stores the result\nback in `ST0`, unless the `TO` qualifier is given, in which case\nit divides the given operand by `ST0` and stores the result in the\noperand.\n\n- `FDIVR` does the same thing, but does the division the other way\nup: so if `TO` is not given, it divides the given operand by\n`ST0` and stores the result in `ST0`, whereas if `TO` is given\nit divides `ST0` by its operand and stores the result in the\noperand.\n\n- `FDIVP` operates like `FDIV TO`, but pops the register stack\nonce it has finished.\n\n- `FDIVRP` operates like `FDIVR TO`, but pops the register stack\nonce it has finished.\n\nFor FP/Integer divisions, see `FIDIV` (`FIDIV`)."},
    "fdivp": {"title":"`FDIV`, `FDIVP`, `FDIVR`, `FDIVRP`: Floating-Point Division","description":"- `FDIV` divides `ST0` by the given operand and stores the result\nback in `ST0`, unless the `TO` qualifier is given, in which case\nit divides the given operand by `ST0` and stores the result in the\noperand.\n\n- `FDIVR` does the same thing, but does the division the other way\nup: so if `TO` is not given, it divides the given operand by\n`ST0` and stores the result in `ST0`, whereas if `TO` is given\nit divides `ST0` by its operand and stores the result in the\noperand.\n\n- `FDIVP` operates like `FDIV TO`, but pops the register stack\nonce it has finished.\n\n- `FDIVRP` operates like `FDIVR TO`, but pops the register stack\nonce it has finished.\n\nFor FP/Integer divisions, see `FIDIV` (`FIDIV`)."},
    "fdivr": {"title":"`FDIV`, `FDIVP`, `FDIVR`, `FDIVRP`: Floating-Point Division","description":"- `FDIV` divides `ST0` by the given operand and stores the result\nback in `ST0`, unless the `TO` qualifier is given, in which case\nit divides the given operand by `ST0` and stores the result in the\noperand.\n\n- `FDIVR` does the same thing, but does the division the other way\nup: so if `TO` is not given, it divides the given operand by\n`ST0` and stores the result in `ST0`, whereas if `TO` is given\nit divides `ST0` by its operand and stores the result in the\noperand.\n\n- `FDIVP` operates like `FDIV TO`, but pops the register stack\nonce it has finished.\n\n- `FDIVRP` operates like `FDIVR TO`, but pops the register stack\nonce it has finished.\n\nFor FP/Integer divisions, see `FIDIV` (`FIDIV`)."},
    "fdivrp": {"title":"`FDIV`, `FDIVP`, `FDIVR`, `FDIVRP`: Floating-Point Division","description":"- `FDIV` divides `ST0` by the given operand and stores the result\nback in `ST0`, unless the `TO` qualifier is given, in which case\nit divides the given operand by `ST0` and stores the result in the\noperand.\n\n- `FDIVR` does the same thing, but does the division the other way\nup: so if `TO` is not given, it divides the given operand by\n`ST0` and stores the result in `ST0`, whereas if `TO` is given\nit divides `ST0` by its operand and stores the result in the\noperand.\n\n- `FDIVP` operates like `FDIV TO`, but pops the register stack\nonce it has finished.\n\n- `FDIVRP` operates like `FDIVR TO`, but pops the register stack\nonce it has finished.\n\nFor FP/Integer divisions, see `FIDIV` (`FIDIV`)."},
    "femms": {"title":"`FEMMS`: Faster Enter/Exit of the MMX or floating-point state","description":"`FEMMS` can be used in place of the `EMMS` instruction on\nprocessors which support the 3DNow! instruction set. Following\nexecution of `FEMMS`, the state of the `MMX/FP` registers\nis undefined, and this allows a faster context switch between\n`FP` and `MMX` instructions. The `FEMMS` instruction can\nalso be used *before* executing `MMX` instructions"},
    "ffree": {"title":"`FFREE`: Flag Floating-Point Register as Unused","description":"`FFREE` marks the given register as being empty.\n\n`FFREEP` marks the given register as being empty, and then\npops the register stack."},
    "fiadd": {"title":"`FIADD`: Floating-Point/Integer Addition","description":"`FIADD` adds the 16-bit or 32-bit integer stored in the given\nmemory location to `ST0`, storing the result in `ST0`."},
    "ficom": {"title":"`FICOM`, `FICOMP`: Floating-Point/Integer Compare","description":"`FICOM` compares `ST0` with the 16-bit or 32-bit integer stored\nin the given memory location, and sets the FPU flags accordingly.\n`FICOMP` does the same, but pops the register stack afterwards."},
    "ficomp": {"title":"`FICOM`, `FICOMP`: Floating-Point/Integer Compare","description":"`FICOM` compares `ST0` with the 16-bit or 32-bit integer stored\nin the given memory location, and sets the FPU flags accordingly.\n`FICOMP` does the same, but pops the register stack afterwards."},
    "fidiv": {"title":"`FIDIV`, `FIDIVR`: Floating-Point/Integer Division","description":"`FIDIV` divides `ST0` by the 16-bit or 32-bit integer stored in\nthe given memory location, and stores the result in `ST0`.\n`FIDIVR` does the division the other way up: it divides the\ninteger by `ST0`, but still stores the result in `ST0`."},
    "fidivr": {"title":"`FIDIV`, `FIDIVR`: Floating-Point/Integer Division","description":"`FIDIV` divides `ST0` by the 16-bit or 32-bit integer stored in\nthe given memory location, and stores the result in `ST0`.\n`FIDIVR` does the division the other way up: it divides the\ninteger by `ST0`, but still stores the result in `ST0`."},
    "fild": {"title":"`FILD`, `FIST`, `FISTP`: Floating-Point/Integer Conversion","description":"`FILD` loads an integer out of a memory location, converts it to a\nreal, and pushes it on the FPU register stack. `FIST` converts\n`ST0` to an integer and stores that in memory; `FISTP` does the\nsame as `FIST`, but pops the register stack afterwards."},
    "fimul": {"title":"`FIMUL`: Floating-Point/Integer Multiplication","description":"`FIMUL` multiplies `ST0` by the 16-bit or 32-bit integer stored\nin the given memory location, and stores the result in `ST0`."},
    "fincstp": {"title":"`FINCSTP`: Increment Floating-Point Stack Pointer","description":"`FINCSTP` increments the 'top' field in the floating-point status\nword. This has the effect of rotating the FPU register stack by one,\nas if the register stack had been popped; however, unlike the\npopping of the stack performed by many FPU instructions, it does not\nflag the new `ST7` (previously `ST0`) as empty. See also\n`FDECSTP` (`FDECSTP`)."},
    "finit": {"title":"`FINIT`, `FNINIT`: initialize Floating-Point Unit","description":"`FINIT` initializes the FPU to its default state. It flags all\nregisters as empty, without actually change their values, clears\nthe top of stack pointer. `FNINIT` does the same, without first\nwaiting for pending exceptions to clear."},
    "fist": {"title":"`FILD`, `FIST`, `FISTP`: Floating-Point/Integer Conversion","description":"`FILD` loads an integer out of a memory location, converts it to a\nreal, and pushes it on the FPU register stack. `FIST` converts\n`ST0` to an integer and stores that in memory; `FISTP` does the\nsame as `FIST`, but pops the register stack afterwards."},
    "fistp": {"title":"`FILD`, `FIST`, `FISTP`: Floating-Point/Integer Conversion","description":"`FILD` loads an integer out of a memory location, converts it to a\nreal, and pushes it on the FPU register stack. `FIST` converts\n`ST0` to an integer and stores that in memory; `FISTP` does the\nsame as `FIST`, but pops the register stack afterwards."},
    "fisub": {"title":"`FISUB`: Floating-Point/Integer Subtraction","description":"`FISUB` subtracts the 16-bit or 32-bit integer stored in the given\nmemory location from `ST0`, and stores the result in `ST0`.\n`FISUBR` does the subtraction the other way round, i.e. it\nsubtracts `ST0` from the given integer, but still stores the\nresult in `ST0`."},
    "fld": {"title":"`FLD`: Floating-Point Load","description":"`FLD` loads a floating-point value out of the given register or\nmemory location, and pushes it on the FPU register stack."},
    "fldcw": {"title":"`FLDCW`: Load Floating-Point Control Word","description":"`FLDCW` loads a 16-bit value out of memory and stores it into the\nFPU control word (governing things like the rounding mode, the\nprecision, and the exception masks). See also `FSTCW`\n(`FSTCW`). If exceptions are enabled and you don't want to\ngenerate one, use `FCLEX` or `FNCLEX` (`FCLEX`) before\nloading the new control word."},
    "fldenv": {"title":"`FLDENV`: Load Floating-Point Environment","description":"`FLDENV` loads the FPU operating environment (control word, status\nword, tag word, instruction pointer, data pointer and last opcode)\nfrom memory. The memory area is 14 or 28 bytes long, depending on\nthe CPU mode at the time. See also `FSTENV` (`FSTENV`)."},
    "fmul": {"title":"`FMUL`, `FMULP`: Floating-Point Multiply","description":"`FMUL` multiplies `ST0` by the given operand, and stores the\nresult in `ST0`, unless the `TO` qualifier is used in which case\nit stores the result in the operand. `FMULP` performs the same\noperation as `FMUL TO`, and then pops the register stack."},
    "fmulp": {"title":"`FMUL`, `FMULP`: Floating-Point Multiply","description":"`FMUL` multiplies `ST0` by the given operand, and stores the\nresult in `ST0`, unless the `TO` qualifier is used in which case\nit stores the result in the operand. `FMULP` performs the same\noperation as `FMUL TO`, and then pops the register stack."},
    "fnclex": {"title":"`FCLEX`, `FNCLEX`: Clear Floating-Point Exceptions","description":"`FCLEX` clears any floating-point exceptions which may be pending.\n`FNCLEX` does the same thing but doesn't wait for previous\nfloating-point operations (including the *handling* of pending\nexceptions) to finish first."},
    "fninit": {"title":"`FINIT`, `FNINIT`: initialize Floating-Point Unit","description":"`FINIT` initializes the FPU to its default state. It flags all\nregisters as empty, without actually change their values, clears\nthe top of stack pointer. `FNINIT` does the same, without first\nwaiting for pending exceptions to clear."},
    "fnop": {"title":"`FNOP`: Floating-Point No Operation","description":"`FNOP` does nothing."},
    "fpatan": {"title":"`FPATAN`, `FPTAN`: Arctangent and Tangent","description":"`FPATAN` computes the arctangent, in radians, of the result of\ndividing `ST1` by `ST0`, stores the result in `ST1`, and pops\nthe register stack. It works like the C `atan2` function, in that\nchanging the sign of both `ST0` and `ST1` changes the output\nvalue by pi (so it performs true rectangular-to-polar coordinate\nconversion, with `ST1` being the Y coordinate and `ST0` being\nthe X coordinate, not merely an arctangent).\n\n`FPTAN` computes the tangent of the value in `ST0` (in radians),\nand stores the result back into `ST0`.\n\nThe absolute value of `ST0` must be less than 2**63."},
    "fprem": {"title":"`FPREM`, `FPREM1`: Floating-Point Partial Remainder","description":"These instructions both produce the remainder obtained by dividing\n`ST0` by `ST1`. This is calculated, notionally, by dividing\n`ST0` by `ST1`, rounding the result to an integer, multiplying\nby `ST1` again, and computing the value which would need to be\nadded back on to the result to get back to the original value in\n`ST0`.\n\nThe two instructions differ in the way the notional round-to-integer\noperation is performed. `FPREM` does it by rounding towards zero,\nso that the remainder it returns always has the same sign as the\noriginal value in `ST0`; `FPREM1` does it by rounding to the\nnearest integer, so that the remainder always has at most half the\nmagnitude of `ST1`.\n\nBoth instructions calculate *partial* remainders, meaning that\nthey may not manage to provide the final result, but might leave\nintermediate results in `ST0` instead. If this happens, they will\nset the C2 flag in the FPU status word; therefore, to calculate a\nremainder, you should repeatedly execute `FPREM` or `FPREM1`\nuntil C2 becomes clear."},
    "fprem1": {"title":"`FPREM`, `FPREM1`: Floating-Point Partial Remainder","description":"These instructions both produce the remainder obtained by dividing\n`ST0` by `ST1`. This is calculated, notionally, by dividing\n`ST0` by `ST1`, rounding the result to an integer, multiplying\nby `ST1` again, and computing the value which would need to be\nadded back on to the result to get back to the original value in\n`ST0`.\n\nThe two instructions differ in the way the notional round-to-integer\noperation is performed. `FPREM` does it by rounding towards zero,\nso that the remainder it returns always has the same sign as the\noriginal value in `ST0`; `FPREM1` does it by rounding to the\nnearest integer, so that the remainder always has at most half the\nmagnitude of `ST1`.\n\nBoth instructions calculate *partial* remainders, meaning that\nthey may not manage to provide the final result, but might leave\nintermediate results in `ST0` instead. If this happens, they will\nset the C2 flag in the FPU status word; therefore, to calculate a\nremainder, you should repeatedly execute `FPREM` or `FPREM1`\nuntil C2 becomes clear."},
    "fptan": {"title":"`FPATAN`, `FPTAN`: Arctangent and Tangent","description":"`FPATAN` computes the arctangent, in radians, of the result of\ndividing `ST1` by `ST0`, stores the result in `ST1`, and pops\nthe register stack. It works like the C `atan2` function, in that\nchanging the sign of both `ST0` and `ST1` changes the output\nvalue by pi (so it performs true rectangular-to-polar coordinate\nconversion, with `ST1` being the Y coordinate and `ST0` being\nthe X coordinate, not merely an arctangent).\n\n`FPTAN` computes the tangent of the value in `ST0` (in radians),\nand stores the result back into `ST0`.\n\nThe absolute value of `ST0` must be less than 2**63."},
    "frndint": {"title":"`FRNDINT`: Floating-Point Round to Integer","description":"`FRNDINT` rounds the contents of `ST0` to an integer, according\nto the current rounding mode set in the FPU control word, and stores\nthe result back in `ST0`."},
    "frstor": {"title":"`FSAVE`, `FRSTOR`: Save/Restore Floating-Point State","description":"`FSAVE` saves the entire floating-point unit state, including all\nthe information saved by `FSTENV` (`FSTENV`) plus the\ncontents of all the registers, to a 94 or 108 byte area of memory\n(depending on the CPU mode). `FRSTOR` restores the floating-point\nstate from the same area of memory.\n\n`FNSAVE` does the same as `FSAVE`, without first waiting for\npending floating-point exceptions to clear."},
    "fsave": {"title":"`FSAVE`, `FRSTOR`: Save/Restore Floating-Point State","description":"`FSAVE` saves the entire floating-point unit state, including all\nthe information saved by `FSTENV` (`FSTENV`) plus the\ncontents of all the registers, to a 94 or 108 byte area of memory\n(depending on the CPU mode). `FRSTOR` restores the floating-point\nstate from the same area of memory.\n\n`FNSAVE` does the same as `FSAVE`, without first waiting for\npending floating-point exceptions to clear."},
    "fscale": {"title":"`FSCALE`: Scale Floating-Point Value by Power of Two","description":"`FSCALE` scales a number by a power of two: it rounds `ST1`\ntowards zero to obtain an integer, then multiplies `ST0` by two to\nthe power of that integer, and stores the result in `ST0`."},
    "fsetpm": {"title":"`FSETPM`: Set Protected Mode","description":"This instruction initializes protected mode on the 287 floating-point\ncoprocessor. It is only meaningful on that processor: the 387 and\nabove treat the instruction as a no-operation."},
    "fsin": {"title":"`FSIN`, `FSINCOS`: Sine and Cosine","description":"`FSIN` calculates the sine of `ST0` (in radians) and stores the\nresult in `ST0`. `FSINCOS` does the same, but then pushes the\ncosine of the same value on the register stack, so that the sine\nends up in `ST1` and the cosine in `ST0`. `FSINCOS` is faster\nthan executing `FSIN` and `FCOS` (see `FCOS`) in succession.\n\nThe absolute value of `ST0` must be less than 2**63."},
    "fsincos": {"title":"`FSIN`, `FSINCOS`: Sine and Cosine","description":"`FSIN` calculates the sine of `ST0` (in radians) and stores the\nresult in `ST0`. `FSINCOS` does the same, but then pushes the\ncosine of the same value on the register stack, so that the sine\nends up in `ST1` and the cosine in `ST0`. `FSINCOS` is faster\nthan executing `FSIN` and `FCOS` (see `FCOS`) in succession.\n\nThe absolute value of `ST0` must be less than 2**63."},
    "fsqrt": {"title":"`FSQRT`: Floating-Point Square Root","description":"`FSQRT` calculates the square root of `ST0` and stores the\nresult in `ST0`."},
    "fst": {"title":"`FST`, `FSTP`: Floating-Point Store","description":"`FST` stores the value in `ST0` into the given memory location\nor other FPU register. `FSTP` does the same, but then pops the\nregister stack."},
    "fstcw": {"title":"`FSTCW`: Store Floating-Point Control Word","description":"`FSTCW` stores the `FPU` control word (governing things like the\nrounding mode, the precision, and the exception masks) into a 2-byte\nmemory area. See also `FLDCW` (`FLDCW`).\n\n`FNSTCW` does the same thing as `FSTCW`, without first waiting\nfor pending floating-point exceptions to clear."},
    "fstenv": {"title":"`FSTENV`: Store Floating-Point Environment","description":"`FSTENV` stores the `FPU` operating environment (control word,\nstatus word, tag word, instruction pointer, data pointer and last\nopcode) into memory. The memory area is 14 or 28 bytes long,\ndepending on the CPU mode at the time. See also `FLDENV`\n(`FLDENV`).\n\n`FNSTENV` does the same thing as `FSTENV`, without first waiting\nfor pending floating-point exceptions to clear."},
    "fstp": {"title":"`FST`, `FSTP`: Floating-Point Store","description":"`FST` stores the value in `ST0` into the given memory location\nor other FPU register. `FSTP` does the same, but then pops the\nregister stack."},
    "fstsw": {"title":"`FSTSW`: Store Floating-Point Status Word","description":"`FSTSW` stores the `FPU` status word into `AX` or into a 2-byte\nmemory area.\n\n`FNSTSW` does the same thing as `FSTSW`, without first waiting\nfor pending floating-point exceptions to clear."},
    "fsub": {"title":"`FSUB`, `FSUBP`, `FSUBR`, `FSUBRP`: Floating-Point Subtract","description":"- `FSUB` subtracts the given operand from `ST0` and stores the\nresult back in `ST0`, unless the `TO` qualifier is given, in\nwhich case it subtracts `ST0` from the given operand and stores\nthe result in the operand.\n\n- `FSUBR` does the same thing, but does the subtraction the other\nway up: so if `TO` is not given, it subtracts `ST0` from the given\noperand and stores the result in `ST0`, whereas if `TO` is given\nit subtracts its operand from `ST0` and stores the result in the\noperand.\n\n- `FSUBP` operates like `FSUB TO`, but pops the register stack\nonce it has finished.\n\n- `FSUBRP` operates like `FSUBR TO`, but pops the register stack\nonce it has finished."},
    "fsubp": {"title":"`FSUB`, `FSUBP`, `FSUBR`, `FSUBRP`: Floating-Point Subtract","description":"- `FSUB` subtracts the given operand from `ST0` and stores the\nresult back in `ST0`, unless the `TO` qualifier is given, in\nwhich case it subtracts `ST0` from the given operand and stores\nthe result in the operand.\n\n- `FSUBR` does the same thing, but does the subtraction the other\nway up: so if `TO` is not given, it subtracts `ST0` from the given\noperand and stores the result in `ST0`, whereas if `TO` is given\nit subtracts its operand from `ST0` and stores the result in the\noperand.\n\n- `FSUBP` operates like `FSUB TO`, but pops the register stack\nonce it has finished.\n\n- `FSUBRP` operates like `FSUBR TO`, but pops the register stack\nonce it has finished."},
    "fsubr": {"title":"`FSUB`, `FSUBP`, `FSUBR`, `FSUBRP`: Floating-Point Subtract","description":"- `FSUB` subtracts the given operand from `ST0` and stores the\nresult back in `ST0`, unless the `TO` qualifier is given, in\nwhich case it subtracts `ST0` from the given operand and stores\nthe result in the operand.\n\n- `FSUBR` does the same thing, but does the subtraction the other\nway up: so if `TO` is not given, it subtracts `ST0` from the given\noperand and stores the result in `ST0`, whereas if `TO` is given\nit subtracts its operand from `ST0` and stores the result in the\noperand.\n\n- `FSUBP` operates like `FSUB TO`, but pops the register stack\nonce it has finished.\n\n- `FSUBRP` operates like `FSUBR TO`, but pops the register stack\nonce it has finished."},
    "fsubrp": {"title":"`FSUB`, `FSUBP`, `FSUBR`, `FSUBRP`: Floating-Point Subtract","description":"- `FSUB` subtracts the given operand from `ST0` and stores the\nresult back in `ST0`, unless the `TO` qualifier is given, in\nwhich case it subtracts `ST0` from the given operand and stores\nthe result in the operand.\n\n- `FSUBR` does the same thing, but does the subtraction the other\nway up: so if `TO` is not given, it subtracts `ST0` from the given\noperand and stores the result in `ST0`, whereas if `TO` is given\nit subtracts its operand from `ST0` and stores the result in the\noperand.\n\n- `FSUBP` operates like `FSUB TO`, but pops the register stack\nonce it has finished.\n\n- `FSUBRP` operates like `FSUBR TO`, but pops the register stack\nonce it has finished."},
    "ftst": {"title":"`FTST`: Test `ST0` Against Zero","description":"`FTST` compares `ST0` with zero and sets the FPU flags\naccordingly. `ST0` is treated as the left-hand side of the\ncomparison, so that a 'less-than' result is generated if `ST0` is\nnegative."},
    "fxam": {"title":"`FXAM`: Examine Class of Value in `ST0`","description":"`FXAM` sets the FPU flags `C3`, `C2` and `C0` depending on\nthe type of value stored in `ST0`:\n\n```\n Register contents     Flags\n```\n\n```\n Unsupported format    000\n NaN                   001\n Finite number         010\n Infinity              011\n Zero                  100\n Empty register        101\n Denormal              110\n```\n\nAdditionally, the `C1` flag is set to the sign of the number."},
    "fxch": {"title":"`FXCH`: Floating-Point Exchange","description":"`FXCH` exchanges `ST0` with a given FPU register. The no-operand\nform exchanges `ST0` with `ST1`."},
    "fxrstor": {"title":"`FXRSTOR`: Restore `FP`, `MMX` and `SSE` State","description":"The `FXRSTOR` instruction reloads the `FPU`, `MMX` and `SSE`\nstate (environment and registers), from the 512 byte memory area defined\nby the source operand. This data should have been written by a previous\n`FXSAVE`."},
    "fxsave": {"title":"`FXSAVE`: Store `FP`, `MMX` and `SSE` State","description":"`FXSAVE`The FXSAVE instruction writes the current `FPU`, `MMX`\nand `SSE` technology states (environment and registers), to the\n512 byte memory area defined by the destination operand. It does this\nwithout checking for pending unmasked floating-point exceptions\n(similar to the operation of `FNSAVE`).\n\nUnlike the `FSAVE/FNSAVE` instructions, the processor retains the\ncontents of the `FPU`, `MMX` and `SSE` state in the processor\nafter the state has been saved. This instruction has been optimized\nto maximize floating-point save performance."},
    "fxtract": {"title":"`FXTRACT`: Extract Exponent and Significand","description":"`FXTRACT` separates the number in `ST0` into its exponent and\nsignificand (mantissa), stores the exponent back into `ST0`, and\nthen pushes the significand on the register stack (so that the\nsignificand ends up in `ST0`, and the exponent in `ST1`)."},
    "fyl2x": {"title":"`FYL2X`, `FYL2XP1`: Compute Y times Log2(X) or Log2(X+1)","description":"`FYL2X` multiplies `ST1` by the base-2 logarithm of `ST0`,\nstores the result in `ST1`, and pops the register stack (so that\nthe result ends up in `ST0`). `ST0` must be non-zero and\npositive.\n\n`FYL2XP1` works the same way, but replacing the base-2 log of\n`ST0` with that of `ST0` plus one. This time, `ST0` must have\nmagnitude no greater than 1 minus half the square root of two."},
    "fyl2xp1": {"title":"`FYL2X`, `FYL2XP1`: Compute Y times Log2(X) or Log2(X+1)","description":"`FYL2X` multiplies `ST1` by the base-2 logarithm of `ST0`,\nstores the result in `ST1`, and pops the register stack (so that\nthe result ends up in `ST0`). `ST0` must be non-zero and\npositive.\n\n`FYL2XP1` works the same way, but replacing the base-2 log of\n`ST0` with that of `ST0` plus one. This time, `ST0` must have\nmagnitude no greater than 1 minus half the square root of two."},
    "hlt": {"title":"`HLT`: Halt Processor","description":"`HLT` puts the processor into a halted state, where it will\nperform no more operations until restarted by an interrupt or a\nreset.\n\nOn the 286 and later processors, this is a privileged instruction."},
    "ibts": {"title":"`IBTS`: Insert Bit String","description":"The implied operation of this instruction is:\n\n```\nIBTS r/m16,AX,CL,reg16\nIBTS r/m32,EAX,CL,reg32\n```\n\nWrites a bit string from the source operand to the destination.\n`CL` indicates the number of bits to be copied, from the low bits\nof the source. `(E)AX` indicates the low order bit offset in the\ndestination that is written to. For example, if `CL` is set to 4\nand `AX` (for 16-bit code) is set to 5, bits 0-3 of `src` will\nbe copied to bits 5-8 of `dst`. This instruction is very poorly\ndocumented, and I have been unable to find any official source of\ndocumentation on it.\n\n`IBTS` is supported only on the early Intel 386s, and conflicts\nwith the opcodes for `CMPXCHG486` (on early Intel 486s). NASM\nsupports it only for completeness. Its counterpart is `XBTS`\n(see `XBTS`)."},
    "icebp": {"title":"`INT3`, `INT1`, `ICEBP`, `INT01`: Breakpoints","description":"`INT1` and `INT3` are short one-byte forms of the instructions\n`INT 1` and `INT 3` (see `INT`). They perform a similar\nfunction to their longer counterparts, but take up less code space.\nThey are used as breakpoints by debuggers.\n\n- `INT1`, and its alternative synonyms `INT01` and `ICEBP`, is\nan instruction used by in-circuit emulators (ICEs). It is present,\nthough not documented, on some processors down to the 286, but is\nonly documented for the Pentium Pro. `INT3` is the instruction\nnormally used as a breakpoint by debuggers.\n\n- `INT3`, and its synonym `INT03`, is not precisely equivalent to\n`INT 3`: the short form, since it is designed to be used as a\nbreakpoint, bypasses the normal `IOPL` checks in virtual-8086 mode,\nand also does not go through interrupt redirection."},
    "idiv": {"title":"`IDIV`: Signed Integer Divide","description":"`IDIV` performs signed integer division. The explicit operand\nprovided is the divisor; the dividend and destination operands\nare implicit, in the following way:\n\n- For `IDIV r/m8`, `AX` is divided by the given operand;\nthe quotient is stored in `AL` and the remainder in `AH`.\n\n- For `IDIV r/m16`, `DX:AX` is divided by the given operand;\nthe quotient is stored in `AX` and the remainder in `DX`.\n\n- For `IDIV r/m32`, `EDX:EAX` is divided by the given operand;\nthe quotient is stored in `EAX` and the remainder in `EDX`.\n\nUnsigned integer division is performed by the `DIV` instruction:\nsee `DIV`."},
    "imul": {"title":"`IMUL`: Signed Integer Multiply","description":"`IMUL` performs signed integer multiplication. For the\nsingle-operand form, the other operand and destination are\nimplicit, in the following way:\n\n- For `IMUL r/m8`, `AL` is multiplied by the given operand;\nthe product is stored in `AX`.\n\n- For `IMUL r/m16`, `AX` is multiplied by the given operand;\nthe product is stored in `DX:AX`.\n\n- For `IMUL r/m32`, `EAX` is multiplied by the given operand;\nthe product is stored in `EDX:EAX`.\n\nThe two-operand form multiplies its two operands and stores the\nresult in the destination (first) operand. The three-operand\nform multiplies its last two operands and stores the result in\nthe first operand.\n\nThe two-operand form with an immediate second operand is in\nfact a shorthand for the three-operand form, as can be seen by\nexamining the opcode descriptions: in the two-operand form, the\ncode `/r` takes both its register and `r/m` parts from the\nsame operand (the first one).\n\nIn the forms with an 8-bit immediate operand and another longer\nsource operand, the immediate operand is considered to be signed,\nand is sign-extended to the length of the other source operand.\nIn these cases, the `BYTE` qualifier is necessary to force\nNASM to generate this form of the instruction.\n\nUnsigned integer multiplication is performed by the `MUL`\ninstruction: see `MUL`."},
    "in": {"title":"`IN`: Input from I/O Port","description":"`IN` reads a byte, word or doubleword from the specified I/O port,\nand stores it in the given destination register. The port number may\nbe specified as an immediate value if it is between 0 and 255, and\notherwise must be stored in `DX`. See also `OUT` (`OUT`)."},
    "inc": {"title":"`INC`: Increment Integer","description":"`INC` adds 1 to its operand. It does *not* affect the carry\nflag: to affect the carry flag, use `ADD something,1` (see\n`ADD`). `INC` affects all the other flags according to the result.\n\nThis instruction can be used with a `LOCK` prefix to allow atomic execution.\n\nSee also `DEC` (`DEC`)."},
    "insb": {"title":"`INSB`, `INSW`, `INSD`: Input String from I/O Port","description":"`INSB` inputs a byte from the I/O port specified in `DX` and\nstores it at `[ES:DI]` or `[ES:EDI]`. It then increments or\ndecrements (depending on the direction flag: increments if the flag\nis clear, decrements if it is set) `DI` or `EDI`.\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`INSW` and `INSD` work in the same way, but they input a word or\na doubleword instead of a byte, and increment or decrement the\naddressing register by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times.\n\nSee also `OUTSB`, `OUTSW` and `OUTSD` (`OUTSB`)."},
    "insd": {"title":"`INSB`, `INSW`, `INSD`: Input String from I/O Port","description":"`INSB` inputs a byte from the I/O port specified in `DX` and\nstores it at `[ES:DI]` or `[ES:EDI]`. It then increments or\ndecrements (depending on the direction flag: increments if the flag\nis clear, decrements if it is set) `DI` or `EDI`.\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`INSW` and `INSD` work in the same way, but they input a word or\na doubleword instead of a byte, and increment or decrement the\naddressing register by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times.\n\nSee also `OUTSB`, `OUTSW` and `OUTSD` (`OUTSB`)."},
    "insw": {"title":"`INSB`, `INSW`, `INSD`: Input String from I/O Port","description":"`INSB` inputs a byte from the I/O port specified in `DX` and\nstores it at `[ES:DI]` or `[ES:EDI]`. It then increments or\ndecrements (depending on the direction flag: increments if the flag\nis clear, decrements if it is set) `DI` or `EDI`.\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`INSW` and `INSD` work in the same way, but they input a word or\na doubleword instead of a byte, and increment or decrement the\naddressing register by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times.\n\nSee also `OUTSB`, `OUTSW` and `OUTSD` (`OUTSB`)."},
    "int": {"title":"`INT`: Software Interrupt","description":"`INT` causes a software interrupt through a specified vector\nnumber from 0 to 255.\n\nThe code generated by the `INT` instruction is always two bytes\nlong: although there are short forms for some `INT` instructions,\nNASM does not generate them when it sees the `INT` mnemonic. In\norder to generate single-byte breakpoint instructions, use the\n`INT3` or `INT1` instructions (see `INT1`) instead."},
    "int01": {"title":"`INT3`, `INT1`, `ICEBP`, `INT01`: Breakpoints","description":"`INT1` and `INT3` are short one-byte forms of the instructions\n`INT 1` and `INT 3` (see `INT`). They perform a similar\nfunction to their longer counterparts, but take up less code space.\nThey are used as breakpoints by debuggers.\n\n- `INT1`, and its alternative synonyms `INT01` and `ICEBP`, is\nan instruction used by in-circuit emulators (ICEs). It is present,\nthough not documented, on some processors down to the 286, but is\nonly documented for the Pentium Pro. `INT3` is the instruction\nnormally used as a breakpoint by debuggers.\n\n- `INT3`, and its synonym `INT03`, is not precisely equivalent to\n`INT 3`: the short form, since it is designed to be used as a\nbreakpoint, bypasses the normal `IOPL` checks in virtual-8086 mode,\nand also does not go through interrupt redirection."},
    "int03": {"title":"`INT3`, `INT1`, `ICEBP`, `INT01`: Breakpoints","description":"`INT1` and `INT3` are short one-byte forms of the instructions\n`INT 1` and `INT 3` (see `INT`). They perform a similar\nfunction to their longer counterparts, but take up less code space.\nThey are used as breakpoints by debuggers.\n\n- `INT1`, and its alternative synonyms `INT01` and `ICEBP`, is\nan instruction used by in-circuit emulators (ICEs). It is present,\nthough not documented, on some processors down to the 286, but is\nonly documented for the Pentium Pro. `INT3` is the instruction\nnormally used as a breakpoint by debuggers.\n\n- `INT3`, and its synonym `INT03`, is not precisely equivalent to\n`INT 3`: the short form, since it is designed to be used as a\nbreakpoint, bypasses the normal `IOPL` checks in virtual-8086 mode,\nand also does not go through interrupt redirection."},
    "int1": {"title":"`INT3`, `INT1`, `ICEBP`, `INT01`: Breakpoints","description":"`INT1` and `INT3` are short one-byte forms of the instructions\n`INT 1` and `INT 3` (see `INT`). They perform a similar\nfunction to their longer counterparts, but take up less code space.\nThey are used as breakpoints by debuggers.\n\n- `INT1`, and its alternative synonyms `INT01` and `ICEBP`, is\nan instruction used by in-circuit emulators (ICEs). It is present,\nthough not documented, on some processors down to the 286, but is\nonly documented for the Pentium Pro. `INT3` is the instruction\nnormally used as a breakpoint by debuggers.\n\n- `INT3`, and its synonym `INT03`, is not precisely equivalent to\n`INT 3`: the short form, since it is designed to be used as a\nbreakpoint, bypasses the normal `IOPL` checks in virtual-8086 mode,\nand also does not go through interrupt redirection."},
    "int3": {"title":"`INT3`, `INT1`, `ICEBP`, `INT01`: Breakpoints","description":"`INT1` and `INT3` are short one-byte forms of the instructions\n`INT 1` and `INT 3` (see `INT`). They perform a similar\nfunction to their longer counterparts, but take up less code space.\nThey are used as breakpoints by debuggers.\n\n- `INT1`, and its alternative synonyms `INT01` and `ICEBP`, is\nan instruction used by in-circuit emulators (ICEs). It is present,\nthough not documented, on some processors down to the 286, but is\nonly documented for the Pentium Pro. `INT3` is the instruction\nnormally used as a breakpoint by debuggers.\n\n- `INT3`, and its synonym `INT03`, is not precisely equivalent to\n`INT 3`: the short form, since it is designed to be used as a\nbreakpoint, bypasses the normal `IOPL` checks in virtual-8086 mode,\nand also does not go through interrupt redirection."},
    "into": {"title":"`INTO`: Interrupt if Overflow","description":"`INTO` performs an `INT 4` software interrupt (see `INT`)\nif and only if the overflow flag is set."},
    "invd": {"title":"`INVD`: Invalidate Internal Caches","description":"`INVD` invalidates and empties the processor's internal caches,\nand causes the processor to instruct external caches to do the same.\nIt does not write the contents of the caches back to memory first:\nany modified data held in the caches will be lost. To write the data\nback first, use `WBINVD` (`WBINVD`)."},
    "invlpg": {"title":"`INVLPG`: Invalidate TLB Entry","description":"`INVLPG` invalidates the translation lookahead buffer (TLB) entry\nassociated with the supplied memory address."},
    "iret": {"title":"`IRET`, `IRETW`, `IRETD`: Return from Interrupt","description":"`IRET` returns from an interrupt (hardware or software) by means\nof popping `IP` (or `EIP`), `CS` and the flags off the stack\nand then continuing execution from the new `CS:IP`.\n\n`IRETW` pops `IP`, `CS` and the flags as 2 bytes each, taking\n6 bytes off the stack in total. `IRETD` pops `EIP` as 4 bytes,\npops a further 4 bytes of which the top two are discarded and the\nbottom two go into `CS`, and pops the flags as 4 bytes as well,\ntaking 12 bytes off the stack.\n\n`IRET` is a shorthand for either `IRETW` or `IRETD`, depending\non the default `BITS` setting at the time."},
    "iretd": {"title":"`IRET`, `IRETW`, `IRETD`: Return from Interrupt","description":"`IRET` returns from an interrupt (hardware or software) by means\nof popping `IP` (or `EIP`), `CS` and the flags off the stack\nand then continuing execution from the new `CS:IP`.\n\n`IRETW` pops `IP`, `CS` and the flags as 2 bytes each, taking\n6 bytes off the stack in total. `IRETD` pops `EIP` as 4 bytes,\npops a further 4 bytes of which the top two are discarded and the\nbottom two go into `CS`, and pops the flags as 4 bytes as well,\ntaking 12 bytes off the stack.\n\n`IRET` is a shorthand for either `IRETW` or `IRETD`, depending\non the default `BITS` setting at the time."},
    "iretq": {"title":"`IRET`, `IRETW`, `IRETD`: Return from Interrupt","description":"`IRET` returns from an interrupt (hardware or software) by means\nof popping `IP` (or `EIP`), `CS` and the flags off the stack\nand then continuing execution from the new `CS:IP`.\n\n`IRETW` pops `IP`, `CS` and the flags as 2 bytes each, taking\n6 bytes off the stack in total. `IRETD` pops `EIP` as 4 bytes,\npops a further 4 bytes of which the top two are discarded and the\nbottom two go into `CS`, and pops the flags as 4 bytes as well,\ntaking 12 bytes off the stack.\n\n`IRET` is a shorthand for either `IRETW` or `IRETD`, depending\non the default `BITS` setting at the time."},
    "iretw": {"title":"`IRET`, `IRETW`, `IRETD`: Return from Interrupt","description":"`IRET` returns from an interrupt (hardware or software) by means\nof popping `IP` (or `EIP`), `CS` and the flags off the stack\nand then continuing execution from the new `CS:IP`.\n\n`IRETW` pops `IP`, `CS` and the flags as 2 bytes each, taking\n6 bytes off the stack in total. `IRETD` pops `EIP` as 4 bytes,\npops a further 4 bytes of which the top two are discarded and the\nbottom two go into `CS`, and pops the flags as 4 bytes as well,\ntaking 12 bytes off the stack.\n\n`IRET` is a shorthand for either `IRETW` or `IRETD`, depending\non the default `BITS` setting at the time."},
    "ja": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jae": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jb": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jbe": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jc": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jcxz": {"title":"`JCXZ`, `JECXZ`: Jump if CX/ECX Zero","description":"`JCXZ` performs a short jump (with maximum range 128 bytes) if and\nonly if the contents of the `CX` register is 0. `JECXZ` does the\nsame thing, but with `ECX`."},
    "je": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jecxz": {"title":"`JCXZ`, `JECXZ`: Jump if CX/ECX Zero","description":"`JCXZ` performs a short jump (with maximum range 128 bytes) if and\nonly if the contents of the `CX` register is 0. `JECXZ` does the\nsame thing, but with `ECX`."},
    "jg": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jge": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jl": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jle": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jmp": {"title":"`JMP`: Jump","description":"`JMP` jumps to a given address. The address may be specified as an\nabsolute segment and offset, or as a relative jump within the\ncurrent segment.\n\n`JMP SHORT imm` has a maximum range of 128 bytes, since the\ndisplacement is specified as only 8 bits, but takes up less code\nspace. NASM does not choose when to generate `JMP SHORT` for you:\nyou must explicitly code `SHORT` every time you want a short jump.\n\nYou can choose between the two immediate far jump forms (\\c{JMP\nimm:imm}) by the use of the `WORD` and `DWORD` keywords: \\c{JMP\nWORD 0x1234:0x5678}) or `JMP DWORD 0x1234:0x56789abc`.\n\nThe `JMP FAR mem` forms execute a far jump by loading the\ndestination address out of memory. The address loaded consists of 16\nor 32 bits of offset (depending on the operand size), and 16 bits of\nsegment. The operand size may be overridden using \\c{JMP WORD FAR\nmem} or `JMP DWORD FAR mem`.\n\nThe `JMP r/m` forms execute a near jump (within the same\nsegment), loading the destination address out of memory or out of a\nregister. The keyword `NEAR` may be specified, for clarity, in\nthese forms, but is not necessary. Again, operand size can be\noverridden using `JMP WORD mem` or `JMP DWORD mem`.\n\nAs a convenience, NASM does not require you to jump to a far symbol\nby coding the cumbersome `JMP SEG routine:routine`, but instead\nallows the easier synonym `JMP FAR routine`.\n\nThe `JMP r/m` forms given above are near calls; NASM will accept\nthe `NEAR` keyword (e.g. `JMP NEAR [address]`), even though it\nis not strictly necessary."},
    "jna": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnae": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnb": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnbe": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnc": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jne": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jng": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnge": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnl": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnle": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jno": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnp": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jns": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jnz": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jo": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jp": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jpe": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jpo": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jrcxz": {"title":"`JCXZ`, `JECXZ`: Jump if CX/ECX Zero","description":"`JCXZ` performs a short jump (with maximum range 128 bytes) if and\nonly if the contents of the `CX` register is 0. `JECXZ` does the\nsame thing, but with `ECX`."},
    "js": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "jz": {"title":"`Jcc`: Conditional Branch","description":"The conditional jump instructions execute a near (same segment)\njump if and only if their conditions are satisfied. For example,\n`JNZ` jumps only if the zero flag is not set.\n\nThe ordinary form of the instructions has only a 128-byte range; the\n`NEAR` form is a 386 extension to the instruction set, and can\nspan the full size of a segment. NASM will not override your choice\nof jump instruction: if you want `Jcc NEAR`, you have to use the\n`NEAR` keyword.\n\nThe `SHORT` keyword is allowed on the first form of the\ninstruction, for clarity, but is not necessary.\n\nFor details of the condition codes, see the condition codes."},
    "lahf": {"title":"`LAHF`: Load AH from Flags","description":"`LAHF` sets the `AH` register according to the contents of the\nlow byte of the flags word.\n\nThe operation of `LAHF` is:\n\n```\n AH <-- SF:ZF:0:AF:0:PF:1:CF\n```\n\nSee also `SAHF` (`SAHF`)."},
    "lar": {"title":"`LAR`: Load Access Rights","description":"`LAR` takes the segment selector specified by its source (second)\noperand, finds the corresponding segment descriptor in the GDT or\nLDT, and loads the access-rights byte of the descriptor into its\ndestination (first) operand."},
    "ldmxcsr": {"title":"`LDMXCSR`: Load Streaming SIMD Extension Control/Status","description":"`LDMXCSR` loads 32-bits of data from the specified memory location\ninto the `MXCSR` control/status register. `MXCSR` is used to\nenable masked/unmasked exception handling, to set rounding modes,\nto set flush-to-zero mode, and to view exception status flags.\n\nFor details of the `MXCSR` register, see the Intel processor docs.\n\nSee also `STMXCSR` (`STMXCSR`"},
    "lds": {"title":"`LDS`, `LES`, `LFS`, `LGS`, `LSS`: Load Far Pointer","description":"These instructions load an entire far pointer (16 or 32 bits of\noffset, plus 16 bits of segment) out of memory in one go. `LDS`,\nfor example, loads 16 or 32 bits from the given memory address into\nthe given register (depending on the size of the register), then\nloads the *next* 16 bits from memory into `DS`. `LES`,\n`LFS`, `LGS` and `LSS` work in the same way but use the other\nsegment registers."},
    "lea": {"title":"`LEA`: Load Effective Address","description":"`LEA`, despite its syntax, does not access memory. It calculates\nthe effective address specified by its second operand as if it were\ngoing to load or store data from it, but instead it stores the\ncalculated address into the register specified by its first operand.\nThis can be used to perform quite complex calculations (e.g. \\c{LEA\nEAX,[EBX+ECX*4+100]}) in one instruction.\n\n`LEA`, despite being a purely arithmetic instruction which\naccesses no memory, still requires square brackets around its second\noperand, as if it were a memory reference.\n\nThe size of the calculation is the current *address* size, and the\nsize that the result is stored as is the current *operand* size.\nIf the address and operand size are not the same, then if the\naddressing mode was 32-bits, the low 16-bits are stored, and if the\naddress was 16-bits, it is zero-extended to 32-bits before storing."},
    "leave": {"title":"`LEAVE`: Destroy Stack Frame","description":"`LEAVE` destroys a stack frame of the form created by the\n`ENTER` instruction (see `ENTER`). It is functionally\nequivalent to `MOV ESP,EBP` followed by `POP EBP` (or \\c{MOV\nSP,BP} followed by `POP BP` in 16-bit mode)."},
    "leaved": {"title":"`LEAVE`: Destroy Stack Frame","description":"`LEAVE` destroys a stack frame of the form created by the\n`ENTER` instruction (see `ENTER`). It is functionally\nequivalent to `MOV ESP,EBP` followed by `POP EBP` (or \\c{MOV\nSP,BP} followed by `POP BP` in 16-bit mode)."},
    "leaveq": {"title":"`LEAVE`: Destroy Stack Frame","description":"`LEAVE` destroys a stack frame of the form created by the\n`ENTER` instruction (see `ENTER`). It is functionally\nequivalent to `MOV ESP,EBP` followed by `POP EBP` (or \\c{MOV\nSP,BP} followed by `POP BP` in 16-bit mode)."},
    "leavew": {"title":"`LEAVE`: Destroy Stack Frame","description":"`LEAVE` destroys a stack frame of the form created by the\n`ENTER` instruction (see `ENTER`). It is functionally\nequivalent to `MOV ESP,EBP` followed by `POP EBP` (or \\c{MOV\nSP,BP} followed by `POP BP` in 16-bit mode)."},
    "les": {"title":"`LDS`, `LES`, `LFS`, `LGS`, `LSS`: Load Far Pointer","description":"These instructions load an entire far pointer (16 or 32 bits of\noffset, plus 16 bits of segment) out of memory in one go. `LDS`,\nfor example, loads 16 or 32 bits from the given memory address into\nthe given register (depending on the size of the register), then\nloads the *next* 16 bits from memory into `DS`. `LES`,\n`LFS`, `LGS` and `LSS` work in the same way but use the other\nsegment registers."},
    "lfence": {"title":"`LFENCE`: Load Fence","description":"`LFENCE` performs a serialising operation on all loads from memory\nthat were issued before the `LFENCE` instruction. This guarantees that\nall memory reads before the `LFENCE` instruction are visible before any\nreads after the `LFENCE` instruction.\n\n`LFENCE` is ordered respective to other `LFENCE` instruction, `MFENCE`,\nany memory read and any other serialising instruction (such as `CPUID`).\n\nWeakly ordered memory types can be used to achieve higher processor\nperformance through such techniques as out-of-order issue and\nspeculative reads. The degree to which a consumer of data recognizes\nor knows that the data is weakly ordered varies among applications\nand may be unknown to the producer of this data. The `LFENCE`\ninstruction provides a performance-efficient way of ensuring load\nordering between routines that produce weakly-ordered results and\nroutines that consume that data.\n\n`LFENCE` uses the following ModRM encoding:\n\n```\n          Mod (7:6)        = 11B\n          Reg/Opcode (5:3) = 101B\n          R/M (2:0)        = 000B\n```\n\nAll other ModRM encodings are defined to be reserved, and use\nof these encodings risks incompatibility with future processors.\n\nSee also `SFENCE` (`SFENCE`) and `MFENCE` (`MFENCE`)."},
    "lfs": {"title":"`LDS`, `LES`, `LFS`, `LGS`, `LSS`: Load Far Pointer","description":"These instructions load an entire far pointer (16 or 32 bits of\noffset, plus 16 bits of segment) out of memory in one go. `LDS`,\nfor example, loads 16 or 32 bits from the given memory address into\nthe given register (depending on the size of the register), then\nloads the *next* 16 bits from memory into `DS`. `LES`,\n`LFS`, `LGS` and `LSS` work in the same way but use the other\nsegment registers."},
    "lgdt": {"title":"`LGDT`, `LIDT`, `LLDT`: Load Descriptor Tables","description":"`LGDT` and `LIDT` both take a 6-byte memory area as an operand:\nthey load a 16-bit size limit and a 32-bit linear address from that\narea (in the opposite order) into the `GDTR` (global descriptor table\nregister) or `IDTR` (interrupt descriptor table register). These are\nthe only instructions which directly use *linear* addresses, rather\nthan segment/offset pairs.\n\n`LLDT` takes a segment selector as an operand. The processor looks\nup that selector in the GDT and stores the limit and base address\ngiven there into the `LDTR` (local descriptor table register).\n\nSee also `SGDT`, `SIDT` and `SLDT` (`SGDT`)."},
    "lgs": {"title":"`LDS`, `LES`, `LFS`, `LGS`, `LSS`: Load Far Pointer","description":"These instructions load an entire far pointer (16 or 32 bits of\noffset, plus 16 bits of segment) out of memory in one go. `LDS`,\nfor example, loads 16 or 32 bits from the given memory address into\nthe given register (depending on the size of the register), then\nloads the *next* 16 bits from memory into `DS`. `LES`,\n`LFS`, `LGS` and `LSS` work in the same way but use the other\nsegment registers."},
    "lidt": {"title":"`LGDT`, `LIDT`, `LLDT`: Load Descriptor Tables","description":"`LGDT` and `LIDT` both take a 6-byte memory area as an operand:\nthey load a 16-bit size limit and a 32-bit linear address from that\narea (in the opposite order) into the `GDTR` (global descriptor table\nregister) or `IDTR` (interrupt descriptor table register). These are\nthe only instructions which directly use *linear* addresses, rather\nthan segment/offset pairs.\n\n`LLDT` takes a segment selector as an operand. The processor looks\nup that selector in the GDT and stores the limit and base address\ngiven there into the `LDTR` (local descriptor table register).\n\nSee also `SGDT`, `SIDT` and `SLDT` (`SGDT`)."},
    "lldt": {"title":"`LGDT`, `LIDT`, `LLDT`: Load Descriptor Tables","description":"`LGDT` and `LIDT` both take a 6-byte memory area as an operand:\nthey load a 16-bit size limit and a 32-bit linear address from that\narea (in the opposite order) into the `GDTR` (global descriptor table\nregister) or `IDTR` (interrupt descriptor table register). These are\nthe only instructions which directly use *linear* addresses, rather\nthan segment/offset pairs.\n\n`LLDT` takes a segment selector as an operand. The processor looks\nup that selector in the GDT and stores the limit and base address\ngiven there into the `LDTR` (local descriptor table register).\n\nSee also `SGDT`, `SIDT` and `SLDT` (`SGDT`)."},
    "lmsw": {"title":"`LMSW`: Load/Store Machine Status Word","description":"`LMSW` loads the bottom four bits of the source operand into the\nbottom four bits of the `CR0` control register (or the Machine\nStatus Word, on 286 processors). See also `SMSW` (`SMSW`)."},
    "loadall": {"title":"`LOADALL`, `LOADALL286`: Load Processor State","description":"This instruction, in its two different-opcode forms, is apparently\nsupported on most 286 processors, some 386 and possibly some 486.\nThe opcode differs between the 286 and the 386.\n\nThe function of the instruction is to load all information relating\nto the state of the processor out of a block of memory: on the 286,\nthis block is located implicitly at absolute address `0x800`, and\non the 386 and 486 it is at `[ES:EDI]`."},
    "loadall286": {"title":"`LOADALL`, `LOADALL286`: Load Processor State","description":"This instruction, in its two different-opcode forms, is apparently\nsupported on most 286 processors, some 386 and possibly some 486.\nThe opcode differs between the 286 and the 386.\n\nThe function of the instruction is to load all information relating\nto the state of the processor out of a block of memory: on the 286,\nthis block is located implicitly at absolute address `0x800`, and\non the 386 and 486 it is at `[ES:EDI]`."},
    "lodsb": {"title":"`LODSB`, `LODSW`, `LODSD`: Load from String","description":"`LODSB` loads a byte from `[DS:SI]` or `[DS:ESI]` into `AL`.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` or\n`ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES LODSB`).\n\n`LODSW` and `LODSD` work in the same way, but they load a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1."},
    "lodsd": {"title":"`LODSB`, `LODSW`, `LODSD`: Load from String","description":"`LODSB` loads a byte from `[DS:SI]` or `[DS:ESI]` into `AL`.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` or\n`ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES LODSB`).\n\n`LODSW` and `LODSD` work in the same way, but they load a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1."},
    "lodsq": {"title":"`LODSB`, `LODSW`, `LODSD`: Load from String","description":"`LODSB` loads a byte from `[DS:SI]` or `[DS:ESI]` into `AL`.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` or\n`ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES LODSB`).\n\n`LODSW` and `LODSD` work in the same way, but they load a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1."},
    "lodsw": {"title":"`LODSB`, `LODSW`, `LODSD`: Load from String","description":"`LODSB` loads a byte from `[DS:SI]` or `[DS:ESI]` into `AL`.\nIt then increments or decrements (depending on the direction flag:\nincrements if the flag is clear, decrements if it is set) `SI` or\n`ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `ES LODSB`).\n\n`LODSW` and `LODSD` work in the same way, but they load a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1."},
    "loop": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopd": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loope": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "looped": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopeq": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopew": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopne": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopned": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopneq": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopnew": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopnz": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopnzd": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopnzq": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopnzw": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopq": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopw": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopz": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopzd": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopzq": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "loopzw": {"title":"`LOOP`, `LOOPE`, `LOOPZ`, `LOOPNE`, `LOOPNZ`: Loop with Counter","description":"`LOOP` decrements its counter register (either `CX` or `ECX` -\nif one is not specified explicitly, the `BITS` setting dictates\nwhich is used) by one, and if the counter does not become zero as a\nresult of this operation, it jumps to the given label. The jump has\na range of 128 bytes.\n\n`LOOPE` (or its synonym `LOOPZ`) adds the additional condition\nthat it only jumps if the counter is nonzero *and* the zero flag\nis set. Similarly, `LOOPNE` (and `LOOPNZ`) jumps only if the\ncounter is nonzero and the zero flag is clear."},
    "lsl": {"title":"`LSL`: Load Segment Limit","description":"`LSL` is given a segment selector in its source (second) operand;\nit computes the segment limit value by loading the segment limit\nfield from the associated segment descriptor in the `GDT` or `LDT`.\n(This involves shifting left by 12 bits if the segment limit is\npage-granular, and not if it is byte-granular; so you end up with a\nbyte limit in either case.) The segment limit obtained is then\nloaded into the destination (first) operand."},
    "lss": {"title":"`LDS`, `LES`, `LFS`, `LGS`, `LSS`: Load Far Pointer","description":"These instructions load an entire far pointer (16 or 32 bits of\noffset, plus 16 bits of segment) out of memory in one go. `LDS`,\nfor example, loads 16 or 32 bits from the given memory address into\nthe given register (depending on the size of the register), then\nloads the *next* 16 bits from memory into `DS`. `LES`,\n`LFS`, `LGS` and `LSS` work in the same way but use the other\nsegment registers."},
    "ltr": {"title":"`LTR`: Load Task Register","description":"`LTR` looks up the segment base and limit in the GDT or LDT\ndescriptor specified by the segment selector given as its operand,\nand loads them into the Task Register."},
    "maskmovdqu": {"title":"`MASKMOVDQU`: Byte Mask Write","description":"`MASKMOVDQU` stores data from xmm1 to the location specified by\n`ES:(E)DI`. The size of the store depends on the address-size\nattribute. The most significant bit in each byte of the mask\nregister xmm2 is used to selectively write the data (0 = no write,\n1 = write) on a per-byte basis."},
    "maskmovq": {"title":"`MASKMOVQ`: Byte Mask Write","description":"`MASKMOVQ` stores data from mm1 to the location specified by\n`ES:(E)DI`. The size of the store depends on the address-size\nattribute. The most significant bit in each byte of the mask\nregister mm2 is used to selectively write the data (0 = no write,\n1 = write) on a per-byte basis."},
    "maxpd": {"title":"`MAXPD`: Return Packed Double-Precision FP Maximum","description":"`MAXPD` performs a SIMD compare of the packed double-precision\nFP numbers from xmm1 and xmm2/mem, and stores the maximum values\nof each pair of values in xmm1. If the values being compared are\nboth zeroes, source2 (xmm2/m128) would be returned. If source2\n(xmm2/m128) is an SNaN, this SNaN is forwarded unchanged to the\ndestination (i.e., a QNaN version of the SNaN is not returned)."},
    "maxps": {"title":"`MAXPS`: Return Packed Single-Precision FP Maximum","description":"`MAXPS` performs a SIMD compare of the packed single-precision\nFP numbers from xmm1 and xmm2/mem, and stores the maximum values\nof each pair of values in xmm1. If the values being compared are\nboth zeroes, source2 (xmm2/m128) would be returned. If source2\n(xmm2/m128) is an SNaN, this SNaN is forwarded unchanged to the\ndestination (i.e., a QNaN version of the SNaN is not returned)."},
    "maxsd": {"title":"`MAXSD`: Return Scalar Double-Precision FP Maximum","description":"`MAXSD` compares the low-order double-precision FP numbers from\nxmm1 and xmm2/mem, and stores the maximum value in xmm1. If the\nvalues being compared are both zeroes, source2 (xmm2/m64) would\nbe returned. If source2 (xmm2/m64) is an SNaN, this SNaN is\nforwarded unchanged to the destination (i.e., a QNaN version of\nthe SNaN is not returned). The high quadword of the destination\nis left unchanged."},
    "maxss": {"title":"`MAXSS`: Return Scalar Single-Precision FP Maximum","description":"`MAXSS` compares the low-order single-precision FP numbers from\nxmm1 and xmm2/mem, and stores the maximum value in xmm1. If the\nvalues being compared are both zeroes, source2 (xmm2/m32) would\nbe returned. If source2 (xmm2/m32) is an SNaN, this SNaN is\nforwarded unchanged to the destination (i.e., a QNaN version of\nthe SNaN is not returned). The high three doublewords of the\ndestination are left unchanged."},
    "mfence": {"title":"`MFENCE`: Memory Fence","description":"`MFENCE` performs a serialising operation on all loads from memory\nand writes to memory that were issued before the `MFENCE` instruction.\nThis guarantees that all memory reads and writes before the `MFENCE`\ninstruction are completed before any reads and writes after the\n`MFENCE` instruction.\n\n`MFENCE` is ordered respective to other `MFENCE` instructions,\n`LFENCE`, `SFENCE`, any memory read and any other serialising\ninstruction (such as `CPUID`).\n\nWeakly ordered memory types can be used to achieve higher processor\nperformance through such techniques as out-of-order issue, speculative\nreads, write-combining, and write-collapsing. The degree to which a\nconsumer of data recognizes or knows that the data is weakly ordered\nvaries among applications and may be unknown to the producer of this\ndata. The `MFENCE` instruction provides a performance-efficient way\nof ensuring load and store ordering between routines that produce\nweakly-ordered results and routines that consume that data.\n\n`MFENCE` uses the following ModRM encoding:\n\n```\n          Mod (7:6)        = 11B\n          Reg/Opcode (5:3) = 110B\n          R/M (2:0)        = 000B\n```\n\nAll other ModRM encodings are defined to be reserved, and use\nof these encodings risks incompatibility with future processors.\n\nSee also `LFENCE` (`LFENCE`) and `SFENCE` (`SFENCE`)."},
    "minpd": {"title":"`MINPD`: Return Packed Double-Precision FP Minimum","description":"`MINPD` performs a SIMD compare of the packed double-precision\nFP numbers from xmm1 and xmm2/mem, and stores the minimum values\nof each pair of values in xmm1. If the values being compared are\nboth zeroes, source2 (xmm2/m128) would be returned. If source2\n(xmm2/m128) is an SNaN, this SNaN is forwarded unchanged to the\ndestination (i.e., a QNaN version of the SNaN is not returned)."},
    "minps": {"title":"`MINPS`: Return Packed Single-Precision FP Minimum","description":"`MINPS` performs a SIMD compare of the packed single-precision\nFP numbers from xmm1 and xmm2/mem, and stores the minimum values\nof each pair of values in xmm1. If the values being compared are\nboth zeroes, source2 (xmm2/m128) would be returned. If source2\n(xmm2/m128) is an SNaN, this SNaN is forwarded unchanged to the\ndestination (i.e., a QNaN version of the SNaN is not returned)."},
    "minsd": {"title":"`MINSD`: Return Scalar Double-Precision FP Minimum","description":"`MINSD` compares the low-order double-precision FP numbers from\nxmm1 and xmm2/mem, and stores the minimum value in xmm1. If the\nvalues being compared are both zeroes, source2 (xmm2/m64) would\nbe returned. If source2 (xmm2/m64) is an SNaN, this SNaN is\nforwarded unchanged to the destination (i.e., a QNaN version of\nthe SNaN is not returned). The high quadword of the destination\nis left unchanged."},
    "minss": {"title":"`MINSS`: Return Scalar Single-Precision FP Minimum","description":"`MINSS` compares the low-order single-precision FP numbers from\nxmm1 and xmm2/mem, and stores the minimum value in xmm1. If the\nvalues being compared are both zeroes, source2 (xmm2/m32) would\nbe returned. If source2 (xmm2/m32) is an SNaN, this SNaN is\nforwarded unchanged to the destination (i.e., a QNaN version of\nthe SNaN is not returned). The high three doublewords of the\ndestination are left unchanged."},
    "mov": {"title":"`MOV`: Move Data","description":"`MOV` copies the contents of its source (second) operand into its\ndestination (first) operand.\n\nIn all forms of the `MOV` instruction, the two operands are the\nsame size, except for moving between a segment register and an\n`r/m32` operand. These instructions are treated exactly like the\ncorresponding 16-bit equivalent (so that, for example, \\c{MOV\nDS,EAX} functions identically to `MOV DS,AX` but saves a prefix\nwhen in 32-bit mode), except that when a segment register is moved\ninto a 32-bit destination, the top two bytes of the result are\nundefined.\n\n`MOV` may not use `CS` as a destination.\n\n`CR4` is only a supported register on the Pentium and above.\n\nTest registers are supported on 386/486 processors and on some\nnon-Intel Pentium class processors."},
    "movapd": {"title":"`MOVAPD`: Move Aligned Packed Double-Precision FP Values","description":"`MOVAPD` moves a double quadword containing 2 packed double-precision\nFP values from the source operand to the destination. When the source\nor destination operand is a memory location, it must be aligned on a\n16-byte boundary.\n\nTo move data in and out of memory locations that are not known to be on\n16-byte boundaries, use the `MOVUPD` instruction (`MOVUPD`)."},
    "movaps": {"title":"`MOVAPS`: Move Aligned Packed Single-Precision FP Values","description":"`MOVAPS` moves a double quadword containing 4 packed single-precision\nFP values from the source operand to the destination. When the source\nor destination operand is a memory location, it must be aligned on a\n16-byte boundary.\n\nTo move data in and out of memory locations that are not known to be on\n16-byte boundaries, use the `MOVUPS` instruction (`MOVUPS`)."},
    "movd": {"title":"`MOVD`: Move Doubleword to/from MMX Register","description":"`MOVD` copies 32 bits from its source (second) operand into its\ndestination (first) operand. When the destination is a 64-bit `MMX`\nregister or a 128-bit `XMM` register, the input value is zero-extended\nto fill the destination register."},
    "movdq2q": {"title":"`MOVDQ2Q`: Move Quadword from XMM to MMX register.","description":"`MOVDQ2Q` moves the low quadword from the source operand to the\ndestination operand."},
    "movdqa": {"title":"`MOVDQA`: Move Aligned Double Quadword","description":"`MOVDQA` moves a double quadword from the source operand to the\ndestination operand. When the source or destination operand is a\nmemory location, it must be aligned to a 16-byte boundary.\n\nTo move a double quadword to or from unaligned memory locations,\nuse the `MOVDQU` instruction (`MOVDQU`)."},
    "movdqu": {"title":"`MOVDQU`: Move Unaligned Double Quadword","description":"`MOVDQU` moves a double quadword from the source operand to the\ndestination operand. When the source or destination operand is a\nmemory location, the memory may be unaligned.\n\nTo move a double quadword to or from known aligned memory locations,\nuse the `MOVDQA` instruction (`MOVDQA`)."},
    "movhlps": {"title":"`MOVHLPS`: Move Packed Single-Precision FP High to Low","description":"`MOVHLPS` moves the two packed single-precision FP values from the\nhigh quadword of the source register xmm2 to the low quadword of the\ndestination register, xmm2. The upper quadword of xmm1 is left unchanged.\n\nThe operation of this instruction is:\n\n```\n   dst[0-63]   := src[64-127],\n   dst[64-127] remains unchanged.\n```"},
    "movhpd": {"title":"`MOVHPD`: Move High Packed Double-Precision FP","description":"`MOVHPD` moves a double-precision FP value between the source and\ndestination operands. One of the operands is a 64-bit memory location,\nthe other is the high quadword of an `XMM` register.\n\nThe operation of this instruction is:\n\n```\n   mem[0-63]   := xmm[64-127];\n```\n\nor\n\n```\n   xmm[0-63]   remains unchanged;\n   xmm[64-127] := mem[0-63].\n```"},
    "movhps": {"title":"`MOVHPS`: Move High Packed Single-Precision FP","description":"`MOVHPS` moves two packed single-precision FP values between the source\nand destination operands. One of the operands is a 64-bit memory location,\nthe other is the high quadword of an `XMM` register.\n\nThe operation of this instruction is:\n\n```\n   mem[0-63]   := xmm[64-127];\n```\n\nor\n\n```\n   xmm[0-63]   remains unchanged;\n   xmm[64-127] := mem[0-63].\n```"},
    "movlhps": {"title":"`MOVLHPS`: Move Packed Single-Precision FP Low to High","description":"`MOVLHPS` moves the two packed single-precision FP values from the\nlow quadword of the source register xmm2 to the high quadword of the\ndestination register, xmm2. The low quadword of xmm1 is left unchanged.\n\nThe operation of this instruction is:\n\n```\n   dst[0-63]   remains unchanged;\n   dst[64-127] := src[0-63].\n```"},
    "movlpd": {"title":"`MOVLPD`: Move Low Packed Double-Precision FP","description":"`MOVLPD` moves a double-precision FP value between the source and\ndestination operands. One of the operands is a 64-bit memory location,\nthe other is the low quadword of an `XMM` register.\n\nThe operation of this instruction is:\n\n```\n   mem(0-63)   := xmm(0-63);\n```\n\nor\n\n```\n   xmm(0-63)   := mem(0-63);\n   xmm(64-127) remains unchanged.\n```"},
    "movlps": {"title":"`MOVLPS`: Move Low Packed Single-Precision FP","description":"`MOVLPS` moves two packed single-precision FP values between the source\nand destination operands. One of the operands is a 64-bit memory location,\nthe other is the low quadword of an `XMM` register.\n\nThe operation of this instruction is:\n\n```\n   mem(0-63)   := xmm(0-63);\n```\n\nor\n\n```\n   xmm(0-63)   := mem(0-63);\n   xmm(64-127) remains unchanged.\n```"},
    "movmskpd": {"title":"`MOVMSKPD`: Extract Packed Double-Precision FP Sign Mask","description":"`MOVMSKPD` inserts a 2-bit mask in r32, formed of the most significant\nbits of each double-precision FP number of the source operand."},
    "movmskps": {"title":"`MOVMSKPS`: Extract Packed Single-Precision FP Sign Mask","description":"`MOVMSKPS` inserts a 4-bit mask in r32, formed of the most significant\nbits of each single-precision FP number of the source operand."},
    "movntdq": {"title":"`MOVNTDQ`: Move Double Quadword Non Temporal","description":"`MOVNTDQ` moves the double quadword from the `XMM` source\nregister to the destination memory location, using a non-temporal\nhint. This store instruction minimizes cache pollution."},
    "movnti": {"title":"`MOVNTI`: Move Doubleword Non Temporal","description":"`MOVNTI` moves the doubleword in the source register\nto the destination memory location, using a non-temporal\nhint. This store instruction minimizes cache pollution."},
    "movntpd": {"title":"`MOVNTPD`: Move Aligned Four Packed Single-Precision FP Values Non Temporal","description":"`MOVNTPD` moves the double quadword from the `XMM` source\nregister to the destination memory location, using a non-temporal\nhint. This store instruction minimizes cache pollution. The memory\nlocation must be aligned to a 16-byte boundary."},
    "movntps": {"title":"`MOVNTPS`: Move Aligned Four Packed Single-Precision FP Values Non Temporal","description":"`MOVNTPS` moves the double quadword from the `XMM` source\nregister to the destination memory location, using a non-temporal\nhint. This store instruction minimizes cache pollution. The memory\nlocation must be aligned to a 16-byte boundary."},
    "movntq": {"title":"`MOVNTQ`: Move Quadword Non Temporal","description":"`MOVNTQ` moves the quadword in the `MMX` source register\nto the destination memory location, using a non-temporal\nhint. This store instruction minimizes cache pollution."},
    "movq": {"title":"`MOVQ`: Move Quadword to/from MMX Register","description":"`MOVQ` copies 64 bits from its source (second) operand into its\ndestination (first) operand. When the source is an `XMM` register,\nthe low quadword is moved. When the destination is an `XMM` register,\nthe destination is the low quadword, and the high quadword is cleared."},
    "movq2dq": {"title":"`MOVQ2DQ`: Move Quadword from MMX to XMM register.","description":"`MOVQ2DQ` moves the quadword from the source operand to the low\nquadword of the destination operand, and clears the high quadword."},
    "movsb": {"title":"`MOVSB`, `MOVSW`, `MOVSD`: Move String","description":"`MOVSB` copies the byte at `[DS:SI]` or `[DS:ESI]` to\n`[ES:DI]` or `[ES:EDI]`. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `SI` and `DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es movsb`). The use of `ES` for the store to `[DI]`\nor `[EDI]` cannot be overridden.\n\n`MOVSW` and `MOVSD` work in the same way, but they copy a word\nor a doubleword instead of a byte, and increment or decrement the\naddressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "movsd": {"title":"`MOVSB`, `MOVSW`, `MOVSD`: Move String","description":"`MOVSB` copies the byte at `[DS:SI]` or `[DS:ESI]` to\n`[ES:DI]` or `[ES:EDI]`. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `SI` and `DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es movsb`). The use of `ES` for the store to `[DI]`\nor `[EDI]` cannot be overridden.\n\n`MOVSW` and `MOVSD` work in the same way, but they copy a word\nor a doubleword instead of a byte, and increment or decrement the\naddressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "movsq": {"title":"`MOVSB`, `MOVSW`, `MOVSD`: Move String","description":"`MOVSB` copies the byte at `[DS:SI]` or `[DS:ESI]` to\n`[ES:DI]` or `[ES:EDI]`. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `SI` and `DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es movsb`). The use of `ES` for the store to `[DI]`\nor `[EDI]` cannot be overridden.\n\n`MOVSW` and `MOVSD` work in the same way, but they copy a word\nor a doubleword instead of a byte, and increment or decrement the\naddressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "movss": {"title":"`MOVSS`: Move Scalar Single-Precision FP Value","description":"`MOVSS` moves a single-precision FP value from the source operand\nto the destination operand. When the source or destination is a\nregister, the low-order FP value is read or written."},
    "movsw": {"title":"`MOVSB`, `MOVSW`, `MOVSD`: Move String","description":"`MOVSB` copies the byte at `[DS:SI]` or `[DS:ESI]` to\n`[ES:DI]` or `[ES:EDI]`. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `SI` and `DI` (or `ESI` and `EDI`).\n\nThe registers used are `SI` and `DI` if the address size is 16\nbits, and `ESI` and `EDI` if it is 32 bits. If you need to use\nan address size not equal to the current `BITS` setting, you can\nuse an explicit `a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es movsb`). The use of `ES` for the store to `[DI]`\nor `[EDI]` cannot be overridden.\n\n`MOVSW` and `MOVSD` work in the same way, but they copy a word\nor a doubleword instead of a byte, and increment or decrement the\naddressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "movsx": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "movsxb": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "movsxw": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "movupd": {"title":"`MOVUPD`: Move Unaligned Packed Double-Precision FP Values","description":"`MOVUPD` moves a double quadword containing 2 packed double-precision\nFP values from the source operand to the destination. This instruction\nmakes no assumptions about alignment of memory operands.\n\nTo move data in and out of memory locations that are known to be on 16-byte\nboundaries, use the `MOVAPD` instruction (`MOVAPD`)."},
    "movups": {"title":"`MOVUPS`: Move Unaligned Packed Single-Precision FP Values","description":"`MOVUPS` moves a double quadword containing 4 packed single-precision\nFP values from the source operand to the destination. This instruction\nmakes no assumptions about alignment of memory operands.\n\nTo move data in and out of memory locations that are known to be on 16-byte\nboundaries, use the `MOVAPS` instruction (`MOVAPS`)."},
    "movzx": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "movzxb": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "movzxd": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "movzxw": {"title":"`MOVSX`, `MOVZX`: Move Data with Sign or Zero Extend","description":"`MOVSX` sign-extends its source (second) operand to the length of\nits destination (first) operand, and copies the result into the\ndestination operand. `MOVZX` does the same, but zero-extends\nrather than sign-extending."},
    "mul": {"title":"`MUL`: Unsigned Integer Multiply","description":"`MUL` performs unsigned integer multiplication. The other operand\nto the multiplication, and the destination operand, are implicit, in\nthe following way:\n\n- For `MUL r/m8`, `AL` is multiplied by the given operand; the\nproduct is stored in `AX`.\n\n- For `MUL r/m16`, `AX` is multiplied by the given operand;\nthe product is stored in `DX:AX`.\n\n- For `MUL r/m32`, `EAX` is multiplied by the given operand;\nthe product is stored in `EDX:EAX`.\n\nSigned integer multiplication is performed by the `IMUL`\ninstruction: see `IMUL`."},
    "mulpd": {"title":"`MULPD`: Packed Single-FP Multiply","description":"`MULPD` performs a SIMD multiply of the packed double-precision FP\nvalues in both operands, and stores the results in the destination register."},
    "mulps": {"title":"`MULPS`: Packed Single-FP Multiply","description":"`MULPS` performs a SIMD multiply of the packed single-precision FP\nvalues in both operands, and stores the results in the destination register."},
    "mulsd": {"title":"`MULSD`: Scalar Single-FP Multiply","description":"`MULSD` multiplies the lowest double-precision FP values of both\noperands, and stores the result in the low quadword of xmm1."},
    "mulss": {"title":"`MULSS`: Scalar Single-FP Multiply","description":"`MULSS` multiplies the lowest single-precision FP values of both\noperands, and stores the result in the low doubleword of xmm1."},
    "neg": {"title":"`NEG`, `NOT`: Two's and One's Complement","description":"`NEG` replaces the contents of its operand by the two's complement\nnegation (invert all the bits and then add one) of the original\nvalue. `NOT`, similarly, performs one's complement (inverts all\nthe bits)."},
    "nop": {"title":"`NOP`: No Operation","description":"`NOP` performs no operation. Its opcode is the same as that\ngenerated by `XCHG AX,AX` or `XCHG EAX,EAX` (depending on the\nprocessor mode; see `XCHG`)."},
    "not": {"title":"`NEG`, `NOT`: Two's and One's Complement","description":"`NEG` replaces the contents of its operand by the two's complement\nnegation (invert all the bits and then add one) of the original\nvalue. `NOT`, similarly, performs one's complement (inverts all\nthe bits)."},
    "or": {"title":"`OR`: Bitwise OR","description":"`OR` performs a bitwise OR operation between its two operands\n(i.e. each bit of the result is 1 if and only if at least one of the\ncorresponding bits of the two inputs was 1), and stores the result\nin the destination (first) operand.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction.\n\nThe MMX instruction `POR` (see `POR`) performs the same\noperation on the 64-bit MMX registers."},
    "orpd": {"title":"`ORPD`: Bit-wise Logical OR of Double-Precision FP Data","description":"`ORPD` return a bit-wise logical OR between xmm1 and xmm2/mem,\nand stores the result in xmm1. If the source operand is a memory\nlocation, it must be aligned to a 16-byte boundary."},
    "orps": {"title":"`ORPS`: Bit-wise Logical OR of Single-Precision FP Data","description":"`ORPS` return a bit-wise logical OR between xmm1 and xmm2/mem,\nand stores the result in xmm1. If the source operand is a memory\nlocation, it must be aligned to a 16-byte boundary."},
    "out": {"title":"`OUT`: Output Data to I/O Port","description":"`OUT` writes the contents of the given source register to the\nspecified I/O port. The port number may be specified as an immediate\nvalue if it is between 0 and 255, and otherwise must be stored in\n`DX`. See also `IN` (`IN`)."},
    "outsb": {"title":"`OUTSB`, `OUTSW`, `OUTSD`: Output String to I/O Port","description":"`OUTSB` loads a byte from `[DS:SI]` or `[DS:ESI]` and writes\nit to the I/O port specified in `DX`. It then increments or\ndecrements (depending on the direction flag: increments if the flag\nis clear, decrements if it is set) `SI` or `ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es outsb`).\n\n`OUTSW` and `OUTSD` work in the same way, but they output a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "outsd": {"title":"`OUTSB`, `OUTSW`, `OUTSD`: Output String to I/O Port","description":"`OUTSB` loads a byte from `[DS:SI]` or `[DS:ESI]` and writes\nit to the I/O port specified in `DX`. It then increments or\ndecrements (depending on the direction flag: increments if the flag\nis clear, decrements if it is set) `SI` or `ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es outsb`).\n\n`OUTSW` and `OUTSD` work in the same way, but they output a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "outsw": {"title":"`OUTSB`, `OUTSW`, `OUTSD`: Output String to I/O Port","description":"`OUTSB` loads a byte from `[DS:SI]` or `[DS:ESI]` and writes\nit to the I/O port specified in `DX`. It then increments or\ndecrements (depending on the direction flag: increments if the flag\nis clear, decrements if it is set) `SI` or `ESI`.\n\nThe register used is `SI` if the address size is 16 bits, and\n`ESI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[SI]` or `[ESI]` can be\noverridden by using a segment register name as a prefix (for\nexample, `es outsb`).\n\n`OUTSW` and `OUTSD` work in the same way, but they output a\nword or a doubleword instead of a byte, and increment or decrement\nthe addressing registers by 2 or 4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "packssdw": {"title":"`PACKSSDW`, `PACKSSWB`, `PACKUSWB`: Pack Data","description":"All these instructions start by combining the source and destination\noperands, and then splitting the result in smaller sections which it\nthen packs into the destination register. The `MMX` versions pack\ntwo 64-bit operands into one 64-bit register, while the `SSE`\nversions pack two 128-bit operands into one 128-bit register.\n\n- `PACKSSWB` splits the combined value into words, and then reduces\nthe words to bytes, using signed saturation. It then packs the bytes\ninto the destination register in the same order the words were in.\n\n- `PACKSSDW` performs the same operation as `PACKSSWB`, except that\nit reduces doublewords to words, then packs them into the destination\nregister.\n\n- `PACKUSWB` performs the same operation as `PACKSSWB`, except that\nit uses unsigned saturation when reducing the size of the elements.\n\nTo perform signed saturation on a number, it is replaced by the largest\nsigned number (`7FFFh` or `7Fh`) that *will* fit, and if it is too\nsmall it is replaced by the smallest signed number (`8000h` or\n`80h`) that will fit. To perform unsigned saturation, the input is\ntreated as unsigned, and the input is replaced by the largest unsigned\nnumber that will fit."},
    "packsswb": {"title":"`PACKSSDW`, `PACKSSWB`, `PACKUSWB`: Pack Data","description":"All these instructions start by combining the source and destination\noperands, and then splitting the result in smaller sections which it\nthen packs into the destination register. The `MMX` versions pack\ntwo 64-bit operands into one 64-bit register, while the `SSE`\nversions pack two 128-bit operands into one 128-bit register.\n\n- `PACKSSWB` splits the combined value into words, and then reduces\nthe words to bytes, using signed saturation. It then packs the bytes\ninto the destination register in the same order the words were in.\n\n- `PACKSSDW` performs the same operation as `PACKSSWB`, except that\nit reduces doublewords to words, then packs them into the destination\nregister.\n\n- `PACKUSWB` performs the same operation as `PACKSSWB`, except that\nit uses unsigned saturation when reducing the size of the elements.\n\nTo perform signed saturation on a number, it is replaced by the largest\nsigned number (`7FFFh` or `7Fh`) that *will* fit, and if it is too\nsmall it is replaced by the smallest signed number (`8000h` or\n`80h`) that will fit. To perform unsigned saturation, the input is\ntreated as unsigned, and the input is replaced by the largest unsigned\nnumber that will fit."},
    "packuswb": {"title":"`PACKSSDW`, `PACKSSWB`, `PACKUSWB`: Pack Data","description":"All these instructions start by combining the source and destination\noperands, and then splitting the result in smaller sections which it\nthen packs into the destination register. The `MMX` versions pack\ntwo 64-bit operands into one 64-bit register, while the `SSE`\nversions pack two 128-bit operands into one 128-bit register.\n\n- `PACKSSWB` splits the combined value into words, and then reduces\nthe words to bytes, using signed saturation. It then packs the bytes\ninto the destination register in the same order the words were in.\n\n- `PACKSSDW` performs the same operation as `PACKSSWB`, except that\nit reduces doublewords to words, then packs them into the destination\nregister.\n\n- `PACKUSWB` performs the same operation as `PACKSSWB`, except that\nit uses unsigned saturation when reducing the size of the elements.\n\nTo perform signed saturation on a number, it is replaced by the largest\nsigned number (`7FFFh` or `7Fh`) that *will* fit, and if it is too\nsmall it is replaced by the smallest signed number (`8000h` or\n`80h`) that will fit. To perform unsigned saturation, the input is\ntreated as unsigned, and the input is replaced by the largest unsigned\nnumber that will fit."},
    "paddb": {"title":"`PADDB`, `PADDW`, `PADDD`: Add Packed Integers","description":"`PADDx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n\n- `PADDB` treats the operands as packed bytes, and adds each byte\nindividually;\n\n- `PADDW` treats the operands as packed words;\n\n- `PADDD` treats its operands as packed doublewords.\n\nWhen an individual result is too large to fit in its destination, it\nis wrapped around and the low bits are stored, with the carry bit\ndiscarded."},
    "paddd": {"title":"`PADDB`, `PADDW`, `PADDD`: Add Packed Integers","description":"`PADDx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n\n- `PADDB` treats the operands as packed bytes, and adds each byte\nindividually;\n\n- `PADDW` treats the operands as packed words;\n\n- `PADDD` treats its operands as packed doublewords.\n\nWhen an individual result is too large to fit in its destination, it\nis wrapped around and the low bits are stored, with the carry bit\ndiscarded."},
    "paddq": {"title":"`PADDQ`: Add Packed Quadword Integers","description":"`PADDQ` adds the quadwords in the source and destination operands, and\nstores the result in the destination register.\n\nWhen an individual result is too large to fit in its destination, it\nis wrapped around and the low bits are stored, with the carry bit\ndiscarded."},
    "paddsb": {"title":"`PADDSB`, `PADDSW`: Add Packed Signed Integers With Saturation","description":"`PADDSx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n`PADDSB` treats the operands as packed bytes, and adds each byte\nindividually; and `PADDSW` treats the operands as packed words.\n\nWhen an individual result is too large to fit in its destination, a\nsaturated value is stored. The resulting value is the value with the\nlargest magnitude of the same sign as the result which will fit in\nthe available space."},
    "paddsiw": {"title":"`PADDSIW`: MMX Packed Addition to Implicit Destination","description":"`PADDSIW`, specific to the Cyrix extensions to the MMX instruction\nset, performs the same function as `PADDSW`, except that the result\nis placed in an implied register.\n\nTo work out the implied register, invert the lowest bit in the register\nnumber. So `PADDSIW MM0,MM2` would put the result in `MM1`, but\n`PADDSIW MM1,MM2` would put the result in `MM0`."},
    "paddsw": {"title":"`PADDSB`, `PADDSW`: Add Packed Signed Integers With Saturation","description":"`PADDSx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n`PADDSB` treats the operands as packed bytes, and adds each byte\nindividually; and `PADDSW` treats the operands as packed words.\n\nWhen an individual result is too large to fit in its destination, a\nsaturated value is stored. The resulting value is the value with the\nlargest magnitude of the same sign as the result which will fit in\nthe available space."},
    "paddusb": {"title":"`PADDUSB`, `PADDUSW`: Add Packed Unsigned Integers With Saturation","description":"`PADDUSx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n`PADDUSB` treats the operands as packed bytes, and adds each byte\nindividually; and `PADDUSW` treats the operands as packed words.\n\nWhen an individual result is too large to fit in its destination, a\nsaturated value is stored. The resulting value is the maximum value\nthat will fit in the available space."},
    "paddusw": {"title":"`PADDUSB`, `PADDUSW`: Add Packed Unsigned Integers With Saturation","description":"`PADDUSx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n`PADDUSB` treats the operands as packed bytes, and adds each byte\nindividually; and `PADDUSW` treats the operands as packed words.\n\nWhen an individual result is too large to fit in its destination, a\nsaturated value is stored. The resulting value is the maximum value\nthat will fit in the available space."},
    "paddw": {"title":"`PADDB`, `PADDW`, `PADDD`: Add Packed Integers","description":"`PADDx` performs packed addition of the two operands, storing the\nresult in the destination (first) operand.\n\n- `PADDB` treats the operands as packed bytes, and adds each byte\nindividually;\n\n- `PADDW` treats the operands as packed words;\n\n- `PADDD` treats its operands as packed doublewords.\n\nWhen an individual result is too large to fit in its destination, it\nis wrapped around and the low bits are stored, with the carry bit\ndiscarded."},
    "pand": {"title":"`PAND`, `PANDN`: MMX Bitwise AND and AND-NOT","description":"`PAND` performs a bitwise AND operation between its two operands\n(i.e. each bit of the result is 1 if and only if the corresponding\nbits of the two inputs were both 1), and stores the result in the\ndestination (first) operand.\n\n`PANDN` performs the same operation, but performs a one's\ncomplement operation on the destination (first) operand first."},
    "pandn": {"title":"`PAND`, `PANDN`: MMX Bitwise AND and AND-NOT","description":"`PAND` performs a bitwise AND operation between its two operands\n(i.e. each bit of the result is 1 if and only if the corresponding\nbits of the two inputs were both 1), and stores the result in the\ndestination (first) operand.\n\n`PANDN` performs the same operation, but performs a one's\ncomplement operation on the destination (first) operand first."},
    "pause": {"title":"`PAUSE`: Spin Loop Hint","description":"`PAUSE` provides a hint to the processor that the following code\nis a spin loop. This improves processor performance by bypassing\npossible memory order violations. On older processors, this instruction\noperates as a `NOP`."},
    "paveb": {"title":"`PAVEB`: MMX Packed Average","description":"`PAVEB`, specific to the Cyrix MMX extensions, treats its two\noperands as vectors of eight unsigned bytes, and calculates the\naverage of the corresponding bytes in the operands. The resulting\nvector of eight averages is stored in the first operand.\n\nThis opcode maps to `MOVMSKPS r32, xmm` on processors that support\nthe SSE instruction set."},
    "pavgb": {"title":"`PAVGB` `PAVGW`: Average Packed Integers","description":"`PAVGB` and `PAVGW` add the unsigned data elements of the source\noperand to the unsigned data elements of the destination register,\nthen adds 1 to the temporary results. The results of the add are then\neach independently right-shifted by one bit position. The high order\nbits of each element are filled with the carry bits of the corresponding\nsum.\n\n- `PAVGB` operates on packed unsigned bytes, and\n\n- `PAVGW` operates on packed unsigned words."},
    "pavgusb": {"title":"`PAVGUSB`: Average of unsigned packed 8-bit values","description":"`PAVGUSB` adds the unsigned data elements of the source operand to\nthe unsigned data elements of the destination register, then adds 1\nto the temporary results. The results of the add are then each\nindependently right-shifted by one bit position. The high order bits\nof each element are filled with the carry bits of the corresponding\nsum.\n\nThis instruction performs exactly the same operations as the `PAVGB`\n`MMX` instruction (`PAVGB`)."},
    "pavgw": {"title":"`PAVGB` `PAVGW`: Average Packed Integers","description":"`PAVGB` and `PAVGW` add the unsigned data elements of the source\noperand to the unsigned data elements of the destination register,\nthen adds 1 to the temporary results. The results of the add are then\neach independently right-shifted by one bit position. The high order\nbits of each element are filled with the carry bits of the corresponding\nsum.\n\n- `PAVGB` operates on packed unsigned bytes, and\n\n- `PAVGW` operates on packed unsigned words."},
    "pdistib": {"title":"`PDISTIB`: MMX Packed Distance and Accumulate with Implied Register","description":"`PDISTIB`, specific to the Cyrix MMX extensions, treats its two\ninput operands as vectors of eight unsigned bytes. For each byte\nposition, it finds the absolute difference between the bytes in that\nposition in the two input operands, and adds that value to the byte\nin the same position in the implied output register. The addition is\nsaturated to an unsigned byte in the same way as `PADDUSB`.\n\nTo work out the implied register, invert the lowest bit in the register\nnumber. So `PDISTIB MM0,M64` would put the result in `MM1`, but\n`PDISTIB MM1,M64` would put the result in `MM0`.\n\nNote that `PDISTIB` cannot take a register as its second source\noperand.\n\nOperation:\n\n```\n   dstI[0-7]     := dstI[0-7]   + ABS(src0[0-7] - src1[0-7]),\n   dstI[8-15]    := dstI[8-15]  + ABS(src0[8-15] - src1[8-15]),\n   .......\n   .......\n   dstI[56-63]   := dstI[56-63] + ABS(src0[56-63] - src1[56-63]).\n```"},
    "pextrw": {"title":"`PEXTRW`: Extract Word","description":"`PEXTRW` moves the word in the source register (second operand)\nthat is pointed to by the count operand (third operand), into the\nlower half of a 32-bit general purpose register. The upper half of\nthe register is cleared to all 0s.\n\nWhen the source operand is an `MMX` register, the two least\nsignificant bits of the count specify the source word. When it is\nan `SSE` register, the three least significant bits specify the\nword location."},
    "pf2id": {"title":"`PF2ID`: Packed Single-Precision FP to Integer Convert","description":"`PF2ID` converts two single-precision FP values in the source operand\nto signed 32-bit integers, using truncation, and stores them in the\ndestination operand. Source values that are outside the range supported\nby the destination are saturated to the largest absolute value of the\nsame sign."},
    "pf2iw": {"title":"`PF2IW`: Packed Single-Precision FP to Integer Word Convert","description":"`PF2IW` converts two single-precision FP values in the source operand\nto signed 16-bit integers, using truncation, and stores them in the\ndestination operand. Source values that are outside the range supported\nby the destination are saturated to the largest absolute value of the\nsame sign.\n\n- In the K6-2 and K6-III, the 16-bit value is zero-extended to 32-bits\nbefore storing.\n\n- In the K6-2+, K6-III+ and Athlon processors, the value is sign-extended\nto 32-bits before storing."},
    "pfacc": {"title":"`PFACC`: Packed Single-Precision FP Accumulate","description":"`PFACC` adds the two single-precision FP values from the destination\noperand together, then adds the two single-precision FP values from the\nsource operand, and places the results in the low and high doublewords\nof the destination operand.\n\nThe operation is:\n\n```\n   dst[0-31]   := dst[0-31] + dst[32-63],\n   dst[32-63]  := src[0-31] + src[32-63].\n```"},
    "pfadd": {"title":"`PFADD`: Packed Single-Precision FP Addition","description":"`PFADD` performs addition on each of two packed single-precision\nFP value pairs.\n\n```\n   dst[0-31]   := dst[0-31]  + src[0-31],\n   dst[32-63]  := dst[32-63] + src[32-63].\n```"},
    "pfmax": {"title":"`PFMAX`: Packed Single-Precision FP Maximum","description":"`PFMAX` returns the higher of each pair of single-precision FP values.\nIf the higher value is zero, it is returned as positive zero."},
    "pfmin": {"title":"`PFMIN`: Packed Single-Precision FP Minimum","description":"`PFMIN` returns the lower of each pair of single-precision FP values.\nIf the lower value is zero, it is returned as positive zero."},
    "pfmul": {"title":"`PFMUL`: Packed Single-Precision FP Multiply","description":"`PFMUL` returns the product of each pair of single-precision FP values.\n\n```\n   dst[0-31]  := dst[0-31]  * src[0-31],\n   dst[32-63] := dst[32-63] * src[32-63].\n```"},
    "pfnacc": {"title":"`PFNACC`: Packed Single-Precision FP Negative Accumulate","description":"`PFNACC` performs a negative accumulate of the two single-precision\nFP values in the source and destination registers. The result of the\naccumulate from the destination register is stored in the low doubleword\nof the destination, and the result of the source accumulate is stored in\nthe high doubleword of the destination register.\n\nThe operation is:\n\n```\n   dst[0-31]  := dst[0-31] - dst[32-63],\n   dst[32-63] := src[0-31] - src[32-63].\n```"},
    "pfpnacc": {"title":"`PFPNACC`: Packed Single-Precision FP Mixed Accumulate","description":"`PFPNACC` performs a positive accumulate of the two single-precision\nFP values in the source register and a negative accumulate of the\ndestination register. The result of the accumulate from the destination\nregister is stored in the low doubleword of the destination, and the\nresult of the source accumulate is stored in the high doubleword of the\ndestination register.\n\nThe operation is:\n\n```\n   dst[0-31]  := dst[0-31] - dst[32-63],\n   dst[32-63] := src[0-31] + src[32-63].\n```"},
    "pfrcp": {"title":"`PFRCP`: Packed Single-Precision FP Reciprocal Approximation","description":"`PFRCP` performs a low precision estimate of the reciprocal of the\nlow-order single-precision FP value in the source operand, storing the\nresult in both halves of the destination register. The result is accurate\nto 14 bits.\n\nFor higher precision reciprocals, this instruction should be followed by\ntwo more instructions: `PFRCPIT1` (`PFRCPIT1`) and `PFRCPIT2`\n(`PFRCPIT1`). This will result in a 24-bit accuracy. For more details,\nsee the AMD 3DNow! technology manual."},
    "pfrcpit1": {"title":"`PFRCPIT1`: Packed Single-Precision FP Reciprocal, First Iteration Step","description":"`PFRCPIT1` performs the first intermediate step in the calculation of\nthe reciprocal of a single-precision FP value. The first source value\n(`mm1` is the original value, and the second source value (`mm2/m64`\nis the result of a `PFRCP` instruction.\n\nFor the final step in a reciprocal, returning the full 24-bit accuracy\nof a single-precision FP value, see `PFRCPIT2` (`PFRCPIT2`). For\nmore details, see the AMD 3DNow! technology manual."},
    "pfrcpit2": {"title":"`PFRCPIT2`: Packed Single-Precision FP Reciprocal/ Reciprocal Square Root, Second Iteration Step","description":"`PFRCPIT2` performs the second and final intermediate step in the\ncalculation of a reciprocal or reciprocal square root, refining the\nvalues returned by the `PFRCP` and `PFRSQRT` instructions,\nrespectively.\n\nThe first source value (`mm1`) is the output of either a `PFRCPIT1`\nor a `PFRSQIT1` instruction, and the second source is the output of\neither the `PFRCP` or the `PFRSQRT` instruction. For more details,\nsee the AMD 3DNow! technology manual."},
    "pfrsqit1": {"title":"`PFRSQIT1`: Packed Single-Precision FP Reciprocal Square Root, First Iteration Step","description":"`PFRSQIT1` performs the first intermediate step in the calculation of\nthe reciprocal square root of a single-precision FP value. The first\nsource value (`mm1` is the square of the result of a `PFRSQRT`\ninstruction, and the second source value (`mm2/m64` is the original\nvalue.\n\nFor the final step in a calculation, returning the full 24-bit accuracy\nof a single-precision FP value, see `PFRCPIT2` (`PFRCPIT2`). For\nmore details, see the AMD 3DNow! technology manual."},
    "pfrsqrt": {"title":"`PFRSQRT`: Packed Single-Precision FP Reciprocal Square Root Approximation","description":"`PFRSQRT` performs a low precision estimate of the reciprocal square\nroot of the low-order single-precision FP value in the source operand,\nstoring the result in both halves of the destination register. The result\nis accurate to 15 bits.\n\nFor higher precision reciprocals, this instruction should be followed by\ntwo more instructions: `PFRSQIT1` (`PFRSQIT1`) and `PFRCPIT2`\n(`PFRCPIT1`). This will result in a 24-bit accuracy. For more details,\nsee the AMD 3DNow! technology manual."},
    "pfsub": {"title":"`PFSUB`: Packed Single-Precision FP Subtract","description":"`PFSUB` subtracts the single-precision FP values in the source from\nthose in the destination, and stores the result in the destination\noperand.\n\n```\n   dst[0-31]  := dst[0-31]  - src[0-31],\n   dst[32-63] := dst[32-63] - src[32-63].\n```"},
    "pfsubr": {"title":"`PFSUBR`: Packed Single-Precision FP Reverse Subtract","description":"`PFSUBR` subtracts the single-precision FP values in the destination\nfrom those in the source, and stores the result in the destination\noperand.\n\n```\n   dst[0-31]  := src[0-31]  - dst[0-31],\n   dst[32-63] := src[32-63] - dst[32-63].\n```"},
    "pi2fd": {"title":"`PI2FD`: Packed Doubleword Integer to Single-Precision FP Convert","description":"`PF2ID` converts two signed 32-bit integers in the source operand\nto single-precision FP values, using truncation of significant digits,\nand stores them in the destination operand."},
    "pinsrw": {"title":"`PINSRW`: Insert Word","description":"`PINSRW` loads a word from a 16-bit register (or the low half of a\n32-bit register), or from memory, and loads it to the word position\nin the destination register, pointed at by the count operand (third\noperand). If the destination is an `MMX` register, the low two bits\nof the count byte are used, if it is an `XMM` register the low 3\nbits are used. The insertion is done in such a way that the other\nwords from the destination register are left untouched."},
    "pmachriw": {"title":"`PMACHRIW`: Packed Multiply and Accumulate with Rounding","description":"`PMACHRIW` takes two packed 16-bit integer inputs, multiplies the\nvalues in the inputs, rounds on bit 15 of each result, then adds bits\n15-30 of each result to the corresponding position of the *implied*\ndestination register.\n\nThe operation of this instruction is:\n\n```\n   dstI[0-15]  := dstI[0-15]  + (mm[0-15] *m64[0-15]\n                                          + 0x00004000)[15-30],\n   dstI[16-31] := dstI[16-31] + (mm[16-31]*m64[16-31]\n                                          + 0x00004000)[15-30],\n   dstI[32-47] := dstI[32-47] + (mm[32-47]*m64[32-47]\n                                          + 0x00004000)[15-30],\n   dstI[48-63] := dstI[48-63] + (mm[48-63]*m64[48-63]\n                                          + 0x00004000)[15-30].\n```\n\nNote that `PMACHRIW` cannot take a register as its second source\noperand."},
    "pmaddwd": {"title":"`PMADDWD`: MMX Packed Multiply and Add","description":"`PMADDWD` treats its two inputs as vectors of signed words. It\nmultiplies corresponding elements of the two operands, giving doubleword\nresults. These are then added together in pairs and stored in the\ndestination operand.\n\nThe operation of this instruction is:\n\n```\n   dst[0-31]   := (dst[0-15] * src[0-15])\n                               + (dst[16-31] * src[16-31]);\n   dst[32-63]  := (dst[32-47] * src[32-47])\n                               + (dst[48-63] * src[48-63]);\n```\n\nThe following apply to the `SSE` version of the instruction:\n\n```\n   dst[64-95]  := (dst[64-79] * src[64-79])\n                               + (dst[80-95] * src[80-95]);\n   dst[96-127] := (dst[96-111] * src[96-111])\n                               + (dst[112-127] * src[112-127]).\n```"},
    "pmagw": {"title":"`PMAGW`: MMX Packed Magnitude","description":"`PMAGW`, specific to the Cyrix MMX extensions, treats both its\noperands as vectors of four signed words. It compares the absolute\nvalues of the words in corresponding positions, and sets each word\nof the destination (first) operand to whichever of the two words in\nthat position had the larger absolute value."},
    "pmaxsw": {"title":"`PMAXSW`: Packed Signed Integer Word Maximum","description":"`PMAXSW` compares each pair of words in the two source operands, and\nfor each pair it stores the maximum value in the destination register."},
    "pmaxub": {"title":"`PMAXUB`: Packed Unsigned Integer Byte Maximum","description":"`PMAXUB` compares each pair of bytes in the two source operands, and\nfor each pair it stores the maximum value in the destination register."},
    "pminsw": {"title":"`PMINSW`: Packed Signed Integer Word Minimum","description":"`PMINSW` compares each pair of words in the two source operands, and\nfor each pair it stores the minimum value in the destination register."},
    "pminub": {"title":"`PMINUB`: Packed Unsigned Integer Byte Minimum","description":"`PMINUB` compares each pair of bytes in the two source operands, and\nfor each pair it stores the minimum value in the destination register."},
    "pmovmskb": {"title":"`PMOVMSKB`: Move Byte Mask To Integer","description":"`PMOVMSKB` returns an 8-bit or 16-bit mask formed of the most\nsignificant bits of each byte of source operand (8-bits for an\n`MMX` register, 16-bits for an `XMM` register)."},
    "pmulhriw": {"title":"`PMULHRWC`, `PMULHRIW`: Multiply Packed 16-bit Integers With Rounding, and Store High Word","description":"These instructions take two packed 16-bit integer inputs, multiply the\nvalues in the inputs, round on bit 15 of each result, then store bits\n15-30 of each result to the corresponding position of the destination\nregister.\n\n- For `PMULHRWC`, the destination is the first source operand.\n\n- For `PMULHRIW`, the destination is an implied register (worked out\nas described for `PADDSIW` (`PADDSIW`)).\n\nThe operation of this instruction is:\n\n```\n   dst[0-15]  := (src1[0-15] *src2[0-15]  + 0x00004000)[15-30]\n   dst[16-31] := (src1[16-31]*src2[16-31] + 0x00004000)[15-30]\n   dst[32-47] := (src1[32-47]*src2[32-47] + 0x00004000)[15-30]\n   dst[48-63] := (src1[48-63]*src2[48-63] + 0x00004000)[15-30]\n```\n\nSee also `PMULHRWA` (`PMULHRWA`) for a 3DNow! version of this\ninstruction."},
    "pmulhrwa": {"title":"`PMULHRWA`: Multiply Packed 16-bit Integers With Rounding, and Store High Word","description":"`PMULHRWA` takes two packed 16-bit integer inputs, multiplies\nthe values in the inputs, rounds on bit 16 of each result, then\nstores bits 16-31 of each result to the corresponding position\nof the destination register.\n\nThe operation of this instruction is:\n\n```\n   dst[0-15]  := (src1[0-15] *src2[0-15]  + 0x00008000)[16-31];\n   dst[16-31] := (src1[16-31]*src2[16-31] + 0x00008000)[16-31];\n   dst[32-47] := (src1[32-47]*src2[32-47] + 0x00008000)[16-31];\n   dst[48-63] := (src1[48-63]*src2[48-63] + 0x00008000)[16-31].\n```\n\nSee also `PMULHRWC` (`PMULHRW`) for a Cyrix version of this\ninstruction."},
    "pmulhrwc": {"title":"`PMULHRWC`, `PMULHRIW`: Multiply Packed 16-bit Integers With Rounding, and Store High Word","description":"These instructions take two packed 16-bit integer inputs, multiply the\nvalues in the inputs, round on bit 15 of each result, then store bits\n15-30 of each result to the corresponding position of the destination\nregister.\n\n- For `PMULHRWC`, the destination is the first source operand.\n\n- For `PMULHRIW`, the destination is an implied register (worked out\nas described for `PADDSIW` (`PADDSIW`)).\n\nThe operation of this instruction is:\n\n```\n   dst[0-15]  := (src1[0-15] *src2[0-15]  + 0x00004000)[15-30]\n   dst[16-31] := (src1[16-31]*src2[16-31] + 0x00004000)[15-30]\n   dst[32-47] := (src1[32-47]*src2[32-47] + 0x00004000)[15-30]\n   dst[48-63] := (src1[48-63]*src2[48-63] + 0x00004000)[15-30]\n```\n\nSee also `PMULHRWA` (`PMULHRWA`) for a 3DNow! version of this\ninstruction."},
    "pmulhuw": {"title":"`PMULHUW`: Multiply Packed 16-bit Integers, and Store High Word","description":"`PMULHUW` takes two packed unsigned 16-bit integer inputs, multiplies\nthe values in the inputs, then stores bits 16-31 of each result to the\ncorresponding position of the destination register."},
    "pmulhw": {"title":"`PMULHW`, `PMULLW`: Multiply Packed 16-bit Integers, and Store","description":"`PMULxW` takes two packed unsigned 16-bit integer inputs, and\nmultiplies the values in the inputs, forming doubleword results.\n\n- `PMULHW` then stores the top 16 bits of each doubleword in the\ndestination (first) operand;\n\n- `PMULLW` stores the bottom 16 bits of each doubleword in the\ndestination operand."},
    "pmullw": {"title":"`PMULHW`, `PMULLW`: Multiply Packed 16-bit Integers, and Store","description":"`PMULxW` takes two packed unsigned 16-bit integer inputs, and\nmultiplies the values in the inputs, forming doubleword results.\n\n- `PMULHW` then stores the top 16 bits of each doubleword in the\ndestination (first) operand;\n\n- `PMULLW` stores the bottom 16 bits of each doubleword in the\ndestination operand."},
    "pmuludq": {"title":"`PMULUDQ`: Multiply Packed Unsigned 32-bit Integers, and Store.","description":"`PMULUDQ` takes two packed unsigned 32-bit integer inputs, and\nmultiplies the values in the inputs, forming quadword results. The\nsource is either an unsigned doubleword in the low doubleword of a\n64-bit operand, or it's two unsigned doublewords in the first and\nthird doublewords of a 128-bit operand. This produces either one or\ntwo 64-bit results, which are stored in the respective quadword\nlocations of the destination register.\n\nThe operation is:\n\n```\n   dst[0-63]   := dst[0-31]  * src[0-31];\n   dst[64-127] := dst[64-95] * src[64-95].\n```"},
    "pmvgezb": {"title":"`PMVccZB`: MMX Packed Conditional Move","description":"These instructions, specific to the Cyrix MMX extensions, perform\nparallel conditional moves. The two input operands are treated as\nvectors of eight bytes. Each byte of the destination (first) operand\nis either written from the corresponding byte of the source (second)\noperand, or left alone, depending on the value of the byte in the\n*implied* operand (specified in the same way as `PADDSIW`, in\n`PADDSIW`).\n\n- `PMVZB` performs each move if the corresponding byte in the\nimplied operand is zero;\n\n- `PMVNZB` moves if the byte is non-zero;\n\n- `PMVLZB` moves if the byte is less than zero;\n\n- `PMVGEZB` moves if the byte is greater than or equal to zero.\n\nNote that these instructions cannot take a register as their second\nsource operand."},
    "pmvlzb": {"title":"`PMVccZB`: MMX Packed Conditional Move","description":"These instructions, specific to the Cyrix MMX extensions, perform\nparallel conditional moves. The two input operands are treated as\nvectors of eight bytes. Each byte of the destination (first) operand\nis either written from the corresponding byte of the source (second)\noperand, or left alone, depending on the value of the byte in the\n*implied* operand (specified in the same way as `PADDSIW`, in\n`PADDSIW`).\n\n- `PMVZB` performs each move if the corresponding byte in the\nimplied operand is zero;\n\n- `PMVNZB` moves if the byte is non-zero;\n\n- `PMVLZB` moves if the byte is less than zero;\n\n- `PMVGEZB` moves if the byte is greater than or equal to zero.\n\nNote that these instructions cannot take a register as their second\nsource operand."},
    "pmvzb": {"title":"`PMVccZB`: MMX Packed Conditional Move","description":"These instructions, specific to the Cyrix MMX extensions, perform\nparallel conditional moves. The two input operands are treated as\nvectors of eight bytes. Each byte of the destination (first) operand\nis either written from the corresponding byte of the source (second)\noperand, or left alone, depending on the value of the byte in the\n*implied* operand (specified in the same way as `PADDSIW`, in\n`PADDSIW`).\n\n- `PMVZB` performs each move if the corresponding byte in the\nimplied operand is zero;\n\n- `PMVNZB` moves if the byte is non-zero;\n\n- `PMVLZB` moves if the byte is less than zero;\n\n- `PMVGEZB` moves if the byte is greater than or equal to zero.\n\nNote that these instructions cannot take a register as their second\nsource operand."},
    "pop": {"title":"`POP`: Pop Data from Stack","description":"`POP` loads a value from the stack (from `[SS:SP]` or\n`[SS:ESP]`) and then increments the stack pointer.\n\nThe address-size attribute of the instruction determines whether\n`SP` or `ESP` is used as the stack pointer: to deliberately\noverride the default given by the `BITS` setting, you can use an\n`a16` or `a32` prefix.\n\nThe operand-size attribute of the instruction determines whether the\nstack pointer is incremented by 2 or 4: this means that segment\nregister pops in `BITS 32` mode will pop 4 bytes off the stack and\ndiscard the upper two of them. If you need to override that, you can\nuse an `o16` or `o32` prefix.\n\nThe above opcode listings give two forms for general-purpose\nregister pop instructions: for example, `POP BX` has the two forms\n`5B` and `8F C3`. NASM will always generate the shorter form\nwhen given `POP BX`. NDISASM will disassemble both.\n\n`POP CS` is not a documented instruction, and is not supported on\nany processor above the 8086 (since they use `0Fh` as an opcode\nprefix for instruction set extensions). However, at least some 8086\nprocessors do support it, and so NASM generates it for completeness."},
    "popa": {"title":"`POPAx`: Pop All General-Purpose Registers","description":"- `POPAW` pops a word from the stack into each of, successively,\n`DI`, `SI`, `BP`, nothing (it discards a word from the stack\nwhich was a placeholder for `SP`), `BX`, `DX`, `CX` and\n`AX`. It is intended to reverse the operation of `PUSHAW` (see\n`PUSHA`), but it ignores the value for `SP` that was pushed\non the stack by `PUSHAW`.\n\n- `POPAD` pops twice as much data, and places the results in\n`EDI`, `ESI`, `EBP`, nothing (placeholder for `ESP`),\n`EBX`, `EDX`, `ECX` and `EAX`. It reverses the operation of\n`PUSHAD`.\n\n`POPA` is an alias mnemonic for either `POPAW` or `POPAD`,\ndepending on the current `BITS` setting.\n\nNote that the registers are popped in reverse order of their numeric\nvalues in opcodes (see the register values)."},
    "popad": {"title":"`POPAx`: Pop All General-Purpose Registers","description":"- `POPAW` pops a word from the stack into each of, successively,\n`DI`, `SI`, `BP`, nothing (it discards a word from the stack\nwhich was a placeholder for `SP`), `BX`, `DX`, `CX` and\n`AX`. It is intended to reverse the operation of `PUSHAW` (see\n`PUSHA`), but it ignores the value for `SP` that was pushed\non the stack by `PUSHAW`.\n\n- `POPAD` pops twice as much data, and places the results in\n`EDI`, `ESI`, `EBP`, nothing (placeholder for `ESP`),\n`EBX`, `EDX`, `ECX` and `EAX`. It reverses the operation of\n`PUSHAD`.\n\n`POPA` is an alias mnemonic for either `POPAW` or `POPAD`,\ndepending on the current `BITS` setting.\n\nNote that the registers are popped in reverse order of their numeric\nvalues in opcodes (see the register values)."},
    "popaw": {"title":"`POPAx`: Pop All General-Purpose Registers","description":"- `POPAW` pops a word from the stack into each of, successively,\n`DI`, `SI`, `BP`, nothing (it discards a word from the stack\nwhich was a placeholder for `SP`), `BX`, `DX`, `CX` and\n`AX`. It is intended to reverse the operation of `PUSHAW` (see\n`PUSHA`), but it ignores the value for `SP` that was pushed\non the stack by `PUSHAW`.\n\n- `POPAD` pops twice as much data, and places the results in\n`EDI`, `ESI`, `EBP`, nothing (placeholder for `ESP`),\n`EBX`, `EDX`, `ECX` and `EAX`. It reverses the operation of\n`PUSHAD`.\n\n`POPA` is an alias mnemonic for either `POPAW` or `POPAD`,\ndepending on the current `BITS` setting.\n\nNote that the registers are popped in reverse order of their numeric\nvalues in opcodes (see the register values)."},
    "popf": {"title":"`POPFx`: Pop Flags Register","description":"- `POPFW` pops a word from the stack and stores it in the bottom 16\nbits of the flags register (or the whole flags register, on\nprocessors below a 386).\n\n- `POPFD` pops a doubleword and stores it in the entire flags register.\n\n`POPF` is an alias mnemonic for either `POPFW` or `POPFD`,\ndepending on the current `BITS` setting.\n\nSee also `PUSHF` (`PUSHF`)."},
    "popfd": {"title":"`POPFx`: Pop Flags Register","description":"- `POPFW` pops a word from the stack and stores it in the bottom 16\nbits of the flags register (or the whole flags register, on\nprocessors below a 386).\n\n- `POPFD` pops a doubleword and stores it in the entire flags register.\n\n`POPF` is an alias mnemonic for either `POPFW` or `POPFD`,\ndepending on the current `BITS` setting.\n\nSee also `PUSHF` (`PUSHF`)."},
    "popfq": {"title":"`POPFx`: Pop Flags Register","description":"- `POPFW` pops a word from the stack and stores it in the bottom 16\nbits of the flags register (or the whole flags register, on\nprocessors below a 386).\n\n- `POPFD` pops a doubleword and stores it in the entire flags register.\n\n`POPF` is an alias mnemonic for either `POPFW` or `POPFD`,\ndepending on the current `BITS` setting.\n\nSee also `PUSHF` (`PUSHF`)."},
    "popfw": {"title":"`POPFx`: Pop Flags Register","description":"- `POPFW` pops a word from the stack and stores it in the bottom 16\nbits of the flags register (or the whole flags register, on\nprocessors below a 386).\n\n- `POPFD` pops a doubleword and stores it in the entire flags register.\n\n`POPF` is an alias mnemonic for either `POPFW` or `POPFD`,\ndepending on the current `BITS` setting.\n\nSee also `PUSHF` (`PUSHF`)."},
    "por": {"title":"`POR`: MMX Bitwise OR","description":"`POR` performs a bitwise OR operation between its two operands\n(i.e. each bit of the result is 1 if and only if at least one of the\ncorresponding bits of the two inputs was 1), and stores the result\nin the destination (first) operand."},
    "prefetch": {"title":"`PREFETCH`: Prefetch Data Into Caches","description":"`PREFETCH` and `PREFETCHW` fetch the line of data from memory that\ncontains the specified byte. `PREFETCHW` performs differently on the\nAthlon to earlier processors.\n\nFor more details, see the 3DNow! Technology Manual."},
    "psadbw": {"title":"`PSADBW`: Packed Sum of Absolute Differences","description":"`PSADBW` The PSADBW instruction computes the absolute value of the\ndifference of the packed unsigned bytes in the two source operands.\nThese differences are then summed to produce a word result in the lower\n16-bit field of the destination register; the rest of the register is\ncleared. The destination operand is an `MMX` or an `XMM` register.\nThe source operand can either be a register or a memory operand."},
    "pshufd": {"title":"`PSHUFD`: Shuffle Packed Doublewords","description":"`PSHUFD` shuffles the doublewords in the source (second) operand\naccording to the encoding specified by imm8, and stores the result\nin the destination (first) operand.\n\nBits 0 and 1 of imm8 encode the source position of the doubleword to\nbe copied to position 0 in the destination operand. Bits 2 and 3\nencode for position 1, bits 4 and 5 encode for position 2, and bits\n6 and 7 encode for position 3. For example, an encoding of 10 in\nbits 0 and 1 of imm8 indicates that the doubleword at bits 64-95 of\nthe source operand will be copied to bits 0-31 of the destination."},
    "pshufhw": {"title":"`PSHUFHW`: Shuffle Packed High Words","description":"`PSHUFW` shuffles the words in the high quadword of the source\n(second) operand according to the encoding specified by imm8, and\nstores the result in the high quadword of the destination (first)\noperand.\n\nThe operation of this instruction is similar to the `PSHUFW`\ninstruction, except that the source and destination are the top\nquadword of a 128-bit operand, instead of being 64-bit operands.\nThe low quadword is copied from the source to the destination\nwithout any changes."},
    "pshuflw": {"title":"`PSHUFLW`: Shuffle Packed Low Words","description":"`PSHUFLW` shuffles the words in the low quadword of the source\n(second) operand according to the encoding specified by imm8, and\nstores the result in the low quadword of the destination (first)\noperand.\n\nThe operation of this instruction is similar to the `PSHUFW`\ninstruction, except that the source and destination are the low\nquadword of a 128-bit operand, instead of being 64-bit operands.\nThe high quadword is copied from the source to the destination\nwithout any changes."},
    "pshufw": {"title":"`PSHUFW`: Shuffle Packed Words","description":"`PSHUFW` shuffles the words in the source (second) operand\naccording to the encoding specified by imm8, and stores the result\nin the destination (first) operand.\n\nBits 0 and 1 of imm8 encode the source position of the word to be\ncopied to position 0 in the destination operand. Bits 2 and 3 encode\nfor position 1, bits 4 and 5 encode for position 2, and bits 6 and 7\nencode for position 3. For example, an encoding of 10 in bits 0 and 1\nof imm8 indicates that the word at bits 32-47 of the source operand\nwill be copied to bits 0-15 of the destination."},
    "pslld": {"title":"`PSLLx`: Packed Data Bit Shift Left Logical","description":"`PSLLx` performs logical left shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nleft by the number of bits specified in the source (second) operand,\nclearing the low-order bits as they are vacated. `PSLLDQ`\nshifts bytes, not bits.\n\n- `PSLLW` shifts word sized elements.\n\n- `PSLLD` shifts doubleword sized elements.\n\n- `PSLLQ` shifts quadword sized elements.\n\n- `PSLLDQ` shifts double quadword sized elements."},
    "psllq": {"title":"`PSLLx`: Packed Data Bit Shift Left Logical","description":"`PSLLx` performs logical left shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nleft by the number of bits specified in the source (second) operand,\nclearing the low-order bits as they are vacated. `PSLLDQ`\nshifts bytes, not bits.\n\n- `PSLLW` shifts word sized elements.\n\n- `PSLLD` shifts doubleword sized elements.\n\n- `PSLLQ` shifts quadword sized elements.\n\n- `PSLLDQ` shifts double quadword sized elements."},
    "psllw": {"title":"`PSLLx`: Packed Data Bit Shift Left Logical","description":"`PSLLx` performs logical left shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nleft by the number of bits specified in the source (second) operand,\nclearing the low-order bits as they are vacated. `PSLLDQ`\nshifts bytes, not bits.\n\n- `PSLLW` shifts word sized elements.\n\n- `PSLLD` shifts doubleword sized elements.\n\n- `PSLLQ` shifts quadword sized elements.\n\n- `PSLLDQ` shifts double quadword sized elements."},
    "psrad": {"title":"`PSRAx`: Packed Data Bit Shift Right Arithmetic","description":"`PSRAx` performs arithmetic right shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nright by the number of bits specified in the source (second) operand,\nsetting the high-order bits to the value of the original sign bit.\n\n- `PSRAW` shifts word sized elements.\n\n- `PSRAD` shifts doubleword sized elements."},
    "psraw": {"title":"`PSRAx`: Packed Data Bit Shift Right Arithmetic","description":"`PSRAx` performs arithmetic right shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nright by the number of bits specified in the source (second) operand,\nsetting the high-order bits to the value of the original sign bit.\n\n- `PSRAW` shifts word sized elements.\n\n- `PSRAD` shifts doubleword sized elements."},
    "psrld": {"title":"`PSRLx`: Packed Data Bit Shift Right Logical","description":"`PSRLx` performs logical right shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nright by the number of bits specified in the source (second) operand,\nclearing the high-order bits as they are vacated. `PSRLDQ`\nshifts bytes, not bits.\n\n- `PSRLW` shifts word sized elements.\n\n- `PSRLD` shifts doubleword sized elements.\n\n- `PSRLQ` shifts quadword sized elements.\n\n- `PSRLDQ` shifts double quadword sized elements."},
    "psrlq": {"title":"`PSRLx`: Packed Data Bit Shift Right Logical","description":"`PSRLx` performs logical right shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nright by the number of bits specified in the source (second) operand,\nclearing the high-order bits as they are vacated. `PSRLDQ`\nshifts bytes, not bits.\n\n- `PSRLW` shifts word sized elements.\n\n- `PSRLD` shifts doubleword sized elements.\n\n- `PSRLQ` shifts quadword sized elements.\n\n- `PSRLDQ` shifts double quadword sized elements."},
    "psrlw": {"title":"`PSRLx`: Packed Data Bit Shift Right Logical","description":"`PSRLx` performs logical right shifts of the data elements in the\ndestination (first) operand, moving each bit in the separate elements\nright by the number of bits specified in the source (second) operand,\nclearing the high-order bits as they are vacated. `PSRLDQ`\nshifts bytes, not bits.\n\n- `PSRLW` shifts word sized elements.\n\n- `PSRLD` shifts doubleword sized elements.\n\n- `PSRLQ` shifts quadword sized elements.\n\n- `PSRLDQ` shifts double quadword sized elements."},
    "psubb": {"title":"`PSUBx`: Subtract Packed Integers","description":"`PSUBx` subtracts packed integers in the source operand from those\nin the destination operand. It doesn't differentiate between signed\nand unsigned integers, and doesn't set any of the flags.\n\n- `PSUBB` operates on byte sized elements.\n\n- `PSUBW` operates on word sized elements.\n\n- `PSUBD` operates on doubleword sized elements.\n\n- `PSUBQ` operates on quadword sized elements."},
    "psubd": {"title":"`PSUBx`: Subtract Packed Integers","description":"`PSUBx` subtracts packed integers in the source operand from those\nin the destination operand. It doesn't differentiate between signed\nand unsigned integers, and doesn't set any of the flags.\n\n- `PSUBB` operates on byte sized elements.\n\n- `PSUBW` operates on word sized elements.\n\n- `PSUBD` operates on doubleword sized elements.\n\n- `PSUBQ` operates on quadword sized elements."},
    "psubq": {"title":"`PSUBx`: Subtract Packed Integers","description":"`PSUBx` subtracts packed integers in the source operand from those\nin the destination operand. It doesn't differentiate between signed\nand unsigned integers, and doesn't set any of the flags.\n\n- `PSUBB` operates on byte sized elements.\n\n- `PSUBW` operates on word sized elements.\n\n- `PSUBD` operates on doubleword sized elements.\n\n- `PSUBQ` operates on quadword sized elements."},
    "psubsiw": {"title":"`PSUBSIW`: MMX Packed Subtract with Saturation to Implied Destination","description":"`PSUBSIW`, specific to the Cyrix extensions to the MMX instruction\nset, performs the same function as `PSUBSW`, except that the\nresult is not placed in the register specified by the first operand,\nbut instead in the implied destination register, specified as for\n`PADDSIW` (`PADDSIW`)."},
    "psubusb": {"title":"`PSUBSxx`, `PSUBUSx`: Subtract Packed Integers With Saturation","description":"`PSUBSx` and `PSUBUSx` subtracts packed integers in the source\noperand from those in the destination operand, and use saturation for\nresults that are outside the range supported by the destination operand.\n\n- `PSUBSB` operates on signed bytes, and uses signed saturation on the\nresults.\n\n- `PSUBSW` operates on signed words, and uses signed saturation on the\nresults.\n\n- `PSUBUSB` operates on unsigned bytes, and uses signed saturation on\nthe results.\n\n- `PSUBUSW` operates on unsigned words, and uses signed saturation on\nthe results."},
    "psubusw": {"title":"`PSUBSxx`, `PSUBUSx`: Subtract Packed Integers With Saturation","description":"`PSUBSx` and `PSUBUSx` subtracts packed integers in the source\noperand from those in the destination operand, and use saturation for\nresults that are outside the range supported by the destination operand.\n\n- `PSUBSB` operates on signed bytes, and uses signed saturation on the\nresults.\n\n- `PSUBSW` operates on signed words, and uses signed saturation on the\nresults.\n\n- `PSUBUSB` operates on unsigned bytes, and uses signed saturation on\nthe results.\n\n- `PSUBUSW` operates on unsigned words, and uses signed saturation on\nthe results."},
    "psubw": {"title":"`PSUBx`: Subtract Packed Integers","description":"`PSUBx` subtracts packed integers in the source operand from those\nin the destination operand. It doesn't differentiate between signed\nand unsigned integers, and doesn't set any of the flags.\n\n- `PSUBB` operates on byte sized elements.\n\n- `PSUBW` operates on word sized elements.\n\n- `PSUBD` operates on doubleword sized elements.\n\n- `PSUBQ` operates on quadword sized elements."},
    "pswapd": {"title":"`PSWAPD`: Swap Packed Data","description":"`PSWAPD` swaps the packed doublewords in the source operand, and\nstores the result in the destination operand.\n\nIn the `K6-2` and `K6-III` processors, this opcode uses the\nmnemonic `PSWAPW`, and it swaps the order of words when copying\nfrom the source to the destination.\n\nThe operation in the `K6-2` and `K6-III` processors is\n\n```\n   dst[0-15]  = src[48-63];\n   dst[16-31] = src[32-47];\n   dst[32-47] = src[16-31];\n   dst[48-63] = src[0-15].\n```\n\nThe operation in the `K6-x+`, `ATHLON` and later processors is:\n\n```\n   dst[0-31]  = src[32-63];\n   dst[32-63] = src[0-31].\n```"},
    "push": {"title":"`PUSH`: Push Data on Stack","description":"`PUSH` decrements the stack pointer (`SP` or `ESP`) by 2 or 4,\nand then stores the given value at `[SS:SP]` or `[SS:ESP]`.\n\nThe address-size attribute of the instruction determines whether\n`SP` or `ESP` is used as the stack pointer: to deliberately\noverride the default given by the `BITS` setting, you can use an\n`a16` or `a32` prefix.\n\nThe operand-size attribute of the instruction determines whether the\nstack pointer is decremented by 2 or 4: this means that segment\nregister pushes in `BITS 32` mode will push 4 bytes on the stack,\nof which the upper two are undefined. If you need to override that,\nyou can use an `o16` or `o32` prefix.\n\nThe above opcode listings give two forms for general-purpose\nregister push instructions: for example, `PUSH BX` has the two\nforms `53` and `FF F3`. NASM will always generate the shorter\nform when given `PUSH BX`. NDISASM will disassemble both.\n\nUnlike the undocumented and barely supported `POP CS`, `PUSH CS`\nis a perfectly valid and sensible instruction, supported on all\nprocessors.\n\nThe instruction `PUSH SP` may be used to distinguish an 8086 from\nlater processors: on an 8086, the value of `SP` stored is the\nvalue it has *after* the push instruction, whereas on later\nprocessors it is the value *before* the push instruction."},
    "pusha": {"title":"`PUSHAx`: Push All General-Purpose Registers","description":"`PUSHAW` pushes, in succession, `AX`, `CX`, `DX`, `BX`,\n`SP`, `BP`, `SI` and `DI` on the stack, decrementing the\nstack pointer by a total of 16.\n\n`PUSHAD` pushes, in succession, `EAX`, `ECX`, `EDX`,\n`EBX`, `ESP`, `EBP`, `ESI` and `EDI` on the stack,\ndecrementing the stack pointer by a total of 32.\n\nIn both cases, the value of `SP` or `ESP` pushed is its\n*original* value, as it had before the instruction was executed.\n\n`PUSHA` is an alias mnemonic for either `PUSHAW` or `PUSHAD`,\ndepending on the current `BITS` setting.\n\nNote that the registers are pushed in order of their numeric values\nin opcodes (see the register values).\n\nSee also `POPA` (`POPA`)."},
    "pushad": {"title":"`PUSHAx`: Push All General-Purpose Registers","description":"`PUSHAW` pushes, in succession, `AX`, `CX`, `DX`, `BX`,\n`SP`, `BP`, `SI` and `DI` on the stack, decrementing the\nstack pointer by a total of 16.\n\n`PUSHAD` pushes, in succession, `EAX`, `ECX`, `EDX`,\n`EBX`, `ESP`, `EBP`, `ESI` and `EDI` on the stack,\ndecrementing the stack pointer by a total of 32.\n\nIn both cases, the value of `SP` or `ESP` pushed is its\n*original* value, as it had before the instruction was executed.\n\n`PUSHA` is an alias mnemonic for either `PUSHAW` or `PUSHAD`,\ndepending on the current `BITS` setting.\n\nNote that the registers are pushed in order of their numeric values\nin opcodes (see the register values).\n\nSee also `POPA` (`POPA`)."},
    "pushaw": {"title":"`PUSHAx`: Push All General-Purpose Registers","description":"`PUSHAW` pushes, in succession, `AX`, `CX`, `DX`, `BX`,\n`SP`, `BP`, `SI` and `DI` on the stack, decrementing the\nstack pointer by a total of 16.\n\n`PUSHAD` pushes, in succession, `EAX`, `ECX`, `EDX`,\n`EBX`, `ESP`, `EBP`, `ESI` and `EDI` on the stack,\ndecrementing the stack pointer by a total of 32.\n\nIn both cases, the value of `SP` or `ESP` pushed is its\n*original* value, as it had before the instruction was executed.\n\n`PUSHA` is an alias mnemonic for either `PUSHAW` or `PUSHAD`,\ndepending on the current `BITS` setting.\n\nNote that the registers are pushed in order of their numeric values\nin opcodes (see the register values).\n\nSee also `POPA` (`POPA`)."},
    "pushf": {"title":"`PUSHFx`: Push Flags Register","description":"- `PUSHFW` pushes the bottom 16 bits of the flags register\n(or the whole flags register, on processors below a 386) onto\nthe stack.\n\n- `PUSHFD` pushes the entire flags register onto the stack.\n\n`PUSHF` is an alias mnemonic for either `PUSHFW` or `PUSHFD`,\ndepending on the current `BITS` setting.\n\nSee also `POPF` (`POPF`)."},
    "pushfd": {"title":"`PUSHFx`: Push Flags Register","description":"- `PUSHFW` pushes the bottom 16 bits of the flags register\n(or the whole flags register, on processors below a 386) onto\nthe stack.\n\n- `PUSHFD` pushes the entire flags register onto the stack.\n\n`PUSHF` is an alias mnemonic for either `PUSHFW` or `PUSHFD`,\ndepending on the current `BITS` setting.\n\nSee also `POPF` (`POPF`)."},
    "pushfq": {"title":"`PUSHFx`: Push Flags Register","description":"- `PUSHFW` pushes the bottom 16 bits of the flags register\n(or the whole flags register, on processors below a 386) onto\nthe stack.\n\n- `PUSHFD` pushes the entire flags register onto the stack.\n\n`PUSHF` is an alias mnemonic for either `PUSHFW` or `PUSHFD`,\ndepending on the current `BITS` setting.\n\nSee also `POPF` (`POPF`)."},
    "pushfw": {"title":"`PUSHFx`: Push Flags Register","description":"- `PUSHFW` pushes the bottom 16 bits of the flags register\n(or the whole flags register, on processors below a 386) onto\nthe stack.\n\n- `PUSHFD` pushes the entire flags register onto the stack.\n\n`PUSHF` is an alias mnemonic for either `PUSHFW` or `PUSHFD`,\ndepending on the current `BITS` setting.\n\nSee also `POPF` (`POPF`)."},
    "pxor": {"title":"`PXOR`: MMX Bitwise XOR","description":"`PXOR` performs a bitwise XOR operation between its two operands\n(i.e. each bit of the result is 1 if and only if exactly one of the\ncorresponding bits of the two inputs was 1), and stores the result\nin the destination (first) operand."},
    "rcl": {"title":"`RCL`, `RCR`: Bitwise Rotate through Carry Bit","description":"`RCL` and `RCR` perform a 9-bit, 17-bit or 33-bit bitwise\nrotation operation, involving the given source/destination (first)\noperand and the carry bit. Thus, for example, in the operation\n`RCL AL,1`, a 9-bit rotation is performed in which `AL` is\nshifted left by 1, the top bit of `AL` moves into the carry flag,\nand the original value of the carry flag is placed in the low bit of\n`AL`.\n\nThe number of bits to rotate by is given by the second operand. Only\nthe bottom five bits of the rotation count are considered by\nprocessors above the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `RCL foo,1` by using a `BYTE` prefix: \\c{RCL\nfoo,BYTE 1}. Similarly with `RCR`."},
    "rcpps": {"title":"`RCPPS`: Packed Single-Precision FP Reciprocal","description":"`RCPPS` returns an approximation of the reciprocal of the packed\nsingle-precision FP values from xmm2/m128. The maximum error for this\napproximation is: |Error| <= 1.5 x 2^-12"},
    "rcpss": {"title":"`RCPSS`: Scalar Single-Precision FP Reciprocal","description":"`RCPSS` returns an approximation of the reciprocal of the lower\nsingle-precision FP value from xmm2/m32; the upper three fields are\npassed through from xmm1. The maximum error for this approximation is:\n|Error| <= 1.5 x 2^-12"},
    "rcr": {"title":"`RCL`, `RCR`: Bitwise Rotate through Carry Bit","description":"`RCL` and `RCR` perform a 9-bit, 17-bit or 33-bit bitwise\nrotation operation, involving the given source/destination (first)\noperand and the carry bit. Thus, for example, in the operation\n`RCL AL,1`, a 9-bit rotation is performed in which `AL` is\nshifted left by 1, the top bit of `AL` moves into the carry flag,\nand the original value of the carry flag is placed in the low bit of\n`AL`.\n\nThe number of bits to rotate by is given by the second operand. Only\nthe bottom five bits of the rotation count are considered by\nprocessors above the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `RCL foo,1` by using a `BYTE` prefix: \\c{RCL\nfoo,BYTE 1}. Similarly with `RCR`."},
    "rdmsr": {"title":"`RDMSR`: Read Model-Specific Registers","description":"`RDMSR` reads the processor Model-Specific Register (MSR) whose\nindex is stored in `ECX`, and stores the result in `EDX:EAX`.\nSee also `WRMSR` (`WRMSR`)."},
    "rdpmc": {"title":"`RDPMC`: Read Performance-Monitoring Counters","description":"`RDPMC` reads the processor performance-monitoring counter whose\nindex is stored in `ECX`, and stores the result in `EDX:EAX`.\n\nThis instruction is available on P6 and later processors and on MMX\nclass processors."},
    "rdshr": {"title":"`RDSHR`: Read SMM Header Pointer Register","description":"`RDSHR` reads the contents of the SMM header pointer register and\nsaves it to the destination operand, which can be either a 32 bit\nmemory location or a 32 bit register.\n\nSee also `WRSHR` (`WRSHR`)."},
    "rdtsc": {"title":"`RDTSC`: Read Time-Stamp Counter","description":"`RDTSC` reads the processor's time-stamp counter into `EDX:EAX`."},
    "ret": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retd": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retf": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retfd": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retfq": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retfw": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retn": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retnd": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retnq": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retnw": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retq": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "retw": {"title":"`RET`, `RETF`, `RETN`: Return from Procedure Call","description":"- `RET`, and its exact synonym `RETN`, pop `IP` or `EIP` from\nthe stack and transfer control to the new address. Optionally, if a\nnumeric second operand is provided, they increment the stack pointer\nby a further `imm16` bytes after popping the return address.\n\n- `RETF` executes a far return: after popping `IP`/`EIP`, it\nthen pops `CS`, and *then* increments the stack pointer by the\noptional argument if present."},
    "rol": {"title":"`ROL`, `ROR`: Bitwise Rotate","description":"`ROL` and `ROR` perform a bitwise rotation operation on the given\nsource/destination (first) operand. Thus, for example, in the\noperation `ROL AL,1`, an 8-bit rotation is performed in which\n`AL` is shifted left by 1 and the original top bit of `AL` moves\nround into the low bit.\n\nThe number of bits to rotate by is given by the second operand. Only\nthe bottom five bits of the rotation count are considered by processors\nabove the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `ROL foo,1` by using a `BYTE` prefix: \\c{ROL\nfoo,BYTE 1}. Similarly with `ROR`."},
    "ror": {"title":"`ROL`, `ROR`: Bitwise Rotate","description":"`ROL` and `ROR` perform a bitwise rotation operation on the given\nsource/destination (first) operand. Thus, for example, in the\noperation `ROL AL,1`, an 8-bit rotation is performed in which\n`AL` is shifted left by 1 and the original top bit of `AL` moves\nround into the low bit.\n\nThe number of bits to rotate by is given by the second operand. Only\nthe bottom five bits of the rotation count are considered by processors\nabove the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `ROL foo,1` by using a `BYTE` prefix: \\c{ROL\nfoo,BYTE 1}. Similarly with `ROR`."},
    "rsdc": {"title":"`RSDC`: Restore Segment Register and Descriptor","description":"`RSDC` restores a segment register (DS, ES, FS, GS, or SS) from mem80,\nand sets up its descriptor."},
    "rsldt": {"title":"`RSLDT`: Restore Segment Register and Descriptor","description":"`RSLDT` restores the Local Descriptor Table (LDTR) from mem80."},
    "rsm": {"title":"`RSM`: Resume from System-Management Mode","description":"`RSM` returns the processor to its normal operating mode when it\nwas in System-Management Mode."},
    "rsqrtps": {"title":"`RSQRTPS`: Packed Single-Precision FP Square Root Reciprocal","description":"`RSQRTPS` computes the approximate reciprocals of the square\nroots of the packed single-precision floating-point values in the\nsource and stores the results in xmm1. The maximum error for this\napproximation is: |Error| <= 1.5 x 2^-12"},
    "rsqrtss": {"title":"`RSQRTSS`: Scalar Single-Precision FP Square Root Reciprocal","description":"`RSQRTSS` returns an approximation of the reciprocal of the\nsquare root of the lowest order single-precision FP value from\nthe source, and stores it in the low doubleword of the destination\nregister. The upper three fields of xmm1 are preserved. The maximum\nerror for this approximation is: |Error| <= 1.5 x 2^-12"},
    "rsts": {"title":"`RSTS`: Restore TSR and Descriptor","description":"`RSTS` restores Task State Register (TSR) from mem80."},
    "sahf": {"title":"`SAHF`: Store AH to Flags","description":"`SAHF` sets the low byte of the flags word according to the\ncontents of the `AH` register.\n\nThe operation of `SAHF` is:\n\n```\n AH --> SF:ZF:0:AF:0:PF:1:CF\n```\n\nSee also `LAHF` (`LAHF`)."},
    "sal": {"title":"`SAL`, `SAR`: Bitwise Arithmetic Shifts","description":"`SAL` and `SAR` perform an arithmetic shift operation on the given\nsource/destination (first) operand. The vacated bits are filled with\nzero for `SAL`, and with copies of the original high bit of the\nsource operand for `SAR`.\n\n`SAL` is a synonym for `SHL` (see `SHL`). NASM will\nassemble either one to the same code, but NDISASM will always\ndisassemble that code as `SHL`.\n\nThe number of bits to shift by is given by the second operand. Only\nthe bottom five bits of the shift count are considered by processors\nabove the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `SAL foo,1` by using a `BYTE` prefix: \\c{SAL\nfoo,BYTE 1}. Similarly with `SAR`."},
    "salc": {"title":"`SALC`: Set AL from Carry Flag","description":"`SALC` is an early undocumented instruction similar in concept to\n`SETcc` (`SETcc`). Its function is to set `AL` to zero if\nthe carry flag is clear, or to `0xFF` if it is set."},
    "sar": {"title":"`SAL`, `SAR`: Bitwise Arithmetic Shifts","description":"`SAL` and `SAR` perform an arithmetic shift operation on the given\nsource/destination (first) operand. The vacated bits are filled with\nzero for `SAL`, and with copies of the original high bit of the\nsource operand for `SAR`.\n\n`SAL` is a synonym for `SHL` (see `SHL`). NASM will\nassemble either one to the same code, but NDISASM will always\ndisassemble that code as `SHL`.\n\nThe number of bits to shift by is given by the second operand. Only\nthe bottom five bits of the shift count are considered by processors\nabove the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `SAL foo,1` by using a `BYTE` prefix: \\c{SAL\nfoo,BYTE 1}. Similarly with `SAR`."},
    "sbb": {"title":"`SBB`: Subtract with Borrow","description":"`SBB` performs integer subtraction: it subtracts its second\noperand, plus the value of the carry flag, from its first, and\nleaves the result in its destination (first) operand. The flags are\nset according to the result of the operation: in particular, the\ncarry flag is affected and can be used by a subsequent `SBB`\ninstruction.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction.\n\nTo subtract one number from another without also subtracting the\ncontents of the carry flag, use `SUB` (`SUB`)."},
    "scasb": {"title":"`SCASB`, `SCASW`, `SCASD`: Scan String","description":"`SCASB` compares the byte in `AL` with the byte at `[ES:DI]`\nor `[ES:EDI]`, and sets the flags accordingly. It then increments\nor decrements (depending on the direction flag: increments if the\nflag is clear, decrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`SCASW` and `SCASD` work in the same way, but they compare a\nword to `AX` or a doubleword to `EAX` instead of a byte to\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "scasd": {"title":"`SCASB`, `SCASW`, `SCASD`: Scan String","description":"`SCASB` compares the byte in `AL` with the byte at `[ES:DI]`\nor `[ES:EDI]`, and sets the flags accordingly. It then increments\nor decrements (depending on the direction flag: increments if the\nflag is clear, decrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`SCASW` and `SCASD` work in the same way, but they compare a\nword to `AX` or a doubleword to `EAX` instead of a byte to\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "scasq": {"title":"`SCASB`, `SCASW`, `SCASD`: Scan String","description":"`SCASB` compares the byte in `AL` with the byte at `[ES:DI]`\nor `[ES:EDI]`, and sets the flags accordingly. It then increments\nor decrements (depending on the direction flag: increments if the\nflag is clear, decrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`SCASW` and `SCASD` work in the same way, but they compare a\nword to `AX` or a doubleword to `EAX` instead of a byte to\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "scasw": {"title":"`SCASB`, `SCASW`, `SCASD`: Scan String","description":"`SCASB` compares the byte in `AL` with the byte at `[ES:DI]`\nor `[ES:EDI]`, and sets the flags accordingly. It then increments\nor decrements (depending on the direction flag: increments if the\nflag is clear, decrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the load from `[DI]` or `[EDI]` cannot be\noverridden.\n\n`SCASW` and `SCASD` work in the same way, but they compare a\nword to `AX` or a doubleword to `EAX` instead of a byte to\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REPE` and `REPNE` prefixes (equivalently, `REPZ` and\n`REPNZ`) may be used to repeat the instruction up to `CX` (or\n`ECX` - again, the address size chooses which) times until the\nfirst unequal or equal byte is found."},
    "seta": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setae": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setb": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setbe": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setc": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "sete": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setg": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setge": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setl": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setle": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setna": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnae": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnb": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnbe": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnc": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setne": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setng": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnge": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnl": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnle": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setno": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnp": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setns": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setnz": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "seto": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setp": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setpe": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setpo": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "sets": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "setz": {"title":"`SETcc`: Set Register from Condition","description":"`SETcc` sets the given 8-bit operand to zero if its condition is\nnot satisfied, and to 1 if it is."},
    "sfence": {"title":"`SFENCE`: Store Fence","description":"`SFENCE` performs a serialising operation on all writes to memory\nthat were issued before the `SFENCE` instruction. This guarantees that\nall memory writes before the `SFENCE` instruction are visible before any\nwrites after the `SFENCE` instruction.\n\n`SFENCE` is ordered respective to other `SFENCE` instruction, `MFENCE`,\nany memory write and any other serialising instruction (such as `CPUID`).\n\nWeakly ordered memory types can be used to achieve higher processor\nperformance through such techniques as out-of-order issue,\nwrite-combining, and write-collapsing. The degree to which a consumer\nof data recognizes or knows that the data is weakly ordered varies\namong applications and may be unknown to the producer of this data.\nThe `SFENCE` instruction provides a performance-efficient way of\ninsuring store ordering between routines that produce weakly-ordered\nresults and routines that consume this data.\n\n`SFENCE` uses the following ModRM encoding:\n\n```\n          Mod (7:6)        = 11B\n          Reg/Opcode (5:3) = 111B\n          R/M (2:0)        = 000B\n```\n\nAll other ModRM encodings are defined to be reserved, and use\nof these encodings risks incompatibility with future processors.\n\nSee also `LFENCE` (`LFENCE`) and `MFENCE` (`MFENCE`)."},
    "sgdt": {"title":"`SGDT`, `SIDT`, `SLDT`: Store Descriptor Table Pointers","description":"`SGDT` and `SIDT` both take a 6-byte memory area as an operand:\nthey store the contents of the GDTR (global descriptor table\nregister) or IDTR (interrupt descriptor table register) into that\narea as a 32-bit linear address and a 16-bit size limit from that\narea (in that order). These are the only instructions which directly\nuse *linear* addresses, rather than segment/offset pairs.\n\n`SLDT` stores the segment selector corresponding to the LDT (local\ndescriptor table) into the given operand.\n\nSee also `LGDT`, `LIDT` and `LLDT` (`LGDT`)."},
    "shl": {"title":"`SHL`, `SHR`: Bitwise Logical Shifts","description":"`SHL` and `SHR` perform a logical shift operation on the given\nsource/destination (first) operand. The vacated bits are filled with\nzero.\n\nA synonym for `SHL` is `SAL` (see `SAL`). NASM will\nassemble either one to the same code, but NDISASM will always\ndisassemble that code as `SHL`.\n\nThe number of bits to shift by is given by the second operand. Only\nthe bottom five bits of the shift count are considered by processors\nabove the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `SHL foo,1` by using a `BYTE` prefix: \\c{SHL\nfoo,BYTE 1}. Similarly with `SHR`."},
    "shld": {"title":"`SHLD`, `SHRD`: Bitwise Double-Precision Shifts","description":"- `SHLD` performs a double-precision left shift. It notionally\nplaces its second operand to the right of its first, then shifts\nthe entire bit string thus generated to the left by a number of\nbits specified in the third operand. It then updates only the\n*first* operand according to the result of this. The second\noperand is not modified.\n\n- `SHRD` performs the corresponding right shift: it notionally\nplaces the second operand to the *left* of the first, shifts the\nwhole bit string right, and updates only the first operand.\n\nFor example, if `EAX` holds `0x01234567` and `EBX` holds\n`0x89ABCDEF`, then the instruction `SHLD EAX,EBX,4` would update\n`EAX` to hold `0x12345678`. Under the same conditions, \\c{SHRD\nEAX,EBX,4} would update `EAX` to hold `0xF0123456`.\n\nThe number of bits to shift by is given by the third operand. Only\nthe bottom five bits of the shift count are considered."},
    "shr": {"title":"`SHL`, `SHR`: Bitwise Logical Shifts","description":"`SHL` and `SHR` perform a logical shift operation on the given\nsource/destination (first) operand. The vacated bits are filled with\nzero.\n\nA synonym for `SHL` is `SAL` (see `SAL`). NASM will\nassemble either one to the same code, but NDISASM will always\ndisassemble that code as `SHL`.\n\nThe number of bits to shift by is given by the second operand. Only\nthe bottom five bits of the shift count are considered by processors\nabove the 8086.\n\nYou can force the longer (286 and upwards, beginning with a `C1`\nbyte) form of `SHL foo,1` by using a `BYTE` prefix: \\c{SHL\nfoo,BYTE 1}. Similarly with `SHR`."},
    "shrd": {"title":"`SHLD`, `SHRD`: Bitwise Double-Precision Shifts","description":"- `SHLD` performs a double-precision left shift. It notionally\nplaces its second operand to the right of its first, then shifts\nthe entire bit string thus generated to the left by a number of\nbits specified in the third operand. It then updates only the\n*first* operand according to the result of this. The second\noperand is not modified.\n\n- `SHRD` performs the corresponding right shift: it notionally\nplaces the second operand to the *left* of the first, shifts the\nwhole bit string right, and updates only the first operand.\n\nFor example, if `EAX` holds `0x01234567` and `EBX` holds\n`0x89ABCDEF`, then the instruction `SHLD EAX,EBX,4` would update\n`EAX` to hold `0x12345678`. Under the same conditions, \\c{SHRD\nEAX,EBX,4} would update `EAX` to hold `0xF0123456`.\n\nThe number of bits to shift by is given by the third operand. Only\nthe bottom five bits of the shift count are considered."},
    "shufpd": {"title":"`SHUFPD`: Shuffle Packed Double-Precision FP Values","description":"`SHUFPD` moves one of the packed double-precision FP values from\nthe destination operand into the low quadword of the destination\noperand; the upper quadword is generated by moving one of the\ndouble-precision FP values from the source operand into the\ndestination. The select (third) operand selects which of the values\nare moved to the destination register.\n\nThe select operand is an 8-bit immediate: bit 0 selects which value\nis moved from the destination operand to the result (where 0 selects\nthe low quadword and 1 selects the high quadword) and bit 1 selects\nwhich value is moved from the source operand to the result.\nBits 2 through 7 of the shuffle operand are reserved."},
    "shufps": {"title":"`SHUFPS`: Shuffle Packed Single-Precision FP Values","description":"`SHUFPS` moves two of the packed single-precision FP values from\nthe destination operand into the low quadword of the destination\noperand; the upper quadword is generated by moving two of the\nsingle-precision FP values from the source operand into the\ndestination. The select (third) operand selects which of the\nvalues are moved to the destination register.\n\nThe select operand is an 8-bit immediate: bits 0 and 1 select the\nvalue to be moved from the destination operand the low doubleword of\nthe result, bits 2 and 3 select the value to be moved from the\ndestination operand the second doubleword of the result, bits 4 and\n5 select the value to be moved from the source operand the third\ndoubleword of the result, and bits 6 and 7 select the value to be\nmoved from the source operand to the high doubleword of the result."},
    "sidt": {"title":"`SGDT`, `SIDT`, `SLDT`: Store Descriptor Table Pointers","description":"`SGDT` and `SIDT` both take a 6-byte memory area as an operand:\nthey store the contents of the GDTR (global descriptor table\nregister) or IDTR (interrupt descriptor table register) into that\narea as a 32-bit linear address and a 16-bit size limit from that\narea (in that order). These are the only instructions which directly\nuse *linear* addresses, rather than segment/offset pairs.\n\n`SLDT` stores the segment selector corresponding to the LDT (local\ndescriptor table) into the given operand.\n\nSee also `LGDT`, `LIDT` and `LLDT` (`LGDT`)."},
    "sldt": {"title":"`SGDT`, `SIDT`, `SLDT`: Store Descriptor Table Pointers","description":"`SGDT` and `SIDT` both take a 6-byte memory area as an operand:\nthey store the contents of the GDTR (global descriptor table\nregister) or IDTR (interrupt descriptor table register) into that\narea as a 32-bit linear address and a 16-bit size limit from that\narea (in that order). These are the only instructions which directly\nuse *linear* addresses, rather than segment/offset pairs.\n\n`SLDT` stores the segment selector corresponding to the LDT (local\ndescriptor table) into the given operand.\n\nSee also `LGDT`, `LIDT` and `LLDT` (`LGDT`)."},
    "smint": {"title":"`SMINT`, `SMINTOLD`: Software SMM Entry (CYRIX)","description":"`SMINT` puts the processor into SMM mode. The CPU state information is\nsaved in the SMM memory header, and then execution begins at the SMM base\naddress.\n\n`SMINTOLD` is the same as `SMINT`, but was the opcode used on the 486.\n\nThis pair of opcodes are specific to the Cyrix and compatible range of\nprocessors (Cyrix, IBM, Via)."},
    "smintold": {"title":"`SMINT`, `SMINTOLD`: Software SMM Entry (CYRIX)","description":"`SMINT` puts the processor into SMM mode. The CPU state information is\nsaved in the SMM memory header, and then execution begins at the SMM base\naddress.\n\n`SMINTOLD` is the same as `SMINT`, but was the opcode used on the 486.\n\nThis pair of opcodes are specific to the Cyrix and compatible range of\nprocessors (Cyrix, IBM, Via)."},
    "smsw": {"title":"`SMSW`: Store Machine Status Word","description":"`SMSW` stores the bottom half of the `CR0` control register (or\nthe Machine Status Word, on 286 processors) into the destination\noperand. See also `LMSW` (`LMSW`).\n\nFor 32-bit code, this would store all of `CR0` in the specified\nregister (or the bottom 16 bits if the destination is a memory location),\nwithout needing an operand size override byte."},
    "sqrtpd": {"title":"`SQRTPD`: Packed Double-Precision FP Square Root","description":"`SQRTPD` calculates the square root of the packed double-precision\nFP value from the source operand, and stores the double-precision\nresults in the destination register."},
    "sqrtps": {"title":"`SQRTPS`: Packed Single-Precision FP Square Root","description":"`SQRTPS` calculates the square root of the packed single-precision\nFP value from the source operand, and stores the single-precision\nresults in the destination register."},
    "sqrtsd": {"title":"`SQRTSD`: Scalar Double-Precision FP Square Root","description":"`SQRTSD` calculates the square root of the low-order double-precision\nFP value from the source operand, and stores the double-precision\nresult in the destination register. The high-quadword remains unchanged."},
    "sqrtss": {"title":"`SQRTSS`: Scalar Single-Precision FP Square Root","description":"`SQRTSS` calculates the square root of the low-order single-precision\nFP value from the source operand, and stores the single-precision\nresult in the destination register. The three high doublewords remain\nunchanged."},
    "stc": {"title":"`STC`, `STD`, `STI`: Set Flags","description":"These instructions set various flags. `STC` sets the carry flag;\n`STD` sets the direction flag; and `STI` sets the interrupt flag\n(thus enabling interrupts).\n\nTo clear the carry, direction, or interrupt flags, use the `CLC`,\n`CLD` and `CLI` instructions (`CLC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "std": {"title":"`STC`, `STD`, `STI`: Set Flags","description":"These instructions set various flags. `STC` sets the carry flag;\n`STD` sets the direction flag; and `STI` sets the interrupt flag\n(thus enabling interrupts).\n\nTo clear the carry, direction, or interrupt flags, use the `CLC`,\n`CLD` and `CLI` instructions (`CLC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "sti": {"title":"`STC`, `STD`, `STI`: Set Flags","description":"These instructions set various flags. `STC` sets the carry flag;\n`STD` sets the direction flag; and `STI` sets the interrupt flag\n(thus enabling interrupts).\n\nTo clear the carry, direction, or interrupt flags, use the `CLC`,\n`CLD` and `CLI` instructions (`CLC`). To invert the carry\nflag, use `CMC` (`CMC`)."},
    "stmxcsr": {"title":"`STMXCSR`: Store Streaming SIMD Extension Control/Status","description":"`STMXCSR` stores the contents of the `MXCSR` control/status\nregister to the specified memory location. `MXCSR` is used to\nenable masked/unmasked exception handling, to set rounding modes,\nto set flush-to-zero mode, and to view exception status flags.\nThe reserved bits in the `MXCSR` register are stored as 0s.\n\nFor details of the `MXCSR` register, see the Intel processor docs.\n\nSee also `LDMXCSR` (`LDMXCSR`)."},
    "stosb": {"title":"`STOSB`, `STOSW`, `STOSD`: Store Byte to String","description":"`STOSB` stores the byte in `AL` at `[ES:DI]` or `[ES:EDI]`,\nand sets the flags accordingly. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the store to `[DI]` or `[EDI]` cannot be\noverridden.\n\n`STOSW` and `STOSD` work in the same way, but they store the\nword in `AX` or the doubleword in `EAX` instead of the byte in\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "stosd": {"title":"`STOSB`, `STOSW`, `STOSD`: Store Byte to String","description":"`STOSB` stores the byte in `AL` at `[ES:DI]` or `[ES:EDI]`,\nand sets the flags accordingly. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the store to `[DI]` or `[EDI]` cannot be\noverridden.\n\n`STOSW` and `STOSD` work in the same way, but they store the\nword in `AX` or the doubleword in `EAX` instead of the byte in\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "stosq": {"title":"`STOSB`, `STOSW`, `STOSD`: Store Byte to String","description":"`STOSB` stores the byte in `AL` at `[ES:DI]` or `[ES:EDI]`,\nand sets the flags accordingly. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the store to `[DI]` or `[EDI]` cannot be\noverridden.\n\n`STOSW` and `STOSD` work in the same way, but they store the\nword in `AX` or the doubleword in `EAX` instead of the byte in\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "stosw": {"title":"`STOSB`, `STOSW`, `STOSD`: Store Byte to String","description":"`STOSB` stores the byte in `AL` at `[ES:DI]` or `[ES:EDI]`,\nand sets the flags accordingly. It then increments or decrements\n(depending on the direction flag: increments if the flag is clear,\ndecrements if it is set) `DI` (or `EDI`).\n\nThe register used is `DI` if the address size is 16 bits, and\n`EDI` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nSegment override prefixes have no effect for this instruction: the\nuse of `ES` for the store to `[DI]` or `[EDI]` cannot be\noverridden.\n\n`STOSW` and `STOSD` work in the same way, but they store the\nword in `AX` or the doubleword in `EAX` instead of the byte in\n`AL`, and increment or decrement the addressing registers by 2 or\n4 instead of 1.\n\nThe `REP` prefix may be used to repeat the instruction `CX` (or\n`ECX` - again, the address size chooses which) times."},
    "str": {"title":"`STR`: Store Task Register","description":"`STR` stores the segment selector corresponding to the contents of\nthe Task Register into its operand. When the operand size is 32 bit and\nthe destination is a register, the upper 16-bits are cleared to 0s.\nWhen the destination operand is a memory location, 16 bits are\nwritten regardless of the operand size."},
    "sub": {"title":"`SUB`: Subtract Integers","description":"`SUB` performs integer subtraction: it subtracts its second\noperand from its first, and leaves the result in its destination\n(first) operand. The flags are set according to the result of the\noperation: in particular, the carry flag is affected and can be used\nby a subsequent `SBB` instruction (`SBB`).\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction."},
    "subpd": {"title":"`SUBPD`: Packed Double-Precision FP Subtract","description":"`SUBPD` subtracts the packed double-precision FP values of\nthe source operand from those of the destination operand, and\nstores the result in the destination operation."},
    "subps": {"title":"`SUBPS`: Packed Single-Precision FP Subtract","description":"`SUBPS` subtracts the packed single-precision FP values of\nthe source operand from those of the destination operand, and\nstores the result in the destination operation."},
    "subsd": {"title":"`SUBSD`: Scalar Single-FP Subtract","description":"`SUBSD` subtracts the low-order double-precision FP value of\nthe source operand from that of the destination operand, and\nstores the result in the destination operation. The high\nquadword is unchanged."},
    "subss": {"title":"`SUBSS`: Scalar Single-FP Subtract","description":"`SUBSS` subtracts the low-order single-precision FP value of\nthe source operand from that of the destination operand, and\nstores the result in the destination operation. The three high\ndoublewords are unchanged."},
    "svdc": {"title":"`SVDC`: Save Segment Register and Descriptor","description":"`SVDC` saves a segment register (DS, ES, FS, GS, or SS) and its\ndescriptor to mem80."},
    "svldt": {"title":"`SVLDT`: Save LDTR and Descriptor","description":"`SVLDT` saves the Local Descriptor Table (LDTR) to mem80."},
    "svts": {"title":"`SVTS`: Save TSR and Descriptor","description":"`SVTS` saves the Task State Register (TSR) to mem80."},
    "syscall": {"title":"`SYSCALL`: Call Operating System","description":"`SYSCALL` provides a fast method of transferring control to a fixed\nentry point in an operating system.\n\n- The `EIP` register is copied into the `ECX` register.\n\n- Bits [31-0] of the 64-bit SYSCALL/SYSRET Target Address Register\n(`STAR`) are copied into the `EIP` register.\n\n- Bits [47-32] of the `STAR` register specify the selector that is\ncopied into the `CS` register.\n\n- Bits [47-32]+1000b of the `STAR` register specify the selector that\nis copied into the SS register.\n\nThe `CS` and `SS` registers should not be modified by the operating\nsystem between the execution of the `SYSCALL` instruction and its\ncorresponding `SYSRET` instruction.\n\nFor more information, see the `SYSCALL and SYSRET Instruction Specification`\n(AMD document number 21086.pdf)."},
    "sysenter": {"title":"`SYSENTER`: Fast System Call","description":"`SYSENTER` executes a fast call to a level 0 system procedure or\nroutine. Before using this instruction, various MSRs need to be set\nup:\n\n- `SYSENTER_CS_MSR` contains the 32-bit segment selector for the\nprivilege level 0 code segment. (This value is also used to compute\nthe segment selector of the privilege level 0 stack segment.)\n\n- `SYSENTER_EIP_MSR` contains the 32-bit offset into the privilege\nlevel 0 code segment to the first instruction of the selected operating\nprocedure or routine.\n\n- `SYSENTER_ESP_MSR` contains the 32-bit stack pointer for the\nprivilege level 0 stack.\n\n`SYSENTER` performs the following sequence of operations:\n\n- Loads the segment selector from the `SYSENTER_CS_MSR` into the\n`CS` register.\n\n- Loads the instruction pointer from the `SYSENTER_EIP_MSR` into\nthe `EIP` register.\n\n- Adds 8 to the value in `SYSENTER_CS_MSR` and loads it into the\n`SS` register.\n\n- Loads the stack pointer from the `SYSENTER_ESP_MSR` into the\n`ESP` register.\n\n- Switches to privilege level 0.\n\n- Clears the `VM` flag in the `EFLAGS` register, if the flag\nis set.\n\n- Begins executing the selected system procedure.\n\nIn particular, note that this instruction des not save the values of\n`CS` or `(E)IP`. If you need to return to the calling code, you\nneed to write your code to cater for this.\n\nFor more information, see the Intel Architecture Software Developer's\nManual, Volume 2."},
    "sysexit": {"title":"`SYSEXIT`: Fast Return From System Call","description":"`SYSEXIT` executes a fast return to privilege level 3 user code.\nThis instruction is a companion instruction to the `SYSENTER`\ninstruction, and can only be executed by privilege level 0 code.\nVarious registers need to be set up before calling this instruction:\n\n- `SYSENTER_CS_MSR` contains the 32-bit segment selector for the\nprivilege level 0 code segment in which the processor is currently\nexecuting. (This value is used to compute the segment selectors for\nthe privilege level 3 code and stack segments.)\n\n- `EDX` contains the 32-bit offset into the privilege level 3 code\nsegment to the first instruction to be executed in the user code.\n\n- `ECX` contains the 32-bit stack pointer for the privilege level 3\nstack.\n\n`SYSEXIT` performs the following sequence of operations:\n\n- Adds 16 to the value in `SYSENTER_CS_MSR` and loads the sum into\nthe `CS` selector register.\n\n- Loads the instruction pointer from the `EDX` register into the\n`EIP` register.\n\n- Adds 24 to the value in `SYSENTER_CS_MSR` and loads the sum\ninto the `SS` selector register.\n\n- Loads the stack pointer from the `ECX` register into the `ESP`\nregister.\n\n- Switches to privilege level 3.\n\n- Begins executing the user code at the `EIP` address.\n\nFor more information on the use of the `SYSENTER` and `SYSEXIT`\ninstructions, see the Intel Architecture Software Developer's\nManual, Volume 2."},
    "sysret": {"title":"`SYSRET`: Return From Operating System","description":"`SYSRET` is the return instruction used in conjunction with the\n`SYSCALL` instruction to provide fast entry/exit to an operating system.\n\n- The `ECX` register, which points to the next sequential instruction\nafter the corresponding `SYSCALL` instruction, is copied into the `EIP`\nregister.\n\n- Bits [63-48] of the `STAR` register specify the selector that is copied\ninto the `CS` register.\n\n- Bits [63-48]+1000b of the `STAR` register specify the selector that is\ncopied into the `SS` register.\n\n- Bits [1-0] of the `SS` register are set to 11b (RPL of 3) regardless of\nthe value of bits [49-48] of the `STAR` register.\n\nThe `CS` and `SS` registers should not be modified by the operating\nsystem between the execution of the `SYSCALL` instruction and its\ncorresponding `SYSRET` instruction.\n\nFor more information, see the `SYSCALL and SYSRET Instruction Specification`\n(AMD document number 21086.pdf)."},
    "test": {"title":"`TEST`: Test Bits (notional bitwise AND)","description":"`TEST` performs a 'mental' bitwise AND of its two operands, and\naffects the flags as if the operation had taken place, but does not\nstore the result of the operation anywhere."},
    "ucomisd": {"title":"`UCOMISD`: Unordered Scalar Double-Precision FP compare and set EFLAGS","description":"`UCOMISD` compares the low-order double-precision FP numbers in the\ntwo operands, and sets the `ZF`, `PF` and `CF` bits in the\n`EFLAGS` register. In addition, the `OF`, `SF` and `AF` bits\nin the `EFLAGS` register are zeroed out. The unordered predicate\n(`ZF`, `PF` and `CF` all set) is returned if either source\noperand is a `NaN` (`qNaN` or `sNaN`)."},
    "ucomiss": {"title":"`UCOMISS`: Unordered Scalar Single-Precision FP compare and set EFLAGS","description":"`UCOMISS` compares the low-order single-precision FP numbers in the\ntwo operands, and sets the `ZF`, `PF` and `CF` bits in the\n`EFLAGS` register. In addition, the `OF`, `SF` and `AF` bits\nin the `EFLAGS` register are zeroed out. The unordered predicate\n(`ZF`, `PF` and `CF` all set) is returned if either source\noperand is a `NaN` (`qNaN` or `sNaN`)."},
    "ud0": {"title":"`UD0`, `UD1`, `UD2`: Undefined Instruction","description":"`UDx` can be used to generate an invalid opcode exception, for testing\npurposes.\n\n`UD0` is specifically documented by AMD as being reserved for this\npurpose.\n\n`UD1` is documented by Intel as being available for this purpose.\n\n`UD2` is specifically documented by Intel as being reserved for this\npurpose. Intel document this as the preferred method of generating an\ninvalid opcode exception.\n\nAll these opcodes can be used to generate invalid opcode exceptions on\nall currently available processors."},
    "ud1": {"title":"`UD0`, `UD1`, `UD2`: Undefined Instruction","description":"`UDx` can be used to generate an invalid opcode exception, for testing\npurposes.\n\n`UD0` is specifically documented by AMD as being reserved for this\npurpose.\n\n`UD1` is documented by Intel as being available for this purpose.\n\n`UD2` is specifically documented by Intel as being reserved for this\npurpose. Intel document this as the preferred method of generating an\ninvalid opcode exception.\n\nAll these opcodes can be used to generate invalid opcode exceptions on\nall currently available processors."},
    "ud2": {"title":"`UD0`, `UD1`, `UD2`: Undefined Instruction","description":"`UDx` can be used to generate an invalid opcode exception, for testing\npurposes.\n\n`UD0` is specifically documented by AMD as being reserved for this\npurpose.\n\n`UD1` is documented by Intel as being available for this purpose.\n\n`UD2` is specifically documented by Intel as being reserved for this\npurpose. Intel document this as the preferred method of generating an\ninvalid opcode exception.\n\nAll these opcodes can be used to generate invalid opcode exceptions on\nall currently available processors."},
    "umov": {"title":"`UMOV`: User Move Data","description":"This undocumented instruction is used by in-circuit emulators to\naccess user memory (as opposed to host memory). It is used just like\nan ordinary memory/register or register/register `MOV`\ninstruction, but accesses user space.\n\nThis instruction is only available on some AMD and IBM 386 and 486\nprocessors."},
    "unpckhpd": {"title":"`UNPCKHPD`: Unpack and Interleave High Packed Double-Precision FP Values","description":"`UNPCKHPD` performs an interleaved unpack of the high-order data\nelements of the source and destination operands, saving the result\nin `xmm1`. It ignores the lower half of the sources.\n\nThe operation of this instruction is:\n\n```\n   dst[63-0]   := dst[127-64];\n   dst[127-64] := src[127-64].\n```"},
    "unpckhps": {"title":"`UNPCKHPS`: Unpack and Interleave High Packed Single-Precision FP Values","description":"`UNPCKHPS` performs an interleaved unpack of the high-order data\nelements of the source and destination operands, saving the result\nin `xmm1`. It ignores the lower half of the sources.\n\nThe operation of this instruction is:\n\n```\n   dst[31-0]   := dst[95-64];\n   dst[63-32]  := src[95-64];\n   dst[95-64]  := dst[127-96];\n   dst[127-96] := src[127-96].\n```"},
    "unpcklpd": {"title":"`UNPCKLPD`: Unpack and Interleave Low Packed Double-Precision FP Data","description":"`UNPCKLPD` performs an interleaved unpack of the low-order data\nelements of the source and destination operands, saving the result\nin `xmm1`. It ignores the lower half of the sources.\n\nThe operation of this instruction is:\n\n```\n   dst[63-0]   := dst[63-0];\n   dst[127-64] := src[63-0].\n```"},
    "unpcklps": {"title":"`UNPCKLPS`: Unpack and Interleave Low Packed Single-Precision FP Data","description":"`UNPCKLPS` performs an interleaved unpack of the low-order data\nelements of the source and destination operands, saving the result\nin `xmm1`. It ignores the lower half of the sources.\n\nThe operation of this instruction is:\n\n```\n   dst[31-0]   := dst[31-0];\n   dst[63-32]  := src[31-0];\n   dst[95-64]  := dst[63-32];\n   dst[127-96] := src[63-32].\n```"},
    "verr": {"title":"`VERR`, `VERW`: Verify Segment Readability/Writability","description":"- `VERR` sets the zero flag if the segment specified by the selector\nin its operand can be read from at the current privilege level.\nOtherwise it is cleared.\n\n- `VERW` sets the zero flag if the segment can be written."},
    "verw": {"title":"`VERR`, `VERW`: Verify Segment Readability/Writability","description":"- `VERR` sets the zero flag if the segment specified by the selector\nin its operand can be read from at the current privilege level.\nOtherwise it is cleared.\n\n- `VERW` sets the zero flag if the segment can be written."},
    "wbinvd": {"title":"`WBINVD`: Write Back and Invalidate Cache","description":"`WBINVD` invalidates and empties the processor's internal caches,\nand causes the processor to instruct external caches to do the same.\nIt writes the contents of the caches back to memory first, so no\ndata is lost. To flush the caches quickly without bothering to write\nthe data back first, use `INVD` (`INVD`)."},
    "wrmsr": {"title":"`WRMSR`: Write Model-Specific Registers","description":"`WRMSR` writes the value in `EDX:EAX` to the processor\nModel-Specific Register (MSR) whose index is stored in `ECX`.\nSee also `RDMSR` (`RDMSR`)."},
    "wrshr": {"title":"`WRSHR`: Write SMM Header Pointer Register","description":"`WRSHR` loads the contents of either a 32-bit memory location or a\n32-bit register into the SMM header pointer register.\n\nSee also `RDSHR` (`RDSHR`)."},
    "xadd": {"title":"`XADD`: Exchange and Add","description":"`XADD` exchanges the values in its two operands, and then adds\nthem together and writes the result into the destination (first)\noperand. This instruction can be used with a `LOCK` prefix for\nmulti-processor synchronisation purposes."},
    "xbts": {"title":"`XBTS`: Extract Bit String","description":"The implied operation of this instruction is:\n\n```\nXBTS r/m16,reg16,AX,CL\nXBTS r/m32,reg32,EAX,CL\n```\n\nWrites a bit string from the source operand to the destination. `CL`\nindicates the number of bits to be copied, and `(E)AX` indicates the\nlow order bit offset in the source. The bits are written to the low\norder bits of the destination register. For example, if `CL` is set\nto 4 and `AX` (for 16-bit code) is set to 5, bits 5-8 of `src` will\nbe copied to bits 0-3 of `dst`. This instruction is very poorly\ndocumented, and I have been unable to find any official source of\ndocumentation on it.\n\n`XBTS` is supported only on the early Intel 386s, and conflicts with\nthe opcodes for `CMPXCHG486` (on early Intel 486s). NASM supports it\nonly for completeness. Its counterpart is `IBTS` (see `IBTS`)."},
    "xchg": {"title":"`XCHG`: Exchange","description":"`XCHG` exchanges the values in its two operands. It can be used\nwith a `LOCK` prefix for purposes of multi-processor\nsynchronisation.\n\n`XCHG AX,AX` or `XCHG EAX,EAX` (depending on the `BITS`\nsetting) generates the opcode `90h`, and so is a synonym for\n`NOP` (`NOP`)."},
    "xlatb": {"title":"`XLATB`: Translate Byte in Lookup Table","description":"`XLATB` adds the value in `AL`, treated as an unsigned byte, to\n`BX` or `EBX`, and loads the byte from the resulting address (in\nthe segment specified by `DS`) back into `AL`.\n\nThe base register used is `BX` if the address size is 16 bits, and\n`EBX` if it is 32 bits. If you need to use an address size not\nequal to the current `BITS` setting, you can use an explicit\n`a16` or `a32` prefix.\n\nThe segment register used to load from `[BX+AL]` or `[EBX+AL]`\ncan be overridden by using a segment register name as a prefix (for\nexample, `es xlatb`)."},
    "xor": {"title":"`XOR`: Bitwise Exclusive OR","description":"`XOR` performs a bitwise XOR operation between its two operands\n(i.e. each bit of the result is 1 if and only if exactly one of the\ncorresponding bits of the two inputs was 1), and stores the result\nin the destination (first) operand.\n\nIn the forms with an 8-bit immediate second operand and a longer\nfirst operand, the second operand is considered to be signed, and is\nsign-extended to the length of the first operand. In these cases,\nthe `BYTE` qualifier is necessary to force NASM to generate this\nform of the instruction.\n\nThe `MMX` instruction `PXOR` (see `PXOR`) performs the same\noperation on the 64-bit `MMX` registers."},
    "xorpd": {"title":"`XORPD`: Bitwise Logical XOR of Double-Precision FP Values","description":"`XORPD` returns a bit-wise logical XOR between the source and\ndestination operands, storing the result in the destination operand."},
    "xorps": {"title":"`XORPS`: Bitwise Logical XOR of Single-Precision FP Values","description":"`XORPS` returns a bit-wise logical XOR between the source and\ndestination operands, storing the result in the destination operand."}
}
