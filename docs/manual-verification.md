# Manual verification matrix

Graphical assertions are deferred ([ADR 0009](./adr/0009-share-screen-keyboard-input-with-terminal.md)), so the compatibility target of [ADR 0003](./adr/0003-preserve-simulator-graphics-conventions.md) — existing EASy68K and MARS/RARS programs drawing what they draw in their own simulator — is checked by hand against this matrix. Automated tests cover the peripherals' pure logic; this document covers what only eyes can confirm: pixels, pacing and the feel of input.

## How to run it

1. `npm run dev` and open the project page for the environment under test. `npm test`, `npm run check` and `npm run lint` must already be clean.
2. Open the program named in the row from `examples/<environment>/`, Build, then Run. Compare against the reference screenshot or the reference simulator named in the row.
3. Walk the row's checks in order. A row passes only if every check passes; record the date, the browser and the result in the row's Result column, and file anything that fails before the phase closes.
4. Undo rows are run after the program has finished: step backwards through the instruction history and watch the Screen follow the program state ([ADR 0005](./adr/0005-restore-screen-state-on-undo.md)).
5. Reset rows use the Terminal's own reset path: Build, Clear execution, closing the project, and the start of each Testcase.

Each environment section is filled in by the phase that implements it (see [screen-peripherals-plan.md](./design/screen-peripherals-plan.md)); the hosting and measurement sections are filled in by phase 4 and phase 8.

`npm run measure` re-takes everything a Core can be driven through under node — every example program, the throughput, the loop lag, the frame pacing and the Screen journal — and prints the tables the "Headless runs" and "Measurements" sections below hold. The rows that only eyes can confirm are listed, with their steps, under "Rows still needing a browser".

## Z80 (phase 5)

Rows for the port-mapped Screen, Keyboard and Mouse of [ADR 0011](./adr/0011-z80-peripherals-through-the-port-map.md), with the three example programs written here (animation with double buffering, keyboard-driven movement, mouse painting). There is no reference simulator for the Z80 Screen — the port map is this project's own interface — so the reference column names the decision a row checks instead. The device's logic is covered by `src/lib/languages/Z80/Z80Device.test.ts` and every program below is run headlessly by `src/lib/languages/Z80/Z80Examples.test.ts`; these rows are what only eyes can confirm.

| #   | Program                                     | Checks                                                                                                                                                                                                | Reference          | Result                                                                                                                                                                                                         |
| --- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Z1  | `examples/z80/bouncing-ball.z80`            | The ball is never half drawn and never sits on a half-cleared background; it moves at the display's pace, not the host's; Stop answers at once                                                        | ADR 0006, ADR 0010 | Pass, phase 5 (2026-09-06, headless Chromium 151, embed page): 208 ink pixels at every sample, centroid moved, Stop after 65 ms                                                                                |
| Z2  | `examples/z80/keyboard-move.z80`            | The arrows move the square only while the Screen has focus, and only while a key is held                                                                                                              | ADR 0008           | Pass, phase 5: the square's left edge went 120 → 226 while the right arrow was held, then stayed at 228 after the release                                                                                      |
| Z3  | `examples/z80/mouse-paint.z80`              | The brush lands under the pointer, a drag that leaves the Screen keeps painting at the clamped edge, Shift paints red, the right button clears, no context menu                                       | ADR 0008           | Pass, phase 5, at zoom ×1. Other zooms: needs manual run, steps below                                                                                                                                          |
| Z4  | `examples/z80/keyboard-move.z80`            | The title is on the Screen in 8 by 8 cells and in the Terminal transcript at the same time, and the play area below it is redrawn without wiping it                                                   | ADR 0003, ADR 0011 | Pass, phase 5: 380 ink pixels in the top text rows, the same text in the transcript                                                                                                                            |
| Z5  | A program that prints and then reads port 0 | With no peripheral port used, the input prompt appears as it always has; after any Screen, Keyboard or Mouse port, the same read takes keystrokes from the focused Screen and echoes them both places | ADR 0009           | Pass, phase 5: the prompt appeared for the text-only program; the graphical one printed `hhii` (echo plus program) in both views                                                                               |
| Z6  | A program drawing two pixels                | Step forward until both pixels are on the Screen, then Undo: the Screen walks back with the code                                                                                                      | ADR 0005           | Pass, phase 5: ink 0 → 1 → 2 over twelve steps, then 2 → 1 → 0 over the undos                                                                                                                                  |
| Z7  | Any of the three                            | Build, Clear execution and closing the project each leave a blank Screen, and the keyboard and mouse state is forgotten with it                                                                       | Design record      | Pass, phase 5 for Stop and phase 8 for Build (`Z80Emulator.test.ts`, under node: a rebuild blanks the pixel, empties the typed queue and forgets the last click). Project close: needs manual run, steps below |
| Z8  | A testcase over a drawing program           | Scripted input still answers the character port line by line, waits complete at once, the elapsed-time port starts at zero, and the Screen resets per testcase                                        | ADR 0002, ADR 0010 | Pass, phase 8, under node (`Z80Emulator.test.ts`): the scripted `q` answered the character port, a tenth of a second of program time passed at once and the elapsed-time port read 10                          |
| Z9  | A program using command 10                  | A resize changes the panel's backing store, its header and its zoom, and clears the Screen                                                                                                            | ADR 0011           | Pass, phase 5: the header went 256 × 192 → 128 × 128 and the Screen showed the color it was cleared to                                                                                                         |
| Z10 | The Z80 I/O documentation page              | The five groups render with their headings, the command table, the color swatches and the examples                                                                                                    | ADR 0011           | Pass, phase 5: five group headings, 27 port cards, 14 commands, 3 mouse views, 11 color swatches                                                                                                               |

## M68K (phase 6)

Rows for the `trap #15` tasks, run against the programs in `examples/m68k/`. The reference is EASy68K itself: its own example programs are checked in verbatim under `examples/m68k/easy68k/` with their license notes, and the runnable programs here are ports of them (`graphics-tour.x68` from `graphicSound.X68`, `mouse-paint.x68` from what `mouseWindowSize.X68` shows of task 61). The adapter's logic is covered by `src/lib/languages/M68K/M68KEmulator.test.ts`, which runs every example against the real Core under node and looks at the pixels; these rows are what only eyes can confirm.

| #   | Program                                   | Checks                                                                                                                                                                                           | Reference          | Result                                                                                                                                                    |
| --- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | `examples/m68k/graphics-tour.x68`         | Every drawing task draws what EASy68K draws: pen and fill colors, pixel, lines, filled and unfilled rectangle and ellipse, flood fill, pen width, and text at a pixel position                   | `graphicSound.X68` | Pass, phase 6 (2026-09-06, headless Chromium 151, embed page): 94 329 lit pixels, top colors blue 23 997, white 22 204, red 19 328, green 19 200          |
| M2  | `examples/m68k/graphics-tour.x68`         | Drawing mode 2 moves the drawing point and leaves the pixels alone; mode 4 draws from where it left off. Tasks 83, 96 and 33 answer with what is on screen                                       | ADR 0003           | Pass, phase 6: pinned in `M68KEmulator.test.ts`; on screen the mode 4 line starts where the mode 2 one ended, and the two reads are printed at the cursor |
| M3  | `examples/m68k/bouncing-ball.x68`         | The ball is never half drawn and never on a half-cleared background; it moves at the pace of task 23, not the host's; Stop answers at once                                                       | ADR 0006, ADR 0010 | Pass, phase 6: 1 804 lit pixels at every one of five samples, centroid moved 199 → 599, Stop answered in 207 ms                                           |
| M4  | `examples/m68k/keyboard-move.x68`         | Task 19 moves the square only while the Screen has focus and only while the key is held; the editor's own shortcuts stay quiet                                                                   | ADR 0008           | Pass, phase 6: centroid 238 → 426 while the right arrow was held and unchanged after the release; a `window` keydown listener never fired                 |
| M5  | `examples/m68k/mouse-paint.x68`           | Task 61 paints under the pointer through a drag, Shift paints red, the right button clears, no context menu                                                                                      | ADR 0008           | Pass, phase 6, at the panel's fit zoom: 1 232 aqua pixels along the drag, 784 red with Shift, the screen cleared on the right button, menu cancelled      |
| M6  | `examples/m68k/keyboard-move.x68`         | The title is on the Screen at the text cursor and in the Terminal transcript at the same time                                                                                                    | ADR 0003           | Pass, phase 6: `Arrow keys move the square` in both, redrawn on every frame without wiping the play area                                                  |
| M7  | A program that draws, then reads a string | With no graphics task the input prompt appears as it always has; after any graphics, keyboard or mouse task the same read takes keystrokes from the focused Screen and echoes them to both views | ADR 0009           | Pass, phase 6: no prompt appeared, `hi` typed on the canvas showed as the echo and again as the program's own output, on the Screen and in the transcript |
| M8  | A program drawing two pixels              | Step forward until both pixels are on the Screen, then Undo: the Screen walks back with the code                                                                                                 | ADR 0005           | Pass, phase 6, under node (`M68KEmulator.test.ts`): ink 2 → 1 → 0 over the undos. In the GUI: needs manual run, steps below                               |
| M9  | A testcase over a drawing program         | Scripted input answers the reads, a delay completes at once, the clock starts at zero, and the Screen resets per testcase                                                                        | ADR 0002, ADR 0010 | Pass, phase 6, under node: a five second delay finished immediately, task 8 answered 500, the drawing and the printed text were on the Screen             |
| M10 | Any of the four                           | Build and Clear execution each leave a blank Screen, and the keyboard and mouse state is forgotten with it                                                                                       | Design record      | Pass, phase 6: blank after Build and after Stop, which is this editor's Clear execution. Project close: needs manual run, steps below                     |
| M11 | The M68K trap tasks documentation page    | The four groups render with their tasks, the colors, the key codes, the rejected tasks and the deviations, and the page is linked from the index, the sidebar and the all page                   | ADR 0003           | Pass, phase 6: 39 task cards, 16 color swatches, 30 key codes, six headings, no horizontal overflow; the all page shows the same 39                       |
| M12 | A program using task 10, 21 or 92 mode 14 | The program stops with an error naming the task or the mode and saying why                                                                                                                       | Design record      | Pass, phase 6, under node: `Trap task 10 (print to the printer) is not supported: the editor has no printer`, and the Core's own message for the mode     |

## MIPS (phase 7)

Rows for the MARS bitmap display and the keyboard and display simulator, with the five display parameters, the memory re-sync after Undo and the transmitter's Terminal output.

| #   | Program                              | Checks                                                                                                                                                  | Reference                    | Result                                                                                                                                                                     |
| --- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | `examples/mips/bitmap-tour.asm`      | One word is one pixel and its low 24 bits are the color, over a whole grid: the ramp, the blue square and the white border land where MARS puts them    | `BitmapDisplay.java`         | Pass, phase 7 (2026-09-06, headless Chromium 151, embed page, 256 × 256): 65 536 lit, centre `#0000ff`, ramp `#c82800`, first and last rows `#ffffff`                      |
| P2  | The display popover                  | The five parameters offer MARS's choices and defaults, and changing one re-syncs the Screen from memory at once without re-running the program          | `BitmapDisplay.java`         | Pass, phase 7: the lists are 1-32, 64-1024 and the five base addresses; display width 512 → 256 took the panel from 512 × 256 to 256 × 256 with no rebuild                 |
| P3  | `examples/mips/bouncing-ball.asm`    | Syscall 32 paces the animation and syscall 30 reports program time from zero; the GUI stays responsive while the program sleeps                         | ADR 0010                     | Pass, phase 7, under node (`MIPSEmulator.test.ts`): the ball is on its background and the elapsed-time line is printed. Browser run not sampled frame by frame             |
| P4  | `examples/mips/keyboard-display.asm` | The receiver's Ready bit and data register answer from the focused Screen's keyboard, one character per read, and the queue never drops one             | `KeyboardAndDisplaySim.java` | Pass, phase 7: clicking the canvas and typing `a` then `b` painted the first two cells `#c2c261` and `#c4c462`                                                             |
| P5  | A program storing to `0xffff000c`    | The transmitter is always Ready, a stored character reaches the console transcript, and ASCII 12 clears it                                              | `KeyboardAndDisplaySim.java` | Pass, phase 7: `Hi!` in the console on the embed page; the form feed is pinned in `MIPSEmulator.test.ts`                                                                   |
| P6  | A program storing 2 to `0xffff0000`  | The program stops with an error naming interrupt-driven I/O and the register it was enabled on                                                          | Design record                | Pass, phase 7: `Interrupt-driven I/O is not supported: the program set the interrupt-enable bit (bit 1) of the receiver control register at 0xffff0000. …`                 |
| P7  | A program storing two pixels         | Step forward until both are on the Screen, then Undo: the image walks back with the code, re-read from the memory the Core rolled back                  | ADR 0005                     | Pass, phase 7, under node. **Blocked in the GUI** by a pre-existing Core defect: see the phase 7 status log and the browser rows below, `canUndo` is false after any Build |
| P8  | A testcase over a drawing program    | Scripted input answers the reads, a sleep completes at once on the virtual clock, syscall 30 starts at zero, and the Screen resets per testcase         | ADR 0010                     | Pass, phase 7, under node: a five second sleep finished in under two seconds of wall time and syscall 30 answered 5000                                                     |
| P9  | The MIPS screen documentation page   | The bitmap display, the four registers, program time and the differences render; the page is linked from the index, the sidebar and the complete page   | Design record                | Pass, phase 7: four sections, `0xffff000c` present, no horizontal overflow, the same content on `/documentation/mips/all#screen`                                           |
| P10 | `examples/mips/bitmap-tour.asm`      | Build and Clear execution each leave a blank Screen in the configured geometry rather than in the language default, and the keyboard state goes with it | Design record                | Pass, phase 7: blank 256 × 256 after Build, 65 536 lit after Run, blank 256 × 256 again after Stop. Project close: needs manual run, steps below                           |

## RISC-V (phase 7)

The MIPS rows repeated against the RARS samples; the two simulators are identical here, so a divergence between the two sections is itself a defect.

| #   | Program                              | Checks                                                                                                                                     | Reference                    | Result                                                                                                                                    |
| --- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| V1  | `examples/risc-v/bitmap-tour.s`      | As P1, through the RARS Core                                                                                                               | `BitmapDisplay.java` (RARS)  | Pass, phase 7 (2026-09-06, headless Chromium 151, embed page): 65 536 lit at the default 512 × 256, and the tour's own picture under node |
| V2  | The display popover                  | As P2: the same five parameters, since RARS's tool is a port of MARS's                                                                     | `BitmapDisplay.java` (RARS)  | Pass, phase 7: the popover is the same component and answered the same on the RISC-V embed page                                           |
| V3  | `examples/risc-v/bouncing-ball.s`    | As P3, with ecall 32 and ecall 30                                                                                                          | ADR 0010                     | Pass, phase 7, under node (`RISC-VEmulator.test.ts`)                                                                                      |
| V4  | `examples/risc-v/keyboard-display.s` | As P4                                                                                                                                      | `KeyboardAndDisplaySim.java` | Pass, phase 7, under node: `abq` typed, echoed and painted                                                                                |
| V5  | A program storing to `0xffff000c`    | As P5                                                                                                                                      | `KeyboardAndDisplaySim.java` | Pass, phase 7, under node                                                                                                                 |
| V6  | A program storing 2 to `0xffff0000`  | As P6                                                                                                                                      | Design record                | Pass, phase 7, under node                                                                                                                 |
| V7  | A program storing two pixels         | As P7                                                                                                                                      | ADR 0005                     | Pass, phase 7, under node. Blocked in the GUI by the same pre-existing Core defect as P7                                                  |
| V8  | A testcase over a drawing program    | As P8                                                                                                                                      | ADR 0010                     | Pass, phase 7, under node                                                                                                                 |
| V9  | The RISC-V screen documentation page | As P9                                                                                                                                      | Design record                | Pass, phase 7: four sections, `0xffff000c` present, no horizontal overflow, the same content on `/documentation/risc-v/all#screen`        |
| V10 | `examples/risc-v/bitmap-tour.s`      | As P10                                                                                                                                     | Design record                | Pass, phase 7, under node (`RISC-VEmulator.test.ts`); the browser run of P10 was done on the MIPS half                                    |
| V11 | RV64                                 | A RISC-V-64 project's memory map: the registers at `0xffff0000` are untested in 64 bit mode and the Core's own smoke test only covers RV32 | Phase 7 Core notes           | Needs manual run, steps below                                                                                                             |

## x86

No Screen: `@specy/x86` wraps a Linux userland emulator with no graphics device, so x86 keeps Terminal I/O only and its program time stays the real clock, including during Testcases. The rows here only confirm that the Screen panel is absent, that nothing about the Terminal changed, and that the run still answers Stop.

| #   | Program                     | Checks                                                                                                    | Reference     | Result                                                                                                                            |
| --- | --------------------------- | --------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| X1  | Any x86 project             | No Screen panel at all, on the project page and in the interactive editor, whatever the screen setting is | Design record | Pass, phase 4 (row H3) and phase 8: `languageHasScreen('X86')` is false and the panel is not rendered                             |
| X2  | A program printing a string | The Terminal transcript is what it was before the peripherals existed, and input still prompts            | Design record | Pass, phase 8, under node: the Core loads and runs, `stdOut` unchanged, no Screen is ever drawn on                                |
| X3  | A compute-only program      | The GUI stays responsive and Stop is answered while Blink runs                                            | ADR 0007      | Pass, phase 8, under node: 35 ms median loop lag after the slice estimate was corrected, against 901 ms and a 17 s Stop before it |

## Headless runs (phase 8)

Every program in `examples/`, run through the Emulator and its peripherals under node — which every Core loads, x86's Blink included. `npm run measure` prints this table (`src/lib/languages/measurements/examples.measure.ts`); it also fails if a program is checked in that no row covers. The assertions about what these programs draw live in the automated suite (`Z80Examples.test.ts`, `M68KEmulator.test.ts`, `MIPSEmulator.test.ts`, `RISC-VEmulator.test.ts`); this table is the run itself.

A program that runs until Stop is given the app's own instruction limit (two million) and a two and a half second wall-clock budget on top of it, because a program paced by waits spends no instructions while it waits. The Screen column is the visible image at the last sample before Stop: **ink** is the pixels that differ from the commonest color, which in every one of these programs is the background it cleared to.

| Core   | Program                            | Exercises                                          | Result                                                            | Screen                               |
| ------ | ---------------------------------- | -------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------ |
| Z80    | `z80/bouncing-ball.z80`            | double buffering, frame sync                       | ran until Stop, 2.5 s                                             | 208 ink, 3 colors, 256 × 192         |
| Z80    | `z80/keyboard-move.z80`            | key state polling, text on both windows            | ran until Stop, 2.5 s                                             | 524 ink, 3 colors, 256 × 192         |
| Z80    | `z80/mouse-paint.z80`              | mouse polling, its views and flags                 | ran until Stop, 2.5 s                                             | 36 ink, 2 colors, 256 × 192          |
| M68K   | `m68k/graphics-tour.x68`           | every drawing task, 80 to 96                       | ran to termination                                                | 94 329 ink, 10 colors, 640 × 480     |
| M68K   | `m68k/bouncing-ball.x68`           | double buffering (92, 94) and program time (23)    | ran until Stop, 2.6 s                                             | 1 804 ink, 3 colors, 640 × 480       |
| M68K   | `m68k/keyboard-move.x68`           | key state (19), text to both windows               | ran until Stop, 2.6 s                                             | 2 550 ink, 3 colors, 640 × 480       |
| M68K   | `m68k/mouse-paint.x68`             | the mouse task (61), its flags and its pixels      | ran until Stop, 2.6 s                                             | 1 732 ink, 3 colors, 640 × 480       |
| M68K   | `m68k/easy68k/graphicSound.X68`    | EASy68K's own graphics and sound reference         | does not assemble here, on purpose (138 errors, from `start:` on) | —                                    |
| M68K   | `m68k/easy68k/mouseWindowSize.X68` | EASy68K's own mouse and window reference           | does not assemble here, on purpose (64 errors)                    | —                                    |
| M68K   | `m68k/easy68k/clockDigital.X68`    | EASy68K's own animated clock reference             | does not assemble here, on purpose (88 errors)                    | —                                    |
| M68K   | `Bad_Apple.s68k`                   | the repository's own animation: task 23 and memory | ran until Stop, 2.6 s                                             | blank, and nothing printed           |
| MIPS   | `mips/bitmap-tour.asm`             | the bitmap display over a whole grid               | ran to termination                                                | 61 440 ink, 60 930 colors, 256 × 256 |
| MIPS   | `mips/bouncing-ball.asm`           | sleep (32) and program time (30)                   | ran until Stop, 2.5 s                                             | 36 ink, 2 colors, 128 × 128          |
| MIPS   | `mips/keyboard-display.asm`        | the four memory-mapped registers                   | ran until Stop, 2.5 s                                             | 2 ink, 3 colors, 64 × 32             |
| RISC-V | `risc-v/bitmap-tour.s`             | the bitmap display over a whole grid               | ran to termination                                                | 61 440 ink, 60 930 colors, 256 × 256 |
| RISC-V | `risc-v/bouncing-ball.s`           | sleep (32) and program time (30)                   | ran until Stop, 2.5 s                                             | 36 ink, 2 colors, 128 × 128          |
| RISC-V | `risc-v/keyboard-display.s`        | the four memory-mapped registers                   | ran until Stop, 2.5 s                                             | 2 ink, 3 colors, 64 × 32             |
| x86    | none checked in                    | that the Core loads and runs at all under node     | loads and runs                                                    | no Screen, by design                 |

Three things the runs say that the rows above do not:

- **The two bitmap tours draw the same picture, pixel for pixel**: 61 440 ink over 60 930 colors on both Cores, which is what "MARS and RARS are identical here" means in practice.
- **The three EASy68K programs are reference material, not runnable here**, exactly as `examples/m68k/README.md` says: `@specy/s68k` has none of EASy68K's structured control statements, `SIMHALT`, `END START` or `OPT`, and they call tasks this editor rejects on purpose. The runnable ports next to them are what the M rows exercise.
- **`Bad_Apple.s68k` prints nothing and draws nothing**, and that is correct: its only trap is task 23, and it writes its frames into memory for the memory viewer to show. Since phase 6 reads task 23 in hundredths of a second, as EASy68K's help does, its `#31` delay is 310 ms rather than 31 ms and the whole animation takes ten times as long as it did before.

## Hosting surfaces (phases 4 and 8)

The Screen panel appears in the project page's right column and in the interactive editor, so every surface that embeds the editor has to be walked: project page, lecture pages, the exam page, embeds and chat, in both the fullscreen and the small layout. Focus indication, editor-shortcut suppression and context-menu suppression are checked on each.

| #   | Surface                                           | Checks                                                                                                                                                                            | Result                                                                             |
| --- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| H1  | Project page, M68K                                | Panel in the right column under memory, header reads the language and 640 × 480, transcript still at the bottom, editor unshrunk                                                  | Pass, phase 4 (2026-09-06)                                                         |
| H2  | Project page, Z80                                 | Integer zoom ×1 on a 256 × 192 Screen                                                                                                                                             | Pass, phase 4                                                                      |
| H3  | Project page, x86                                 | No Screen panel at all                                                                                                                                                            | Pass, phase 4                                                                      |
| H4  | Project page, narrow window                       | Panel spans the column without widening the page                                                                                                                                  | Pass, phase 4 (700 px viewport)                                                    |
| H5  | Project page, focus and shortcuts                 | Click gives the canvas focus and a visible ring; Shift+C never reaches the window handler; Esc releases and the keys return                                                       | Pass, phase 4 (driven through CDP)                                                 |
| H6  | Project page, mouse                               | Pointer capture held through a drag that leaves the canvas, released on button up; context menu and middle click both cancelled                                                   | Pass, phase 4                                                                      |
| H7  | Project page, zoom toggle                         | Actual size shows 640 × 480 inside the panel's own scroll box without widening the page                                                                                           | Pass, phase 4                                                                      |
| H8  | Interactive editor, small layout                  | Show/Hide screen bar above the transcript, panel appears below it (embed page, lecture page)                                                                                      | Pass, phase 4                                                                      |
| H9  | Interactive editor, fullscreen layout             | Panel between the memory column and the transcript                                                                                                                                | Pass, phase 4 (scratch route). The chat page itself: needs manual run, steps below |
| H10 | Exam page                                         | Small layout, toggle present, nothing else moved                                                                                                                                  | Pass, phase 4 (exam authoring page). A live session: needs manual run, steps below |
| H11 | Rendering                                         | Drawing on the Screen reaches the canvas within a frame and clears `dirty`                                                                                                        | Pass, phase 4 (rectangle, line and text drawn from the console)                    |
| H12 | Resize                                            | A program's resize follows through to the backing store, the header and the zoom                                                                                                  | Pass, phase 4; rechecked in phase 5 after fixing a canvas left blank by a resize   |
| H13 | Project page, a modifier released over the Screen | Hold Shift outside the canvas, click the canvas, release Shift over it, click a neutral part of the page and press `r`: nothing runs, because the page no longer holds Shift down | Needs manual run, steps below (review fix, 2026-09-06)                             |
| H14 | Project page, Escape and a rebound shortcut       | Bind Clear execution to Escape in the shortcuts panel, focus the canvas, press Escape: the panel releases focus and the execution is not cleared                                  | Needs manual run, steps below (review fix, 2026-09-06)                             |

## Rows still needing a browser (phase 8)

Everything a Core can be driven through under node is covered by the automated suite and by the headless runs above. What is left needs eyes, a pointer, or a page that only exists in a session; each row here names the steps, so a later run does not have to reconstruct them.

| Row                                      | Steps                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Z3, other zooms                          | `npm run dev`, new Z80 project, paste `examples/z80/mouse-paint.z80`, Build, Run, click the canvas, turn zoom-to-fit off with the panel's 1:1 button and drag across the canvas: the brush lands under the pointer at that zoom too, and the drag keeps painting at the clamped edge when it leaves                                                                                              |
| Z7, M10, P10, V10, closing a project     | With the program stopped, leave the project (the back arrow to the project list) and open it again: the Screen is blank, and a key typed on it before the close is gone                                                                                                                                                                                                                          |
| M8, Undo in the GUI                      | M68K project, a program that draws two pixels (tasks 80 and 82 twice, then task 9), Build, Step until both are on the Screen, then press Undo repeatedly: the pixels disappear one at a time, newest first                                                                                                                                                                                       |
| P7, V7, Undo in the GUI                  | Blocked by a pre-existing Core defect, not by this feature: `assemble()` reallocates the backstep buffer that MARS and RARS keep as a singleton, so the semantic check that follows every Build empties the undo history and the Undo button is disabled. Re-run these once the Cores keep it per instance                                                                                       |
| V3 to V9, RISC-V in a browser            | The MIPS rows P3 to P9 repeated on a RISC-V project page with `examples/risc-v/`: the two adapters share `src/lib/languages/mars/`, and the node suite runs both, so this is a spot check rather than a second full pass                                                                                                                                                                         |
| V11, RV64                                | New RISC-V-64 project, `examples/risc-v/keyboard-display.s`, set the display to 8 × 8 units and 512 × 256, Build, Run, click the canvas and type: the memory map at `0xffff0000` is untested in 64 bit mode and may not exist there                                                                                                                                                              |
| H13, a modifier released over the Screen | Z80 or M68K project, Build so the run controls are live. Hold Shift down outside the canvas, click the canvas (which focuses it), release Shift over it, then click a neutral part of the page and press `r` alone: nothing must happen. Before the review fix the panel swallowed the release, the page kept `ShiftLeft` in its held-key map, and `r` alone ran the program as `ShiftLeft+KeyR` |
| H14, Escape and a rebound shortcut       | Open the shortcuts panel and bind Clear execution to Escape, then focus the Screen and press Escape: the canvas releases focus (its own gesture) and the execution is **not** cleared. ADR 0008 gives every key of a focused Screen to the program, Escape included as far as the page is concerned                                                                                              |
| H9, the chat page                        | Open the chat page, ask the agent for any program so the editor mounts, then check the Screen panel sits between the memory column and the transcript in the fullscreen layout                                                                                                                                                                                                                   |
| H10, a live exam session                 | Create an exam with a track, take it as a student, and check the small layout's Show screen toggle is there and nothing else moved                                                                                                                                                                                                                                                               |

## Measurements (phase 8)

The numbers behind the tuning decisions, taken with `npm run measure` on 2026-09-06 under node 24 and summarized, with the reasoning, in [screen-peripherals.md](./design/screen-peripherals.md). Targets from [ADR 0007](./adr/0007-generic-emulator-run-scheduling.md): yields cost under five percent of compute-only throughput on every Core, and Stop is answered within a tenth of a second. Both are met on every Core.

| Measurement                                               | Environment | Method                                                                                                                                             | Value                                                                                                   |
| --------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Instructions per second, no yields                        | all five    | A compute-only loop run as one slice, `throughput.measure.ts`                                                                                      | Z80 11.4 M, M68K 19.6 M, MIPS 1.23 M, RISC-V 26.9 k, x86 10.6 k                                         |
| Instructions per second, with yields                      | all five    | The same loop through `run()`, which slices and yields                                                                                             | Z80 11.2 M, M68K 18.8 M, MIPS 1.18 M, RISC-V 26.5 k, x86 10.5 k — 1.4% to 4.3% of throughput            |
| Stop latency                                              | all five    | How late a 10 ms timer fires while a program runs (the host is held for the whole slice, so a click is not delivered until it ends), then the Stop | Worst tick 85 ms, nothing over 100 ms; Stop itself answered in 0.7 to 2.6 ms                            |
| Frame pacing of the animation examples                    | four        | 120 frames of each `bouncing-ball`, timed at the wait the program paces itself with, `animation.measure.ts`                                        | Z80 17.0 ms, M68K 24.0 ms (asks 20), MIPS 17.2 ms (asks 16), RISC-V 18.4 ms (asks 16)                   |
| Screen history bytes of the animation examples            | four        | The journal released by 100 undo steps after 120 frames                                                                                            | Z80 6.4 MB, M68K 58.8 MB, MIPS and RISC-V 0 (framebuffer mode journals nothing)                         |
| Key hold interval, `DEFAULT_KEY_HOLD_INTERVAL_MS`         | all         | Frame pacing above: a program polls once a frame, so a held key must survive one                                                                   | Unchanged at 30 ms, which covers the 17 to 24 ms frame                                                  |
| Double-click interval, `DEFAULT_DOUBLE_CLICK_INTERVAL_MS` | all         | Not measurable here: it is a human interval, and EASy68K's flag means Windows' own                                                                 | Unchanged at 500 ms, Windows' default                                                                   |
| Screen slice budget, `SCREEN_SLICE_MS`                    | all         | Frame pacing above                                                                                                                                 | Unchanged at 16 ms, one display frame                                                                   |
| Compute slice budget, `COMPUTE_SLICE_MS`                  | all         | Loop lag above: the host is held for as long as the slice runs                                                                                     | 100 → 50 ms, half the ADR's tenth of a second                                                           |
| Instructions per millisecond, `M68K_INSTRUCTIONS_PER_MS`  | M68K        | Compute loop                                                                                                                                       | Measured 18 800, set to 15 000                                                                          |
| Instructions per millisecond, `Z80_INSTRUCTIONS_PER_MS`   | Z80         | Compute loop                                                                                                                                       | Measured 11 000, set to 10 000                                                                          |
| Instructions per millisecond, `MIPS_INSTRUCTIONS_PER_MS`  | MIPS        | Compute loop                                                                                                                                       | Measured 1 200, left at 1 000                                                                           |
| Instructions per millisecond, `RISCV_INSTRUCTIONS_PER_MS` | RISC-V      | Compute loop, with the shipped undo history; RARS runs a `j` loop at 26 and everything else at 150 to 320                                          | Measured 27, set to 25 (was 1 000, which held the host 3.7 s a slice)                                   |
| Instructions per millisecond, `X86_INSTRUCTIONS_PER_MS`   | x86         | Compute loop                                                                                                                                       | Measured 11, set to 10 (was 2 000, which answered Stop 17 s late)                                       |
| Screen history budget default, `screenHistoryBudgetMb`    | all         | The heaviest animation example's 100 undo steps                                                                                                    | Unchanged at 64 MB, against the M68K ball's 58.8 MB; `DEFAULT_SCREEN_HISTORY_BYTES` 32 → 64 MB to match |
